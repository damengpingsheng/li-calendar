# R1–R6 左缘收缩：新一轮分析与可能原因

日期：2026-09-09  
评审基线：`8972ebe`，当前实现 `b66cfb3`（R6）  
对象：`总结-退出全屏左缘收缩排障R1-R6.md`

本轮只读核对文档、代码与历史日志，未重新操作 PotPlayer、启用调试、构建或部署。保留用户已经接受现状、暂停优化的决定；下列实验仅供以后恢复工作时使用。

## 1. 总体判断

R6 去掉原生归位后的额外稳定等待，符合用户“收缩开始得更早”的明确诉求。真机反馈“大幅改善”应作为有效进展保留，不应因仍可察觉就否定前面的修复。

但以下结论仍超出了证据：

- 回摆必然由 PotPlayer/系统单独产生，与覆盖层代码无关。
- 收缩已达到物理极限，端到端延迟不超过 150ms。
- 扩展持续时间已经不可压缩，剩下只能接受或做动画。

当前更合理的解释是：**遮盖边界本身会产生可见变化，同时仍可能存在观测延迟、收缩后跟踪中断，以及覆盖层与任务栏层级控制相互影响。** 底色色差是候选之一，不宜先默认它是最后原因。

## 2. 证据有明显进步，但应分清观测与归因

23:39 的完整日志确实出现了 3660 与 3618 交替，例如：

```text
40.220  3660
40.575  phase->normal / 置顶
40.589  phase->covered / 插入播放器之后
40.806  3618
41.186  起，多次 z-reclaim early
41.336  3660
41.722  phase->normal / 置顶
41.799  phase->covered
41.954  3618
```

这比只摘取非正常值更有说服力：探测输出确实交替。但仍有三个边界：

1. 这是 R4 阶段日志，不能直接证明 R6 的剩余感知仍具有相同次数和时长。
2. `uia rect=` 包含定位函数的 HWND 快速路径和 UIA 路径，尚未记录每次具体来源、控件身份和探测耗时；它不是像素画面记录。
3. 没有对应单次退出操作的同步视频，以及关闭覆盖层后的对照，不能据此断言是“系统单独产生的约 5 秒动画”。

尤其是记录中 z 操作与矩形交替紧密相邻。相关性不等于因果，但足以要求排除应用自身参与的反馈过程。

## 3. 可能原因，按优先验证顺序

### A. 高优先级假设：置顶/潜入控制可能参与回摆

目前逻辑一方面根据任务栏左侧三点判断 Covered，另一方面在时钟中心命中任务栏或自己时提前 HWND_TOPMOST，随后 Covered 又把覆盖层插到播放器之后。

两个区域的实际遮挡可以不同，两套控制条件因此可能来回切换。日志中一个短时段内出现多次 `z-reclaim early`，紧接着又进入 Covered，说明应当观测实际层级，而不仅是阶段变量。

更重要的是，命中覆盖层自己也执行置顶并清空 LAST_FOLLOW_POS，不能说明当前层级真的需要修复。这可能增加无必要的层级更新。是否进一步引起任务栏或播放器响应，当前未证实。

**验证方式：**关闭应用后，只读观测原生时钟，在同样操作下是否仍出现同频率的矩形往返；再与应用开启时比较。若开启才出现或明显加重，应先查层级反馈，而不是接受“系统物理下限”。如需进一步隔离，可在诊断版本保留只读探测，停用几何/z 操作做单变量比较。

### B. 代码可确认：150ms 是 sleep 参数，不是端到端上限

`mouse_hook.rs` 重探循环的顺序为：

```text
计算间隔 → sleep → 更新覆盖状态 → 按条件探测 → 重贴
```

因此实际延迟还包括探测耗时、线程调度、窗口调用、渲染呈现，以及状态刚变化时已经开始的旧间隔睡眠。稳态采用 500ms 调用、每四次重探，不能用加密阶段的参数概括所有退出路径。

“收到读数后 1ms 完成窗口操作”只能证明后半段快，不能推导“原生真实归位后 1ms”或“不超过 150ms”。

### C. 代码可确认：首次收缩后，回摆跟踪可能过早结束

`relocate_clock_overlay_endorsed` 收缩后清空：

```text
mask = None
native_observed = None
shrink_requested = false
```

而轮询加密条件中，Normal 只有 mask 存在时才返回 150ms；“3 秒内见过 Visible”的补充只用于 Covered+mask。

因此存在以下路径：

1. 第一次归位并收缩，phase 为 Normal；
2. mask/native_observed 被清空，加密轮询停止；
3. 原生随后再次偏离，但任务栏左侧探测仍为 Visible；
4. `note_probe` 在 Normal 下仅当 mask 或 native_observed 已存在，才把非认可值送进遮盖数据；此时两者都没有，读数只进入 8 秒候选路径。

这意味着 R6 的“退出轮回摆逐读数跟踪”并非覆盖所有分支：**首次收缩之后可能既降频，又不再把 Normal 下的新偏离当成遮盖依据。** 直到再次进入 Covered 等其他路径介入才恢复。

这是静态代码可达风险，尚未证明对应用户这次剩余感知。将来若修复，宜独立保存“退出观测窗口/本轮仍需短时跟踪”的状态；观测窗口可以延续，但不能重新变成延迟收缩的门槛。

### D. 代码可确认：扩展与收缩并非对称的同周期实时处理

循环先 update，再探测。探测到非认可位后，若 mask 已存在且没有收缩请求，`relocate` 会直接返回；新遮盖范围不一定在本轮应用，可能等下一个 update。

若已有 mask，Visible 分支也主要保持现有 mask，仅 mask 为空时从 native_observed 构建新并集。这不是对每个新矩形无条件实时更新遮盖。

相反，相等样本会请求收缩并在后续 relocate 执行，收缩快并不能证明扩展也同样快。应分别统计探测到偏离→扩展、探测到归位→收缩。

### E. 中高优先级：收缩使用分离的尺寸与位置调用

扩展路径可以通过一次 SetWindowPos 同时设置位置和尺寸；收缩经过 `apply_overlay_geometry`，仍分别调用 `window.set_size` 和 `window.set_position`。

从 `(3618,2076,3818,2160)` 收缩到 `(3660,2076,3818,2160)` 时，如果中间状态被呈现，先缩宽到 158、左缘暂留 3618，会临时把右缘带到 3776。由于内容相对窗口右缘对齐，文字也可能出现短暂移动或裁切。

这不意味着中间状态一定上屏，但操作后最终矩形正确不能排除它。应使用连续画面验证，必要时用一次完整 RECT 应用做对照。并发旧快照应用问题也仍存在：状态读出后释放锁，窗口操作和状态清理之间没有轮次/版本检查。

### F. 中优先级：背景边界和采样误差

遮盖宽度变化 42 物理像素，即便文字完全静止，背景色差、相邻图标重新露出，也足以让左缘变化可见。因此“可察觉”不必等同于“动画太慢”。

不过文档所说“采显示桌面细条”并非所有状态下都成立。采样实际依赖共享缓存的 `clock.right + [3,8,13]`：

- 正常值 3818 时，点在 3821/3826/3831；
- 全屏值 3769 时，点在 3772/3777/3782，已进入覆盖层或其他内容可能所在区域。

采到自己、播放器或其他内容会污染底色；前端配色主要在初始化/主题切换时拉取，也不会自动保证退出后与局部底色一致。此问题取决于当时是否重新采色，不能假定每次退出都发生。

“改成本地采样”也需避免直接采到已经展开的遮盖本身。正确实验应取得未被自身窗口覆盖的参考，而不是采样同一个错误色再反馈回去。

## 4. 不赞成现在称为物理极限

当前限制更准确地说是“选定的外部探测＋不透明遮盖方案下的时序与视觉权衡”。

- 控件矩形偏离，不代表该矩形每一部分都存在可见文字像素。
- 原生文字在任务栏实际露出之前偏离，不一定需要用户可见的遮盖。
- 采样延迟不是硬件物理下限。
- 是否存在应用自身驱动的回摆尚未通过对照排除。

即使最后确认剩余变化不可避免，也应在完成这些区分后收窄结论。用户接受暂停是产品取舍，不需要用“已经达到物理极限”来解释。

## 5. 对未来建议的评审

### 截图方向正确，但 200ms 间隔不足以分析短暂变化

200ms 只有 5 帧/秒，会漏掉 1～150ms 的变化，也难以判断 150ms 量化的影响。将来恢复时，宜短时录制时钟局部的 60fps 或更高帧率画面，并与单调时间日志对齐。画面和探测分开采集，不在钩子回调里增加重任务。

明确记录一次退出操作，分别标出最后视频帧、原生内容出现、自定义文字出现、左缘开始回缩和最终稳定。

### 本地底色应先验证污染来源

先比较扩展区与同位置无遮盖参考画面，区分色差、图标遮挡和原生文字碎片。直接换采样点可能只是把自采样问题换个位置。

### 不优先建议 CSS 收缩动画

实际变化由原生窗口的位置/尺寸控制，CSS 过渡不会自动插值外层 HWND。需要重新设计内容与背景容器，才能避免文字跟随。

此外，用户已经澄清希望更早开始收缩；动画又增加一段边界可见的持续时间。扩展动画还可能在尚未盖满时暴露原生文字。因此只应在明确证明是跳变观感而非延迟后做可撤销对照，不宜作为默认下一步。

## 6. 如果恢复工作，最有价值的最小实验

1. **原生基线对照**：应用关闭与开启，各做相同 PotPlayer 单次退出，记录原生矩形、对象身份、实际画面。先判断回摆是否独立于覆盖层存在。
2. **分解时延**：记录探测开始/结束、来源、读数、状态版本、实际 RECT 操作及画面；不要仅测日志读数到 SetWindowPos 的时间。
3. **验证首次收缩后的漏洞**：重点捕获 Normal+mask=None 期间再次出现非认可值，检查是否降频及漏扩展。
4. **单变量比较收缩应用方式**：分离 size/position 与完整 RECT 一次应用比较，观察文字锚点及右缘是否瞬态移动。
5. 前述都正常后，再测扩展背景色差。每轮只改变一个因素，避免再次产生“所有修复都通过 ffplay、真实感知仍无法归因”的循环。

## 7. 其他文档事项

- 23:39 证据应明确标为 R4 历史证据；R6 真机反馈支持改善，不等于 R6 所有候选机制已经逐帧证实。
- “150ms 内”“时点等于原生归位时点”改为“成功观测归位后尽快应用，端到端延迟待测”。
- 文档末尾建议全局结束所有 `msedgewebview2.exe`，范围过大，会影响其他使用 WebView2 的应用。以后若确需清理，应限定本应用所属进程，先核实是否真的存在参数复用问题。本轮未执行任何进程操作。

## 8. 最终意见

R6 在交互目标上是正确回调：不要在原生归位后人为再等。当前最值得怀疑的并非“仍然缺少一个更短的收缩动画”，而是：

1. 覆盖层层级控制是否参与原生矩形回摆；
2. 首次收缩后跟踪状态和加密轮询过早结束；
3. 尺寸/位置分开应用与观测到呈现的未测延迟；
4. 最后才是纯粹的背景色差及遮盖边界本身。

建议维持暂停，不继续堆加门槛或动画。如果重启此问题，先做原生基线对照和同步画面取证。
