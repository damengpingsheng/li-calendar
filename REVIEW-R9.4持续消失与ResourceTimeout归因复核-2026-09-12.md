# R9.4 新一轮复核：持续消失、ResourceTimeout 归因与下一步

日期：2026-09-12。依据：本轮最新《总结-退出全屏左缘收缩排障R1-R6.md》（内容已至 R9.4）、当前源码、`F:\Software\2026-09-12` 下六段录屏及微软官方资料。本轮只分析，未修改程序、构建或部署。

## 1. 结论

**最新问题确实已经升级为“自定义时钟持续不可见”，不能继续按左缘收缩或短暂色差处理。但文档把它定案为“显卡 TDR → WebView 表面丢失 → 应用侧无解”，证据不足，其中对 0x1CC 的解释有实质错误。**

我目前更倾向的工作假设是：全屏切换暴露了覆盖层的可见性、呈现或后台调用阻塞问题，而当前检测与恢复链无法区分这些故障，也不能可靠恢复，最终留下持续失败状态。显示栈问题可能是触发因素，尚未证明是唯一根因。

原生 GDI 时钟值得做，尤其适合这样小而简单、可靠性优先的部件。但正确承诺是“移除 Chromium 呈现依赖，缩短故障链”，不是“免疫显卡问题、保证全部消失”。推荐先做有判别力的小型对照，再决定整体迁移。

## 2. 录屏体现的是几类不同问题

本轮检查了后五段的每两秒概览帧，并结合此前对第一段的局部密集抽帧结果。概览用于比较状态，不用于测量百毫秒闪现。五张概览保存在项目 `review-evidence-20260912` 目录，按文件时间命名；拼图末尾黑色空格是补位，不是录屏黑屏。

| 录屏 | 时长约 | 可见证据及限制 |
|---|---:|---|
| 02-14-26 | 74.83 秒 | 此前 38.7/38.9 秒帧对比显示背景色块变化，文字位置相对稳定；属于色差证据 |
| 10-52-01 | 33.57 秒 | 自定义粗体时钟与原生细体时钟交替出现，部分时刻有浅色块；已不只是背景色差 |
| 13-44-53 | 31.30 秒 | 稀疏概览多数显示自定义时钟；不能据此排除文档报告的短暂透出，也不能独立确认其精确持续时间 |
| 14-11-32 | 32.17 秒 | 可见自定义文字偏左、右侧原生内容残留以及原生时钟露出，符合整窗平移的视觉代价 |
| 14-52-06 | 10.63 秒 | 可见任务栏的抽样帧主要显示原生时钟；持续失败问题已出现 |
| 16-38-48（最新） | 15.87 秒 | 所有两秒抽样帧都显示原生时钟，秒数从 16:38:50 走到 16:39:04，自定义时钟未见恢复 |

最新视频录制开始时已经处于失败状态，**没有拍到故障发生的瞬间**。它支持持续不可见，也显示桌面并未整体停止更新；它无法单独分辨窗口被压住、主动隐藏、状态机停滞或 WebView 内容未呈现。用户反馈的分钟级不恢复是额外证据，不能说这段约 16 秒的视频本身证明了几分钟。

这些文件平均帧率约 22～23 fps，且不是应当直接假定的固定帧率。精确测量短暂缺失应使用帧时间戳，不能统一用“帧数÷30”。

文档 §3.22 与录屏表对 16-38-48 的复验编号存在不一致。本轮按用户说明把它作为最新现状证据；判断某次修复是否有效，还应关联启动日志中的构建版本和会话 ID，不能只靠文件时间倒推。

## 3. 当前归因中最需要纠正的地方

### 3.1 0x1CC 不等于显卡 TDR

微软对 `EXRESOURCE_TIMEOUT_LIVEDUMP (0x1CC)` 的定义是内核 ERESOURCE 超时，可能涉及死锁或严重争用；它用于标识 live dump。这个代码本身不指明显卡驱动，也不证明发生了显示栈重置。[微软定义](https://learn.microsoft.com/en-us/windows-hardware/drivers/debugger/bug-check-0x1cc--exresource-timeout-livedump)

因此，“三天九次、签名相同”即使完全准确，也只能先作为重复资源超时的线索。要落到显示栈，需要查看 dump 中等待线程、资源持有者和调用栈，并对照实际故障时刻。只有看到相关栈和事件，才有依据继续判断具体驱动、系统显示组件或其他资源竞争。

本轮没有解析这些内核 dump，九次及其签名仍属于原文记录，不能升级为本轮独立证实。文档应撤回“终极根因”“WER 实锤显示驱动”“每次必然导致 WebView 表面丢失”等措辞。

### 3.2 窗口在位不等于透明表面已被证实

矩形正确、visible=true、具有 topmost 属性，不能共同保证窗口最终显示在任务栏之上。还需判断实际遮挡关系、当前状态机分支、相关窗口有效性以及呈现状态。单点命中测试也不是完整的合成结果证明。

旧版本 resize 与透明闪现可能有关，但“零 resize → 零透明帧”只是机制预测；后续持续消失说明它至少没有覆盖全部故障。不能再把每一类症状都归入同一条已定案因果链。

### 3.3 日志静默不等于已定位 GetPixel 无限阻塞

移除持几何锁的 GetPixel 探针是合理的风险收敛。不过，若没有卡住时的线程栈，日志断点最多把它列为重点嫌疑，不能排除 UIA、其他 Win32 调用、锁等待或日志自身阻塞。

R9.4 仍保留 appearance worker 的屏幕采样。因此“移除一个探针”不等于完成了阻塞隔离。换成 GDI 绘字后，如果仍复用卡住的几何线程、锁和采样路径，也可能继续不显示。

## 4. 当前源码里可以直接确认的恢复缺口

代码位置均为 `src-tauri/src/window_manager/windows/clock_overlay.rs`，行号基于本轮读取版本。

### 4.1 看门狗检测目标与故障目标不一致（2018～2064 行）

`LAST_DIAG` 在后端进入 `overlay_diag_dump()` 时刷新。它证明的是诊断函数被调用，不是 WebView 刚刚呈现成功。

- 后端活跃、WebView 持续不可见时，时间戳可以一直新鲜，看门狗不触发。
- 后端因为 UIA、锁或其他调用停住时，它又会被解释成显示驱动问题。
- 初值为 None 时不触发，覆盖不到诊断心跳从未建立的情形。
- 看门狗自己重写该时间戳，混淆“观察到恢复”与“尝试过恢复”。

应分开记录几何循环完成时间、采样完成时间、前端响应时间、故障事件及恢复尝试时间。前端响应也只能证明执行活性，不能单独证明屏幕呈现。

### 4.2 恢复成功没有闭环（2044～2053 行）

当前 hide/show/eval 的结果全部忽略，随后就写 `reload issued`。`location.reload()` 是向页面请求执行脚本，不能等同于已经完成 WebView 原生恢复，更不能解除持锁线程阻塞。

每轮重置时间戳后又要等超过 20 秒，实际重试大致为 25 秒加调用耗时，并非文档所说的失败后每 5 秒重试。hide/show 还会主动制造至少约 200ms 隐藏期。恢复应记录调用结果、完成确认、冷却和重试上限，并由真正的观测确认恢复。

### 4.3 窗口缓存先于成功提交（1049～1074 行）

代码先写入 `LAST_FOLLOW_POS`，再调用 `SetWindowPos`，且忽略其结果。若调用失败，缓存仍声称目标已应用；下一轮相同目标可能被变化检测直接跳过。z 自愈可能在部分分支清缓存，但不能替代所有路径的正确提交规则。

这是明确的可靠性缺口，**尚不能说它就是最新视频的原因**。应仅在调用成功并按需核验后提交缓存；失败时保留重试资格，并记录错误。`z self-heal` 日志同样不应在未确认 SetWindowPos 成功时表述为已经修复。

### 4.4 WebView 故障恢复尚未穷尽

本轮源码搜索没有发现应用显式处理 `ProcessFailed` / `BrowserProcessExited` 的路径。微软提供这些事件区分浏览器退出、渲染器退出/无响应及 GPU 进程退出；浏览器退出需要重建控件，主渲染器退出可使用宿主 Reload API 或重建。GPU 进程退出一般有自动恢复，持续失败仍需进一步诊断。[微软故障处理文档](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/process-related-events)

所以“reload 无效 → 所有 WebView 应用侧措施已耗尽”不成立。接入需要核对当前 Tauri/WebView2 原生接口和线程要求；这里不是声称某个 API 一定解决本机故障。

## 5. 下一步应做能区分原因的实验

不建议再改动画参数、采样死区或直接升级驱动后宣布解决。先固定当前版本、播放器配置和同一组全屏操作，保留基线。

| 优先级 | 动作 | 能回答什么 |
|---|---|---|
| P0 | 失败时保存应用线程栈；若再次出现 0x1CC，解析对应资源等待链，并关联会话时间 | 究竟卡在哪里；是否真是显示栈，还是应用锁/其他调用 |
| P0 | 记录窗口实际状态、状态机决策、原生调用结果，补充 WebView 故障事件；独立记录各工作链心跳 | 被压住/隐藏/调度停住/渲染故障分别走哪条链 |
| P1 | 修正缓存成功提交规则及看门狗观测/重试逻辑 | 消除已能从代码确认的恢复缺口 |
| P1 | 做固定位置、固定底色、只显示时间的最小 GDI 对照版本，关闭屏幕采样，保持相同全屏判定和 z 策略 | 改变呈现方式后，持续不可见是否仍复现 |
| P2 | GDI 稳定后逐项加回采样、渐变、附加文本，并保留每步基线 | 是 WebView 呈现依赖还是采样/共享控制链在触发问题 |

卡住调用的隔离不能仅靠“外面等超时”：超时不会终止底层阻塞调用，也不能无限增加替代线程。若确认某类调用可能长时间不返回，应避免持共享锁执行；必要时放入可重启辅助进程，最后可信数据供主显示链继续使用。

测试应覆盖快速连续切换和留出恢复时间的切换，并持续观察失败后的恢复情况。例如每组至少 30 次，在此前容易出问题的时长范围内重复；这是实用验收门槛，不是数学意义上的永不复发保证。记录自定义时钟缺失次数/最长时长、后台活性和恢复是否需要重启。

## 6. 有没有更接近根治的办法

**有：把时钟做成一个不依赖 WebView 才能保持基本显示的原生部件，同时消除显示链对采样、UIA 和可阻塞共享锁的依赖。**

推荐目标结构：后端提供时间/农历/天气数据；原生窗口绘制并维护最后可用画面；采样只异步提供可选外观更新，失败时继续显示；窗口控制单点管理，成功后提交状态；设置等复杂 UI 继续使用 WebView。与只在失败时临时切换 GDI 相比，原生时钟常驻主路径可以省去双套文字呈现和复杂故障切换。

但必须分清三层收益：

1. **WebView 专属故障**：原生绘制能移除这项依赖，是明确的结构性收益。
2. **窗口控制/阻塞故障**：需要同步修复或隔离；只替换 TextOut 不解决。
3. **系统显示故障与任务栏材质匹配**：GDI 仍处于 Windows 桌面显示环境，不能承诺显示驱动异常时必然可见；异步采样复刻任务栏也仍有误差和滞后。

左侧一小条原生背景看似正常，不能证明完整 GDI 时钟在同一故障下会持续刷新；应使用带递增秒数的完整对照件验证。若产品接受固定底色的小型时钟面板，还可删除像素采样链，进一步减少故障面；如果坚持与任务栏材质精确融合，则这部分复杂性仍然存在。

## 7. 建议如何修改现状总结

保留 R8.8 算法修复、R9.1 职责拆分、R9.4 移除持锁采样等实施记录；把“已解决”限定到对应证据支持的范围。最新总判断建议改成：

> 最新录屏确认自定义时钟持续不可见。显示相关资源争用、WebView 呈现故障、窗口控制或后台阻塞均仍待区分。0x1CC 提供内核资源超时线索，尚不能单独证明显卡 TDR。当前看门狗与原生调用结果处理存在恢复缺口。下一步先保留故障现场并完善分类诊断，同时用最小 GDI 时钟作对照；验证后决定是否迁移原生主呈现路径。

这比“环境级已定案、GDI 唯一路径且保证根治”更符合现有证据，也能让下一次测试真正缩小问题范围。
