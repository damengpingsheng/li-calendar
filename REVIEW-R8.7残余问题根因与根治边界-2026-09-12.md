# R8.7 残余问题：根因复核与根治边界

日期：2026-09-12；基线：`a0ac017`，实现 `e15440f`。

本轮重读更新到 R8.7 的《总结-退出全屏左缘收缩排障R1-R6.md》，并核对现有 Rust/React 实现。未重放两段视频或重新做真机实验；视频结论按原文已有报告对待。本轮不改应用、不构建、不部署，保留暂停状态。

## 1. 核心结论

**不能接受“剩余三项均非实现缺陷、已达架构极限、唯有拆原生背景才能根治”的定案。**

动态任务栏背景与外部覆盖层异步呈现，确实存在时空匹配困难。但当前实现还有可以直接从代码证明的颜色更新缺陷，以及收缩动画引入的并发/响应风险。它们尚未排除，不能把剩余色差都归于系统或物理下限。

当前最值得怀疑的是：

1. 双端渐变只用右侧反馈统一平移，无法追踪渐变斜率变化；
2. 内部反馈缺失时冻结更新，并错误地记录为收敛；
3. 显示桌面条已被证明可能有特殊状态，却再次作为右端反馈的权威目标；
4. 实际背景呈现落后于最后发出的命令时，闭环可能重复累加修正；
5. 窗口收缩会重映射渐变，动画又阻塞新几何响应；
6. 在这些实现问题之外，仍可能有采样调度及 WebView 呈现延迟。

## 2. 最明确的代码问题：渐变没有真正持续跟踪两个端点

位置：`clock_overlay.rs::run_appearance_sample`，约 1665–1745 行。

当前每轮读取：

```text
right = strip 或 primary
left = 左侧净列 或 right
```

但只有首次无 LAST_PUSHED_BG 时直接推送 `(left,right)`。后续是：

```text
corr = right - inside
cand_right = prev_right + corr
cand_left  = prev_left  + corr
```

**新的 left 不参与后续候选计算。** 未发生通道裁剪时：

```text
cand_left - cand_right = prev_left - prev_right
```

即渐变两端的差值被保留，后续只能整体变亮/变暗，不能跟随渐变斜率变化。

可直接构造反例（单个颜色通道）：

```text
旧推送：left=233, right=231
新真实参考：left=243, right=231
内部右侧读数：inside=231
```

此时 corr=0，日志 d=0，停止推送，但左端仍差 10。提高采样频率不会解决，换成 GDI 渲染而保留同一算法也不会解决。

**判断：这是确定的算法能力缺口，可能解释“偶发、细微、持续不恢复”，但尚未确认就是用户录像里每个异常片段的来源。**

建议：左右目标分别来自本轮可信观测；渲染校正应作为独立且有界的误差估计，不应使用旧两端统一平移替代更新目标。若认为渲染偏差近似为零，先用直接双端更新作为对照，通常比盲目增加控制环更易验证。

## 3. 内部反馈不可用时并没有退化为开环

同一函数中：

```text
inside=None → corr=(0,0,0) → candidate=previous → d=0 → return
```

这会在外部参考已经更新、内部窗口暂时被遮住或不能正确命中时保留旧色。文档称“该轮退化纯开环”，实际是冻结；日志还可能输出 `converged ... inside=none ... d=0`。

**缺少测量不等于测得零误差。** 这很可能与退出、快速再入全屏等关键时段重叠。

建议分清状态：

- 外部可信＋内部可信：可计算反馈；
- 外部可信＋内部缺失：允许使用当前外部目标前馈，或明确记录延后，不能称收敛；
- 外部不可信：保留最后可信值并记录失败；
- 收敛：只有可用的对应观测能支持此结论。

## 4. 右侧条带再次被当成权威目标

R8.4 的证据认为“显示桌面”条带可能发生与任务栏主体不同的悬停/材质状态，因而将其降为回退。但 R8.5 之后：

```text
right = s.strip.unwrap_or(s.primary)
corr = right - inside
```

条带存在时不仅决定右端，还通过 corr 推动左右两端一起改变。

如果条带变亮而主体没变，算法可能把整个覆盖背景提亮。随后内部追上条带、d=0，仍不能证明左侧主体匹配。这重新引入了 R8.4 试图规避的风险。

不能把 `left-strip` 的每次差异都解释为连续横向渐变：它也可能是两个不同材质区域的差异。应通过没有交互语义的主体区域、多参考点或受控悬停实验区分。两端线性插值只有在两端确实属于同一连续表面时才合理。

## 5. 闭环没有将显示读数与已呈现版本绑定

seq 能阻止旧消息覆盖新消息，但不能证明 GetPixel 读到的是 LAST_PUSHED_BG 对应版本的画面。

当前使用“最后发出的值＋现在屏幕误差”。例如目标为 110、已显示 100，先发 110；下一轮若屏幕仍显示 100，就可能以最后已发 110 再加 10，发出 120。之后再反向修正。是否实际发生取决于 worker 频率和渲染延迟，需记录验证。

建议先测量 command seq、前端应用 seq 和画面变化；对反馈设更新节奏和有界增益，避免在上一条命令尚未呈现时重复累计误差。简单场景优先直接发送可信目标色，不必假设一定需要积分式校正。

`d=0` 目前衡量的是这一控制公式的修正量，而不是整个遮盖区域与任务栏的最大像素误差。它不足以支撑文档“稳态全程无差、剩余只在切换瞬间”的绝对结论。

## 6. R8.7 动画不能用于证明过程必然可感知

### 6.1 当前实验只否定一个具体动画实现

代码是 4 次 SetWindowPos，步间 sleep 25ms；只有三次 sleep，即名义睡眠总时长 75ms，加上调用/调度耗时，而非固定 100ms。42px 分四步，每步约 10～11px，也不是逐帧连续插值。

一次“与上一版无明显变化”能证明这个版本没带来主观收益，不能推导“所有形式的边界变化必然可感知”或“动画参数再无意义”。不过用户目标明确，不值得继续盲扫动画参数。

### 6.2 新原生偏离可打破安全前提

动画安全论证是“中间矩形始终包含 endorsed，所以不露原生”。这只在动画全程原生都处于 endorsed 时成立。前文恰好证明曾存在回摆。

当前动画期间 update/relocate 会让位，循环没有每步检查最新原生矩形或退出轮次。快速重新进入全屏、新偏离或显示配置变化时，动画仍可继续收向旧终点，延迟必要扩展或 z 序处理。

### 6.3 串行锁保护存在间隙

动画需求由持 GEOM_APPLY_LOCK 的函数返回；之后在锁外才设置 SHRINK_ANIMATING。两者之间可有另一几何调用进入。动画本身在锁外执行，没有轮次取消机制，仍不能称所有几何竞态已经排除。

以上是静态代码风险，不是本轮已经复现的故障。考虑动画无主观收益，将来恢复时应先做关闭动画的对照基线，或使动画可被新的遮盖需求立即取消。

## 7. 收缩不只是边界移动：背景渐变也被重新映射

前端使用整个 `100vw` 宽度上的 `linear-gradient(90deg, bgLeft, bg)`。窗口从 200 缩到 158 时，同一屏幕 x 所处的渐变比例改变，即便两端色值不变，窗口内中间位置也会变色。

同时采样净列随窗口左缘移动，样本对应的屏幕位置也变了。仅传两个颜色、没有参考坐标/几何版本，可能用旧区域采出的端点描绘新区域。

建议把背景定义成屏幕坐标上的颜色函数或固定参考纹理：先有参考 x 与对应颜色，再在 mask 范围内裁切。收缩仅改变可见范围，不同时拉伸整段背景。只有参考确属连续表面时才使用插值。

这是一条现架构内的实质优化方向，不能被“42px 边界移动就是物理极限”排除。

## 8. 呈现层拆分能否彻底根治？

**不能仅凭拆成原生背景窗口就承诺根治。**

我之前建议的“固定文字窗＋独立背景”是有条件的性能/呈现原型。新版文档将其提升为“唯一根治路径”，超出了原建议及现有证据。

如果目标颜色每 150～500ms 才被发现，换成原生背景也必须等待目标颜色。它最多去掉或减少采样之后的 React/WebView 绘制延迟，不会自动消除前面的观测延迟、错误参考、反馈缺陷和空间重映射。

GDI/DWM 也不能理解为“可直接取得任务栏同一材质且一帧内同步”的承诺。自建背景可能与任务栏采样不同背景、材质参数不同或被独立调度，需要实测。

拆分的真实收益是：

- 文字几何固定，不随 mask resize 重新布局；
- 背景可单独绘制、换色与裁切；
- 更容易观测并控制两个阶段的呈现耗时。

代价是双窗口层级、输入和自动隐藏同步。它是后续选择，不能替代先修明显的色彩算法问题。

## 9. “彻底根治”需要区分范围

### 范围一：根治当前实现中的可复现缺陷

有明确路线：

1. 双端目标每轮独立更新；
2. 内部点缺失不得误判收敛；
3. 特殊条带不能无条件主导主体底色；
4. 反馈绑定测量/呈现状态，防止积累旧帧误差；
5. 固定屏幕坐标背景，收缩只裁切；
6. 动画取消与几何版本控制。

这些可以用小型合成输入测试与真实画面对照验证，属于实现修复，不需要先重写整个呈现层。

### 范围二：在本机常用场景达到用户不可察觉

存在希望，但需要实测。先修以上问题，再把总延迟分为事件发现、采样、排队、发送、呈现。150～500ms 是当前策略和实现结果，不是外部覆盖技术的普遍最短时间；约 70ms UIA 也尚未分解树查找与属性读取。

### 范围三：任意任务栏材质、任意切换中严格像素一致

外部窗口采样并重绘，不能普遍保证和 Shell 同帧、同材质、同像素。真正消除这一类模仿误差，通常需要改变需求或接管方式：

- 可靠抑制原生文字且保留需要的布局/背景，直接使用原生背景，只画自定义内容；
- 在同一 Shell 呈现体系内有受支持的集成方式；
- 改成自有固定背景/独立时钟区域，不再要求伪装成原生任务栏。

前两条本项目尚未证明在本机可行。UIA 元素不是可任意隐藏的独立 HWND，不应默认存在稳定受支持接口；不建议直接用全局日期格式副作用、跨进程 owner、AttachThreadInput 或侵入式 Explorer 修改拼成所谓根治。

## 10. 推荐下一步：先做三个能判定的实验

### 实验 A：用合成输入验证颜色算法

不操作桌面即可验证：

- 右端不变、左端变化，输出是否更新；
- 内部读数缺失、外部目标变化，是否继续保留旧色且误报收敛；
- 内部滞后一轮，是否反复过冲；
- 条带特殊高亮、主体不变，是否把主体染亮。

前两项当前代码即可推导失败。修完后再用真实采样验证，避免继续靠整体主观反馈诊断确定的算法问题。

### 实验 B：固定可信双端目标，关闭 servo 和动画做短时对照

保持几何/z 策略不变，以本轮可信双端样本直接更新、背景按屏幕坐标裁切。比较左边缘色差峰值/持续时间和文字位移。如果显著改善，则现有反馈或渐变重映射参与了问题。

### 实验 C：分解延迟后再决定是否拆背景窗口

统一 session ID、build、单调时间和 seq；记录参考点坐标、左右目标、内部颜色、几何版本、命令发送和前端应用时刻，配无损局部采集。

若正确目标已及时到达而 WebView 呈现迟，做原生背景原型；若目标本身晚，先优化触发/采样/UIA；若只有条带状态异常，先修参考区域。

20fps、低码率 H.264 视频足以支持明显残块和较大色差的定性判断，不能可靠界定 1～2 RGB 下限或几毫秒改善。细微色差应以同一采集链路的无损帧为准，不用日志 d=0 或压缩视频替代全区域误差测量。

## 11. 建议修订文档结论

将“均非实现缺陷、已经到架构极限”改成：

> 已修复多项几何与采样可靠性问题，残余仍涉及动态背景匹配和呈现时序。当前闭环双端更新、反馈缺失分支及动画控制还有待验证风险；原生背景拆分属于后续原型方向，尚不能保证根治。

保留用户接受暂停的决定。若未来继续，优先完成颜色算法的可判定修复，而非继续增加动画、固定等待或直接重写呈现层。

## 12. 用户补充录屏后的画面核对

用户随后提供 `F:/Software/2026-09-12/02-14-26.mp4`，本轮已实际解码并检查全程稀疏概览，以及 38–40 秒、48–50 秒的 10fps 局部连续帧，另保存两张原尺寸帧。

### 素材与版本边界

- ffprobe：832×274，时长约 74.833 秒，H.264；名义帧率 30fps、平均约 22.13fps，视频流码率约 369kbps。
- 与总结中的 836×245、20fps 等描述不完全相同。以后对齐应以本文件时间戳为准，不照搬其他素材参数。
- 按总结的版本关联，本段对应复验⑫/R8.6，发生在 R8.7 动画引入之前。因此可用于核对颜色/几何现象，不能独立验证 R8.7 动画成败。

### 直接观察

1. 约 38.7 秒，右侧自定义时钟区域有明显较浅的矩形背景，与左邻任务栏形成竖向接缝。
2. 约 38.9 秒，接缝明显减弱、背景趋于一致；对比这两帧，时间和农历文字没有与整块背景同幅度的左右移动。
3. 48–50 秒连续帧同样包含背景色块出现/消退；其中部分转换伴随音量、网络等原生图标的位置或可见状态变化，应与覆盖层自身位移分开分析。
4. 黑帧只表明这份局部录屏没有显示任务栏，不能单独拿它当作全屏状态/任务栏真实曝光的精确事件。

两张帧相隔 0.2 秒，并不表示异常准确持续 0.2 秒；这是抽帧对比，不是逐帧测得的起止边界。压缩视频可以确认整块色差，不能可靠证明 1～2 RGB 的误差下限。

### 对根因判断的影响

画面支持“至少一类明显感知主要来自背景色块，而非文字真的大幅移动”。因此优先排查颜色目标、条带参考和反馈更新仍然合理，不应继续只调收缩动画。

录像不能区分“正确目标色晚到”与“目标/反馈公式不正确”，也无法直接证明第 2～5 节代码缺陷在这两个时刻发生。需要将对应版本的左右参考、内部色、推送 seq 与画面匹配，才能完成具体归因。

证据文件（裁切与缩放只用于阅读，原尺寸对比帧另存）：

- [38.7 秒原尺寸帧](review-evidence-20260912/38.7s.png)
- [38.9 秒原尺寸帧](review-evidence-20260912/38.9s.png)
- [38–40 秒局部序列](review-evidence-20260912/38-40s.png)
- [48–50 秒局部序列](review-evidence-20260912/48-50s.png)

序列每行从左到右、逐行向下，约每 0.1 秒一格；精确源帧时间会受原视频时间戳和重采样影响。
