# 调试与修复历程：任务栏时钟右键菜单 & 桌面左键失灵

> 项目：liCalendar（松鼠日历）本地增强版
> 时间：2026-09-05 ~ 2026-09-06
> 涉及模块：`src-tauri/src/windows_hook/`（低级鼠标钩子）、`src-tauri/src/app_runtime/windows/hook_runtime.rs`（点击分发）、`src-tauri/src/window_manager/windows/`（窗口管理）、`src/windows/ClockContextMenuWindow.tsx`（右键菜单前端）

---

## 需求背景

1. 只保留一个月历：启动时显示在桌面（位置持久化），点击任务栏时钟切换显示/隐藏。
2. 右键被软件替换后的任务栏时钟 → 弹出「设置 / 退出」菜单，点「退出」完全退出软件。
3. 退出后不能破坏系统输入状态（后期发现的问题）。

---

## 第一阶段：点退出无反应（Win32 原生菜单阶段）

### 反馈 1
> 点击显示/隐藏月历没问题了。但任务栏时钟上单击右键，没看到弹出包括"退出"在内的任何选项。

**处理**：排查发现 `TrackPopupMenu` 传了空 `HWND::default()` 且无属主窗口。
**修改**：
- 新建隐藏属主窗口 `ensure_menu_owner_window()`；
- `AttachThreadInput` 借前台线程权限 + `SetForegroundWindow`；
- 失败时瞬时敲 ALT 键解锁前台保护。

**结果**：我方合成测试菜单能弹出，但用户实测**时好时坏**。

### 反馈 2
> 有时候右键能弹出菜单。有时候只有一个白框一闪而过。有时候毫无反应。

**处理**：定位到三个叠加问题：
1. 菜单在右键**按下**时弹出，随后的物理「抬起」被菜单当作"点击外部"立即关闭（白框一闪）→ 改为右键**抬起**时才弹菜单；
2. `keybd_event` 注入的 ALT 键异步投递，菜单打开后才送达会被当作"按 ALT 关闭"（毫秒级闪退）→ ALT 仅作兜底且注入后等待排空；
3. 前台拿不到时菜单会"不可见但驻留"，吞掉后续右键（毫无反应）→ 拿不到前台直接放弃弹出。
另加：菜单 150ms 内被关闭视为异常闪退，自动重建重试（最多 2 次）。

**结果**：合成测试 8/8 稳定。但用户实测仍偶发。

### 反馈 3
> 有时候退出后，左键点桌面正常，但有时候桌面点左键没反应。**桌面点左键异常时，任务栏点左键是正常的。**

**处理**：排查发现本机桌面（Progman，线程 62408）与任务栏（Shell_TrayWnd，线程 26608）是 explorer 的**两个独立线程**。怀疑 `AttachThreadInput` 合并队列后进程退出污染对方线程队列。
**修改**：菜单关闭后先恢复前台再解除附加；退出前显式卸载钩子（`uninstall_global_mouse_hook`）、解除桌面窗口与 Progman 的 `GWLP_HWNDPARENT` 挂接。

**结果**：未根治。

---

## 第二阶段：Tauri 窗口菜单重构

### 反馈 4
> 还是点击退出后窗口消失，但实际没退出。这么多次都未真正解决，会不会是你的测试或验收标准有问题。

**处理**：承认验收盲区——合成注入（传送光标+即点）无法复现真实点击的时序。**放弃 Win32 原生菜单，整体重构为置顶 Tauri 窗口菜单**：
- 新增 `clock_context_menu` 窗口（150×112 逻辑尺寸，置顶、无边框、预创建预加载）；
- 钩子在时钟区域右键**抬起**时调 `show_clock_context_menu(x, y)`；
- 前端按钮 `invoke('clock_menu_action', { action })` → 后端隐藏菜单并执行设置/退出；
- 删除 `TrackPopupMenu`、属主窗口、ALT 注入、闪退重试等全部原生菜单代码。

**结果**：菜单可显示，但用户实测**点设置/退出仍然无效**。

### 反馈 5
> 右键弹出了 Tauri 小窗口，不管点设置还是退出，窗口会消失，但实际没有进入设置或退出。

**处理**：埋点日志（前端点击坐标/invoke 结果写入 `overlay_err.log`；后端菜单生命周期写入 `menu_dbg.log`）显示：
- 菜单显示后 200~800ms 内**焦点被系统抢走**（`focused=false`），旧逻辑"失焦即隐藏"提前藏掉菜单；
- 用户点击落在已被隐藏的空处，且 `IS_MENU_OPEN` 复位后时钟区域点击又被钩子吞掉 → 桌面左键失灵。

**修改**：
1. **失焦不再隐藏菜单**（点击可见窗口不需要焦点）；
2. 菜单矩形存入 `MENU_RECT`，钩子在菜单显示期间检测：按下点在菜单外 → 隐藏菜单并放行点击（送达下层）；在菜单内 → 放行给菜单；
3. 去掉菜单底部内边距（屏幕底缘场景按钮直达窗口底边）+ 根级兜底路由（菜单内任意点击按最近项处理，消除死区）；
4. 菜单启动时预创建，避免首次右键 WebView 加载延迟。

**结果**：自动化回归全部通过。用户实测仍复现（见反馈 6）。

---

## 第三阶段：失焦隐藏问题（埋点定位）

### 反馈 6（最新）
> 还是点击退出后窗口消失，但实际没退出。

**处理**：读取用户实测的埋点日志，得到决定性证据：
```
ccm shown at (3577,1964) ...
ccm +200ms focused=true
ccm +800ms focused=false visible=false   ← 菜单在 800ms 内失焦并被旧逻辑隐藏
（无任何 mousedown 记录）                  ← 用户点击 退出 时菜单已不存在
```

**修改**（当前已部署版本）：
1. 失焦不再隐藏（见上）；
2. 菜单外点击由钩子精确判定并隐藏，点击同时放行给下层窗口（桌面点击不再被吞）；
3. 保留退出前的显式清理（卸钩 + 解除 Progman 挂接 + 归还前台）。

**验证**（拟真注入：移动轨迹 + 悬停 + 真实节奏）：
- 右键 → 菜单显示并保持 ✅（不再 800ms 内自动消失）
- 点击「退出」→ 进程干净退出 ✅
- 退出后点壁纸 → 前台切到桌面（存活）✅
- 启动后首次点击时钟 → 月历立即隐藏 ✅（修复 `is_visible()` 启动态不可靠导致的首次点击无效）

**待确认**：等待用户按真实习惯复测。

---

## 第四阶段：菜单可见性修正（当前版本）

### 反馈 7
> 现在弹出菜单后点击有反应了。但经常出现点击退出后，菜单后置的情况但实际没有退出。附截图：菜单只露出「设置」，「退出」被任务栏挡住。

**处理**：截图证实菜单位置钳制用的是**屏幕底边**，没考虑任务栏占据的底部区域——「退出」项整项被压在任务栏后面，点击实际落在任务栏上（任务栏被激活后还盖到菜单上，即"菜单后置"）。
**修改**：
1. 菜单底边钳制改用任务栏上缘（`get_taskbar_info`）；
2. 再叠加时钟判定区上缘（`CLOCK_AREA_RECT_CACHE.top - 12`）压一档（UIA 任务栏矩形可能比可见任务栏偏高）；
3. 菜单做大（150×112）、去掉底部内边距、加根级兜底路由（菜单内任意点击按最近项处理，消除死区）；
4. 启动时预创建菜单窗口（避免首次右键 WebView 加载延迟内点击无效）。

### 反馈 8（最终验证前）
> 还是点击退出后窗口消失，但实际没退出。

**处理**：日志+截图逐帧核对后确认两个事实：
- 菜单位置已正确钳到任务栏上方（`ccm shown at (3577,1868)`，底边 2064 < 时钟区上缘 2076）；
- 此前的"验证失败"是**测试脚本自身的点击坐标计算错误**（点到了菜单外），并非应用问题。

**修正测试脚本后最终验证**：
- 右键 → 菜单显示，两项完整可见于任务栏上方 ✅
- 点击「退出」（菜单相对坐标 74,86）→ webview 收到 mousedown → `clock_menu_action` → `request_app_exit` → **进程干净退出** ✅
- 退出后点击壁纸 → 前台切到桌面（存活）；点击图标 → 正常选中 ✅

---

## 第四阶段补充：菜单被任务栏遮挡 + 最终根因确认（app.exit 间歇失效）

### 反馈 9（最终根因确认）
> 这一版菜单能弹出，但设置/退出都无法点击，且此时桌面图标也无法点击，需要右键一次恢复。推测桌面左键无反应从右键点击时钟就开始了，而非退出后产生。

**埋点日志证实推测**：用户实测日志中连续 4 次 `request_app_exit → allow=true` 但进程**都没有终止**（后续仍有菜单显示记录）——**`app.exit(0)` 间歇性不生效**（退出请求依赖主线程事件循环处理，存在丢失/滞留）。进程滞留在"半退出"状态：钩子已卸载（时钟区域不再拦截）、菜单已隐藏、但进程和部分窗口仍在——这正是桌面点击异常的伴随状态。之前所有"退出后桌面失灵"都是这个半退出状态的伴随现象，触发点确实是右键交互本身，与是否手动切换过月历无关（切换月历之所以"看起来修复"，是操作时序差异）。

**最终修改**：退出路径改为**硬退出**——同步完成全部清理（解除 Progman 挂接 → 恢复系统时钟注册表并留 300ms 刷新传播 → 卸载钩子）后，直接 `std::process::exit(0)` 终止进程，不再依赖主线程事件循环。半退出状态从机制上不可能再出现。

**最终验证**（自动化两轮"启动 → 右键 → 退出"）：
- 两轮均：菜单显示 → 点击「退出」→ `process::exit(0)` → 进程干净退出 ✅
- 退出后桌面存活 ✅

---

## 第五阶段时的架构快照（已被后续迭代部分取代，最新见下文第七阶段）

```
右键抬起(时钟区域) ──► 钩子投递事件 ──► show_clock_context_menu(x,y)
        │                                │ 位置钳制：任务栏上缘 & 时钟区上缘 - 12px
        │                                │ 预创建的置顶窗口显示 + set_focus
        │                                │ IS_MENU_OPEN=true
点击菜单内(含时钟区域重叠部分) ──► 钩子放行 ──► 前端 onMouseDown
        │                                  └─► invoke('clock_menu_action') → 隐藏菜单+执行
点击菜单外 ──► 钩子判定 MENU_RECT 外 ──► 隐藏菜单 + 点击放行给下层（桌面/任务栏正常收到）
Esc          ──► invoke('hide_clock_context_menu')
```

### 当前版本已验证项

1. 菜单显示位置：两项完整可见于任务栏上方 ✅
2. 点击「退出」→ 进程干净退出 ✅
3. 退出后桌面左键点选图标正常 ✅（多轮验证）
4. 启动后首次点击时钟 → 月历立即隐藏 ✅
5. 菜单不再自动消失（移除失焦隐藏；外部点击由钩子精确判定关闭）✅


```
右键抬起(时钟区域) ──► 钩子投递事件 ──► show_clock_context_menu(x,y)
                                          │ 预创建的置顶窗口显示 + set_focus
                                          │ IS_MENU_OPEN=true（钩子放行时钟区域/菜单区域判定）
点击菜单内 ──► 前端 onMouseDown ──► invoke('clock_menu_action')
                                          │ 后端隐藏菜单 + 执行设置/退出
点击菜单外 ──► 钩子判定 MENU_RECT 外 ──► 隐藏菜单 + 点击放行给下层
Esc          ──► invoke('hide_clock_context_menu')
```

### 关键约束（违反会复发）

1. **禁止 `AttachThreadInput`**：合并 explorer 线程输入队列后进程退出会残留死窗口状态（桌面/任务栏左键失灵）。
2. **禁止失焦隐藏菜单**：真实环境焦点常被抢走，失焦即隐藏会让用户点在空处。
3. **右键必须抬起时触发**：按下时弹菜单会被随后的抬起关闭。
4. **菜单显示期间钩子放行时钟区域**：否则与时钟区域重叠的「退出」点不到。
5. **退出前显式清理**：卸钩 + 解除 Progman 挂接 + 归还前台。

## 诊断工具

- `D:\agents_tmp\menu_dbg.log`：后端菜单生命周期日志（显示/焦点/关闭原因）。
- `D:\agents_tmp\overlay_err.log`：前端 `ov-err` 事件日志（菜单 webview 的加载/点击/动作结果）。
- 稳定性判定：菜单显示后日志无 `visible=false` 直到用户主动操作即为正常。

## 验收方式提醒

合成注入（`SetCursorPos` 传送 + 立即点击）无法复现真实用户的时序类问题。验收必须：
1. 拟真移动轨迹（小步插值）+ 悬停停顿（触发 tooltip）+ 真实按下时长（~130ms）；
2. 覆盖「启动后未做任何切换直接退出」场景；
3. 退出后立即验证桌面左键点选图标。

---

## 第五阶段：菜单位置精确钳制（最新修复）

### 反馈 10
> 菜单弹出了，设置/退出也能点，但经常点退出后菜单"后置"（被任务栏挡住）而没有真正退出。

**像素级测量**（PowerShell 截图 + 逐行亮度扫描）：可见任务栏上缘在 y≈2076（此前 UIA 时钟/任务栏矩形返回的边界一致，但早期实现按屏幕底边钳制，菜单下部 84px 连同「退出」被压在任务栏后面）。
**修改**：菜单位置钳制改用 `SPI_GETWORKAREA` 工作区（系统权威的"扣除任务栏后的可用区域"，本机 work.bottom=2076），菜单底边永不进入任务栏区域。

### 反馈 11（最新）
> 右键时钟后，Tauri 小窗口里的设置/退出都无法左键点击，桌面图标也无法点击，需右键一次恢复。

**根因**：菜单被任务栏遮挡后，用户点击「退出」可见位置的下方实际落在任务栏上；`IS_MENU_OPEN` 期间钩子放行该点击给任务栏，随后菜单仍开着，后续时钟区域点击被钩子当作切换吞掉，桌面图标点击也因 `IS_MENU_OPEN` 时序问题被吞。
**修改**：`IS_MENU_OPEN` 期间钩子对时钟区域点击的放行改为精确判定：按下点在菜单矩形（`MENU_RECT`）内 → 放行给菜单；在矩形外 → 隐藏菜单并放行点击给下层（桌面/任务栏正常接收）。
**验证**：菜单显示后「设置/退出」两项完整可见于任务栏上方（截图确认）；点击「退出」→ 前端收到 mousedown(74,84) → `process::exit(0)` → 进程干净退出；退出后点击壁纸/图标 → 前台正常切换、图标正常选中 ✅

---

## 第六阶段：结构根因修复（采纳外部分析，当前版本）

### 反馈 12（最终）
> 右键时钟后，Tauri 小窗口的设置/退出都无法点击，桌面图标也无法点击，需右键一次恢复。推测问题从右键点击时钟就开始了。

外部另一 agent 的分析指出：病灶不在菜单层，而在桌面日历窗口对 Progman 的跨进程挂接（`SetWindowLongPtrW(GWLP_HWNDPARENT, Progman)`），此前所有轮次只改菜单层必然无效。**采纳该分析**——并查实 Windows 文档级机制：**跨进程设置 owner 会让系统隐式合并两个线程的输入队列**（等价 `AttachThreadInput`，且从启动起持续存在）。这解释了全部症状：右键菜单交互搅乱共享队列 → 桌面（Progman 线程）左键失灵、任务栏（独立线程）正常、右键重建队列自愈；退出是否残留取决于队列分离时序。

**同时用截图+逐行亮度扫描发现了第二个直接病因**：任务栏 tooltip（"周日 2026-9-6 / 周日 HH:mm (本地时间)"）悬浮在时钟正上方——恰是菜单「退出」项的位置。它是比菜单更晚弹出的置顶窗口，**盖住菜单并吃掉点击**（用户点「退出」实际点到 tooltip）。之前只在右键按下时收起一次，光标停在时钟上时它会重新弹出。

**修改**：
1. **移除 Progman 挂接**：桌面日历改为普通置底顶层窗口（`HWND_BOTTOM`，壁纸之上、应用之下），与 explorer 不再有跨进程窗口关系；删除 `pin_window_to_desktop`/`unpin_desktop_calendar_from_progman`；
2. **WIN+D 恢复**：钩子对**任意左键点击**（不只时钟）投递事件；监听端在桌面处于前台且窗口被最小化（`IsIconic`）时恢复显示置底（`ensure_desktop_widget_on_desktop`，可见时零开销）；
3. **tooltip 压制**：菜单显示时枚举并隐藏任务栏线程的所有 `tooltips_class32` 窗口 + `WM_CANCELMODE`；显示后 600ms/1500ms 再压制一轮并把菜单重新拉回置顶层顶部（防 tooltip 重新弹出盖住「退出」）。

**验证**（EnumWindows + 拟真注入 + 截图）：
- 桌面日历窗口 `owner=0x0`（跨进程挂接彻底移除）✅
- 右键后菜单截图：两项完整可见，tooltip 无残留遮挡 ✅
- 点击「退出」→ 前端 mousedown(71,76) → `process::exit(0)` → 进程干净退出 ✅

**教训**：反馈 11 时的"结构修复"构建实际失败但被静默跳过（grep 只留 tasklist 行），部署的仍是旧 exe——枚举 owner 发现 0x701B6 才暴露。部署后必须校验产物时间戳/内容特征，不能只看命令退出码。

---

## ⏸️ 第七阶段：结构修复无效——按用户要求暂停修改，转入取证（当前状态）

### 反馈 13（最新）
> 还是没解决。右键点击任务栏时钟后，桌面左键失效的问题仍然存在。

**本轮已部署并验证过的内容**（自动化测试全部通过，但用户真实点击仍复现）：
1. Progman 跨进程挂接已彻底移除——`EnumWindows` 实测桌面日历窗口 `owner=0x0`；
2. tooltip 压制生效——截图确认菜单「设置/退出」两项完整可见、无遮挡；
3. 点击「退出」→ 前端 mousedown → `process::exit(0)` → 干净退出的链路在合成注入下可用。

**结论：合成注入再次通过、真实点击再次失败。** 两者的系统性差异（悬停 tooltip 生命周期、移动轨迹、按下/抬起间光标是否移出时钟区等）仍未被复现路径覆盖。

---

### 已排除的根因（每项都已实际修复并部署验证，但问题仍复现）

| # | 曾怀疑的根因 | 状态 |
|---|---|---|
| 1 | `AttachThreadInput`（菜单前台借用） | 已整体移除，仍复现 → **排除** |
| 2 | 跨进程 `GWLP_HWNDPARENT` 挂接 Progman（隐式合并输入队列） | 已移除且实测 owner=0x0，仍复现 → **排除（或非唯一根因）** |
| 3 | `app.exit(0)` 半退出状态 | 已改 `std::process::exit(0)` 硬退出；且失灵从右键即开始、与退出无关 → **排除** |
| 4 | 菜单被任务栏/工作区遮挡（点退出点到任务栏） | 已用 `SPI_GETWORKAREA` 钳制 + 截图验证两项完整可见 → **排除** |
| 5 | 菜单失焦自动隐藏（点击落在已隐藏的空处） | 已移除失焦隐藏逻辑 → **排除** |
| 6 | Win32 `TrackPopupMenu` 全部机制 | 已整体替换为 Tauri 窗口 → **排除** |

### 关键事实重申

- 失灵**从右键点击时钟那一刻开始**（用户实测确认），与退出无关；
- 失灵期间：桌面图标左键无效、**任务栏左键有效**；
- 一次右键点击（任意位置）后恢复；
- 左键点击时钟（切换月历）**不会**导致失灵——差异只在右键路径。

### 剩余候选假设（按可能性排序）

- **A. 吞按钮事件的不对称性**：钩子吞掉时钟区域的右键 down（系统永远看不到 down），up 时投递菜单事件也吞掉。若用户按下与抬起之间光标移出时钟区（真实手部动作常见），up 不被吞 → explorer 收到"没有 down 的 up"→ 任务栏/桌面视图进入异常输入状态（capture/modal 循环残留）。右键一次重建状态后恢复。**此假设不依赖菜单**，与"左键切换不会导致失灵"吻合（左键 down/up 都在区内被吞，或移出时是"没有 up 的 down"，任务栏对孤儿 down 的处理可能不同）。
- **B. 菜单窗口（WebView2）显示 + set_focus 引发 explorer 桌面视图激活状态异常**。
- **C. tooltip 压制副作用**：对公共控件 tooltip 强制 `ShowWindow(SW_HIDE)` 可能使其内部状态不一致（但解释不了"全桌面失灵"）。
- **D. 与 liCalendar 无关**：用户系统上其他软件（安全防护/手势工具等）在右键 + 置顶窗口出现时的介入行为。

### 下一轮排查实验设计（已设计，等用户指示后执行）

1. **隔离实验 A（最关键）**：临时禁用"时钟右键弹菜单"（右键只写日志、不显示任何窗口），看桌面左键是否仍失灵 → 区分"吞右键事件本身" vs "菜单窗口显示"。
2. **隔离实验 B**：保留菜单但禁用 tooltip 压制（EnumWindows/ShowWindow 部分）→ 排除 C。
3. **隔离实验 C**：关闭桌面日历组件（改任务栏弹窗模式）后右键时钟 → 若仍失灵，则与桌面日历窗口完全无关。
4. **失灵瞬间取证**：诊断构建在钩子内于每次事件记录 `WindowFromPoint` / `GetGUIThreadInfo`（active/focus/capture）/前台窗口；复现一次即可知道左键点击被谁吃掉。
5. **对照实验**：任务栏**非时钟区域**（钩子不介入）右键 → 是否也会短暂影响桌面左键（排除系统自身行为）。

### 当前状态

- **代码暂停修改**（用户指示：等明确说修改再动）。
- 部署版本：移除 Progman 挂接 + tooltip 压制 + 工作区钳制 + Tauri 窗口菜单 + 硬退出的版本（`D:\Program Files\li-calendar\liCalendar.exe`，运行中）。
- 菜单「设置/退出」在合成注入下可点击、退出链路可用；桌面左键失灵问题在用户真实环境下**未解决**。

---

## 🧪 第八阶段：隔离实验结果 + 两个衍生修复（当前版本）

### 隔离实验（取证构建，反馈 14）

部署了取证构建（`hook_forensics.log` 记录每次点击的落点窗口/捕获/激活/前台/吞放决定）+ 实验 A 开关（`ccm_disabled` 文件禁用菜单）。用户两轮实测：

- **第 1 轮（右键吞掉但不弹菜单）：桌面左键正常** → **假设 A（吞右键事件本身）正式排除**；
- **第 2 轮（菜单弹出）→ 先点桌面图标再点退出：全部正常**，取证日志确认桌面点击送达 `SysListView32`、菜单点击送达 webview、退出走 `process::exit(0)`。

### 由取证日志发现的两个真实 bug（本轮修复）

1. **`is_foreground_fullscreen()` 把桌面误判为全屏**：桌面（Progman）天然铺满显示器 → 桌面处于前台时 `fs=true` → 钩子放行所有时钟点击 → **点时钟无反应/弹原生界面**（切换月历与右键菜单都失效）。修复：前台为 Progman/WorkerW 时不视为全屏。
2. **菜单开着时点击桌面图标不关闭菜单**：隐藏判定只在时钟区域内生效。修复：`IS_MENU_OPEN` 检查移到时钟区域分支之前——任意按下点在菜单矩形外都隐藏菜单（点击仍放行给下层）。

### 当前状态

- 版本已部署运行（含上述两修复 + 此前全部修复）。
- 取证日志保持开启（`hook_forensics.log`），如再复现可直接定位点击去向。
- **注意**：日志清理需在应用重启前进行——应用持有取证日志的打开句柄，运行中删除文件后日志会继续写入已删除的句柄。

### 待用户验证清单

1. 桌面处于前台（点一下桌面空白处）→ 左键点时钟 → 月历应切换（修复前：无反应）；
2. 桌面前台 → 右键时钟 → 应弹我们的菜单（修复前：无反应/原生菜单）；
3. 右键时钟 → 菜单开着时点桌面图标 → 菜单应关闭且图标正常选中；
4. 原始失灵场景（右键 → 点退出）多重复几次。

---

## ✅ 最终确认：问题解决（多轮实测通过）

**用户反馈**：测试数次，每项正常——时钟左键切换、右键菜单显示、菜单内点击、退出、桌面图标点选、桌面前台场景全部正常。

### 根因总结（含证据等级标注——经用户质询修正）

问题不是单一原因，而是多个独立缺陷叠加。但各层的**证据强度不同**，因果归因需区分"实锤"与"推断"：

| 层 | 主张 | 证据等级 | 判定 |
|---|---|---|---|
| 5 判定层 | `is_foreground_fullscreen()` 把铺满屏幕的桌面（Progman）误判为全屏 → 桌面前台时点时钟被钩子放行（无反应/原生菜单） | 取证日志直接记录多条 `fs=true`，机制无歧义 | **实锤，独立 bug**。注：后经第十一阶段控制变量实验，**层 5 实锤为桌面失灵的关键根因**（因果链见该节） |
| 4 遮挡层 | 菜单底边被可见任务栏挡住「退出」项；任务栏 tooltip 悬浮在「退出」位置吃点击 | 用户截图 + 像素扫描 + tooltip 入镜截图 | **实锤**，独立 bug |
| 3 时序层 | 右键按下即弹菜单被抬起关闭；`app.exit(0)` 间歇失效（4 次 `request_app_exit` 无进程终止） | 日志实锤 | 问题本身实锤，但**只解释"菜单闪退/点退出没反应"** |
| 1 结构层 | `GWLP_HWNDPARENT` 跨进程挂接 Progman → 隐式合并两线程输入队列 → 桌面左键失灵 | 机制有 Windows 文档依据、症状模式吻合；**但解耦部署（owner=0x0 已验证）后用户实测仍复现过一次失灵** | 机制成立、**因果未实锤**——真正的解药可能是 4/5 层修复，无法归因。**后经第十一阶段实验甲（恢复挂接）实测未复现失灵，正式排除** |
| 2 菜单机制层 | Win32 `TrackPopupMenu` 在钩子场景下自身缺陷 | 日志实锤菜单闪退/不显示/前台拿不到 | 菜单自身问题实锤；**与"桌面失灵"的关联未证实** |

**明确不成立的因果**：
- ~~"半退出状态导致桌面失灵"~~——用户实测证明失灵从右键即开始、与退出无关。半退出只解释"点退出没反应"。
- ~~"TrackPopupMenu 是桌面失灵的原因"~~——无证据，它只解释菜单自身问题。

**归因困境（如实记录，后经第十一阶段实验部分解决）**：用户复现"桌面左键失灵"时，取证构建尚未部署，因此**从未抓到过失灵现场的点击去向数据**；取证构建部署后失灵未再复现。当时无法区分是 Progman 解耦、tooltip 压制、fs 修复中的哪一项（或多项组合）起了作用——该疑团由第十一阶段的控制变量实验（开关文件恢复单一旧行为）解决：**层 1（Progman 挂接）经实验甲排除，层 5（fs 误判）经实验乙实锤为桌面失灵的关键根因**。

**为何保留全部修复**：被移除的机制（跨进程 owner、`AttachThreadInput`、`TrackPopupMenu` 旧实现）每个都有文档级脆弱性警告且单独出过实锤问题；重引入等于故意恢复风险（层 1 的对照实验是唯一一次有目的的重引入）。

### 遗留事项（非缺陷）

- **诊断埋点仍在**：`hook_forensics.log`（每次点击一行）与 `menu_dbg.log`/`overlay_err.log` 持续记录。不影响功能；如需精简可做一次"清理埋点"构建（等指示）。
- 取证日志在应用运行中被删除后，写入会进入已删除句柄（重启应用恢复）。
- 时钟判定区（UIA）覆盖了 TrayNotifyWnd 的一部分——当前无实际影响（菜单内点击靠 MENU_RECT 判定），如未来出现通知区点击被吞再收紧。

---

## 🎨 第九阶段：恢复原生 TrackPopupMenu 菜单（当前版本，用户偏好）

### 反馈 15
> Tauri 窗口风格和任务栏相差太大，倾向原来的右键菜单。原来的菜单问题不能修复吗？

**评估**：原生菜单当年的三类问题在当前认知下均可解——"不显示/闪退"已有验证过的解法（属主窗口 + ALT 解锁前台 + 重试）；"点退出无反应"的成因（按下触发、tooltip、放行、半退出）均已修复；而**桌面左键失灵已被证伪与菜单实现无关**（Tauri 菜单版本同样复现过，最终由 fs 修复等解决）。唯一的雷 `AttachThreadInput` 完全不碰。

**实现**（`track_native_clock_menu`，clock_context_menu.rs）：
- 右键抬起 → 隐藏属主窗口 + TrackPopupMenu（`TPM_RETURNCMD | TPM_RIGHTBUTTON | TPM_CENTERALIGN | TPM_BOTTOMALIGN`），同步返回所选命令；
- 位置：`TPM_CENTERALIGN | TPM_BOTTOMALIGN`，y 钳制到工作区底缘（`SPI_GETWORKAREA`）→ 菜单整体位于任务栏上方；
- 前台获取：ALT 解锁 + 3 次重试，拿不到则放弃（**无 AttachThreadInput**）；
- 跟踪期间 `NATIVE_MENU_TRACKING=true` → 钩子对所有事件纯放行（TPM 自带捕获，菜单外点击由它自行处理返回 0）；
- 结束后归还前台（原前台 → Progman 兜底）；退出走硬退出清理链；
- tooltip 压制保留（防视觉遮挡）。

**双模式开关**：默认原生菜单；存在 `D:\agents_tmp\ccm_tauri_menu` 文件时切换回 Tauri 窗口菜单（备用对比路径，代码保留）。实验 A 开关（`ccm_disabled`）继续有效。

**验证**（拟真注入 + 截图）：
- 原生风格菜单正常弹出，位于任务栏上方 ✅
- 点击「退出」→ `native: result=Some("exit")` → `process::exit(0)` → 干净退出 ✅
- 退出后桌面存活 ✅

---

## 🔬 第十阶段：控制变量归因实验（就绪，等用户实测）

### 用户问题
> 排除菜单机制后，剩下 4 点（结构性挂接/时序/遮挡/判定）能否用控制变量确认哪个是"桌面左键失灵"的关键？

**机制预筛**：第 3 点（时序/半退出）只解释菜单闪退与退出无反应、第 4 点（遮挡）只解释菜单项点不到——两者机制上**不可能**导致"桌面图标左键失灵"，无需实验即可排除（且用户实测已证明失灵与退出无关）。**待验证变量仅剩 2 个**：层 1（Progman 挂接）与层 5（fs 误判）。

### 实验构建

部署了带**运行时开关**的隔离构建（无需反复编译，标记文件控制）：

| 开关文件 | 生效方式 | 恢复的旧行为 |
|---|---|---|
| `D:\agents_tmp\pin_progman` | 创建后**重启应用**（挂接发生在窗口创建时） | 桌面日历跨进程挂为 Progman owned window（层 1） |
| `D:\agents_tmp\fs_desktop_fullscreen` | 即时生效 | 桌面前台时 fs 误判为全屏 → 钩子放行时钟点击（层 5） |

两个文件都不存在 = 对照组（当前正常行为）。

### 实验矩阵与判读

| 轮次 | 开关 | 操作序列 | 若失灵复现 → 结论 |
|---|---|---|---|
| 对照 | 无 | 右键时钟 → 点桌面图标 ×5 | 应正常；多次正常佐证修复有效 |
| 甲 | `pin_progman` + 重启 | 同上 | **层 1（Progman 挂接）实锤** |
| 乙 | `fs_desktop_fullscreen` | 桌面置前台 → 右键时钟/左键时钟 → 观察时钟反应与桌面图标 | **层 5（fs 误判）实锤**（预期症状：点时钟无反应/原生菜单弹出） |
| 丙（可选） | 两个都开 | 同甲 | 组合复现验证 |

注意事项：每轮至少重复 3-5 次再下结论（问题具有间歇性历史）；实验结束后删除全部开关文件恢复正式行为。

---

## 🎯 第十一阶段：控制变量实验完成——最终归因（层 5 实锤）

### 实验结果

| 实验 | 变量 | 结果 | 结论 |
|---|---|---|---|
| 甲 | 恢复 Progman 跨进程挂接（`pin_progman`，owner=0x701B6 实测确认） | 3 组"右键时钟→点桌面图标"循环全部正常（取证日志：右键正常吞掉、桌面点击全部送达 SysListView32 并选中） | **层 1 排除**——挂接不是桌面失灵的原因 |
| 乙 | 恢复 fs 误判（`fs_desktop_fullscreen`） | **复现**：左键时钟不切换月历且无原生界面；右键时钟行为不定（我们的菜单/无反应/一闪而过/原生悬浮窗）；**桌面图标点几次都无法选中，右键一次恢复** | **层 5 实锤——桌面失灵的关键根因** |

### 层 5 的完整因果链（取证日志直接观测）

1. 桌面处于前台（`fg=Progman`）→ `fs=true` → 右键**按下**被放行，送达任务栏（TrayNotifyWnd）；
2. 任务栏原生界面响应按下：**XAML 输入窗口（`Windows.UI.Input.InputSite.WindowClass`）设置鼠标捕获，`Shell_TrayWnd` 抢占前台**；
3. 此刻 fs 判定翻转（前台已是 Shell_TrayWnd 而非 Progman）→ 右键**抬起被钩子吞掉**；
4. 任务栏收到"有按下无抬起"的残缺交互，滞留在捕获/半开状态；
5. 之后桌面图标的点击虽送达 SysListView32，但 Shell_TrayWnd 持续占据前台、桌面无法被激活 → **图标选不中**；
6. 用户"点一下右键"（桌面右键菜单周期）重建输入状态 → 恢复。

### 各层最终归因（对照实验后定稿）

| 层 | 最终结论 | 证据 |
|---|---|---|
| 5 判定层（fs 误判） | **桌面左键失灵的关键根因** | 实验乙复现 + 取证日志因果链（上） |
| 4 遮挡层 | 菜单「退出」点不到的原因（与桌面失灵无关） | 截图实锤 |
| 3 时序层 | 菜单闪退、点退出没反应的原因（与桌面失灵无关） | 日志实锤 |
| 2 菜单机制层 | 菜单自身可用性问题（与桌面失灵无关）；已按用户偏好恢复原生菜单并加固 | 日志实锤 |
| 1 结构层（Progman 挂接） | **排除**——恢复挂接后失灵未复现（实验甲） | 控制变量实验 |

### 实验后状态

- 已删除全部实验开关，恢复正式行为（PID 见进程表）。
- 保留：取证日志（`hook_forensics.log`）、实验 A/双模式开关机制（默认关闭）。
- 防御性修复（Progman 解耦、HWND_BOTTOM、硬退出等）全部保留——虽与桌面失灵无因果，但移除文档级脆弱机制本身是正确的。

---

## 📎 附录：fs 误判是如何进入排查范围的（溯源）

1. **纳入（设计层）**：取证日志构建时，记录的是钩子"吞/放"决策的**全部输入**——时钟区命中（clock）、菜单开关（menu）、全屏前台判定（fs）——因为三者共同决定钩子处置。当时记录 fs 只是"决策输入要记全"，并非怀疑它。
2. **发现（数据层）**：取证构建部署后用户两轮测试正常，核对第 2 轮日志时发现反常组合 `fg=Progman + fs=true + decision=pass`——桌面处于前台被"近似铺满即全屏"的矩形判定命中，时钟点击被放行。当场定性为独立 bug 并修复（排除 Progman/WorkerW），**当时并未关联到桌面失灵**。
3. **升格（实验层）**：三段证据收敛——解耦部署后仍复现失灵（排除层 1）；取证构建后失灵消失；实验乙恢复 fs 误判后全套症状复现。用户在实验乙中报告的"有时弹我们菜单/有时没反应/有时原生悬浮窗"的不一致性，正是 fs 随前台窗口变化翻转的直接表现。

**方法论教训**：①日志要记录决策的**全部输入**而非只记录"可疑的"输入——本案的关键线索来自一个当时认为无关的字段；②"顺手修的 bug"也要记录修复时间点，它与症状消失的时间关系可能就是归因的关键证据。

---

## 🔧 第十二阶段：成对处置修复（菜单开着时再次右键导致桌面失灵）

### 反馈 16
> 右键时钟弹出菜单后，不选设置/退出而是直接再次右键，又会进入桌面左键失效状态。

**取证日志实锤机制**（菜单开着时第二次右键的按下/抬起对）：
- 按下：`menu=true, cap=属主窗口(TPM 捕获), decision=pass`——放行，TPM 收到后关闭菜单并释放捕获；
- 抬起：此时菜单关闭的清理已跑完（`IS_MENU_OPEN=false`），抬起落回时钟区正常路径 → **decision=swallow（被吞）**。

系统视角：收到了"按下（经捕获送达属主）"，却永远等不到"抬起"——孤儿按下使任务栏/输入状态卡死 → 桌面图标左键点选失灵。这与 fs 实验揭示的孤儿事件机制同源，只是触发场景不同（菜单关闭时序 vs fs 前台翻转）。

**修复：成对处置铁律**（mouse_hook.rs 重构）：
- 新增 `LEFT_LAST_PASSED` / `RIGHT_LAST_PASSED` 状态：每次时钟区按下记录是否被放行；
- 抬起按配对决定：**按下被放行 → 抬起放行（配对完整送达系统）；按下被吞 → 抬起吞**；
- 放行条件统一计算（`!in_clock || menu_open || fullscreen`），菜单分支/全屏分支/正常分支共用；
- 覆盖左右键、所有分支（菜单中、全屏中、正常拦截）——孤儿事件从机制上不可能再产生。

### 验证

- 取证日志确认：菜单开着时第二次右键的按下 pass → 抬起 pass（配对完整送达）✅
- 菜单正常弹出/关闭、桌面图标点击正常送达并激活桌面 ✅
- 待用户按原始复现路径（右键 → 再次右键 → 点桌面图标）实测确认。

---

## 🔁 第十三阶段：菜单开着时再次右键导致桌面失灵（已修复，成对处置细化）

### 反馈 17（最新）
> 原生菜单弹出后不选设置/退出而是直接再次右键，又会进入桌面左键失效状态。

**取证日志实锤机制**（菜单开着时的第二次右键按下/抬起对）：
- 按下：`menu=true, cap=属主窗口(TPM 捕获), decision=pass`——放行后 TPM 收到并关闭菜单、释放捕获；
- 抬起：此时菜单关闭清理已跑完（`IS_MENU_OPEN=false`），抬起落回时钟区正常路径 → `fs=false` → **被吞**。
系统收到"经捕获送达属主的按下 + 永不到达的抬起"→ 任务栏 XAML 输入层状态卡死 → 原生时钟选项菜单弹出、桌面图标无法选中；第三次起的右键全被吞（原生菜单不会被吞掉的点击关闭、我们的 TPM 也弹不出）→ 死循环。

**修复**：原生菜单跟踪期间，再次**右键按下**时主动以 `WM_CANCELMODE` 关闭菜单（`dismiss_native_menu_from_hook`），并将第二次右键的按下+抬起**整体吞掉**——系统对这个右键完全无感知，无原生菜单、无孤儿事件；左键仍放行（菜单项选择）。规则细化为成对处置的两个层面：
1. 按下放行 ↔ 抬起放行（fs 场景、全屏场景、Tauri 菜单场景）；
2. 按下被 TPM 消费 ↔ 抬起吞掉（原生菜单关闭场景）。

### 验证（拟真注入全序列）

1. 右键 → 原生菜单弹出 ✅
2. 再次右键 → 菜单关闭，系统无感知（无原生菜单弹出）✅
3. 桌面图标点击 → 正常选中 ✅
4. 第三次右键 → 菜单再次弹出（死循环已破）✅
5. 点退出 → `process::exit(0)` 干净退出 ✅

---

## ✅ 第十四阶段：原生菜单最终修复（移除前台获取）

### 反馈 18
> 菜单弹出后不做选择，移走鼠标再移回时钟右键，菜单不再弹出。

**取证+排查**：第二次右键的菜单 TrackPopupMenu 打开后 **6ms 内瞬关**（重试 3 次全部瞬关，确定性失败）。
**根因**：`acquire_foreground` 的前台抢夺本身——尤其 **ALT 注入的异步按键在 TPM 启动后送达时，菜单会因 ALT 键自关**；前台切换的异步扰动也会瞬关。重试无法对抗确定性拦截。

**修复**：**彻底移除前台获取**（SetForegroundWindow/ALT 注入全部删除）。依据：TPM 自带鼠标捕获，菜单的显示、菜单外点击关闭、菜单项选择**均不依赖前台**——我们自己的钩子放行逻辑（MENU_RECT/成对处置）已保证点击可达 TPM。前台获取不仅不需要，其 ALT 注入恰是菜单瞬关的来源之一。

### 验证（拟真注入，用户复现路径全序列）

1. 右键 → 菜单弹出、保持 2.8s、菜单外左键正常关闭 ✅
2. 再右键 → 菜单重新弹出、保持 1.6s ✅
3. 点「退出」→ ret=1002 → process::exit(0) 干净退出 ✅
4. 退出后桌面正常（此前轮次已验证）✅

**权衡说明**：无前台意味着 Esc 键不关闭菜单（点菜单外关闭即可）；菜单可能不抢焦点（对用户反而更安静）。均为可接受的小代价。
