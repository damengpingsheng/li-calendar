# 退出全屏时钟：残余色差、根本优化与下一步方向

日期：2026-09-10  
基线：`ea4592f`，实现 `cf6a2f8`（R7.6）  
阅读对象：`总结-退出全屏左缘收缩排障R1-R6.md`（内容实际已更新至 R7.6）。

本轮核对了总结、采色与几何代码、前端外观事件处理，以及 UI Automation 官方资料；未重新运行帧取证。总结中的真机截图结论作为既有实验记录引用，不冒充本轮独立复验。仅输出建议，不恢复已暂停的实现、部署或自动化任务。

## 1. 结论与建议优先级

**存在进一步优化空间，但目前没有证据支持某个小补丁能保证所有场景彻底零色差。**

R7 系列已经取得实质进展：修复跟踪中断、非对称扩展、尺寸/位置中间态、启动未就绪闪现和遮盖重建循环，并通过动态采色显著改善色差。当前应集中处理“背景颜色如何可靠、及时地到达屏幕”，不应再把所有现象当作收缩慢。

推荐次序：

1. **先修采色数据链**：稳定参考区域、单工作线程合并请求、结果版本控制、记录失败原因和前端应用时刻。
2. **颜色恢复独立于几何探测**：任务栏局部一露出就请求刷新，不等待约 70ms 的 UIA 查找和后续收缩路径。
3. **测量 UIA 分段耗时并复用目标元素**：减少重复树查找，不能把当前 70ms 当作不可降低的物理常数。
4. 如果文字抖动或 WebView 呈现仍是主要问题，再实验**固定文字窗口＋独立不透明遮盖背景**。
5. 真正取消遮盖需求，需要可靠地取消原生时钟绘制或改变产品呈现位置；这属于单独的可行性研究，不是默认可用的修复。

## 2. 对当前归因的校准

### 已有证据支持的部分

- 文档报告的 31fps 画面捕获到了原生文字残块，说明某些“收缩感”实际上是扩展前露底。
- 动态底色变化远大于当场横向色差，说明静态颜色是重要原因。
- R7.5/R7.6 换色后用户反馈改善，支持继续检查颜色链。
- R7 探测来源为 `uia-class`，否定了之前“本机始终能走 HWND 快速路径”的判断。

### 仍需保留边界的部分

- “退出后偶发色差”不等于已经确认“只发生在 fast-path 收缩前 220ms”。短暂错色、错误颜色持续、旧事件晚到，是不同问题。
- 单场横向色差小于 2 RGB，不代表所有主题、壁纸、透明效果和位置都不存在局部差异。
- 读数后 1～6ms 落位是窗口应用耗时，不是从系统画面变化到新背景显示的完整时间。
- 同一属性矩形变化与实际文字像素、背景合成并不严格同帧。
- 原生回摆是否独立于覆盖层仍未做 A 基线对照，不应断言完全是系统行为。

另外，文档称两正常矩形存在安全包含关系也需修正：`(3660,...,3818,...)` 与 `(3649,...,3819,...)` 中，后者包含前者；锁住较窄旧值时不能用同一理由保证覆盖安全。

## 3. 当前采色实现中的具体风险

### 3.1 采样参考点仍跟着全屏位形移动

`sample_taskbar_pixel` 以共享缓存 `clock.right + 3/8/13` 为采样列。

- 正常右缘 3818 时，采样列为 3821/3826/3831；
- 全屏右缘 3769 时，变成 3772/3777/3782，可能落入覆盖层自身范围。

归属校验会阻止直接采到自己，这是正确防御；但它同时可能让退出关键时刻一直返回 None，继续使用旧色。这是采样可用性问题，单纯增加刷新调用次数未必有效。

**建议：**以任务栏/显示器和经验证的可见参考区域确定采样点，避开 overlay/mask 实际矩形，不跟随暂态原生 right。不能硬编码一条适用于所有机器的“显示桌面宽度”；显示器、DPI、任务栏重建和区域不足时应使参考区域失效并重选。

### 3.2 所有权检查与像素采集仍不是同一时刻

当前先检查三列中线的 WindowFromPoint，再读取三列上中下的像素：

- 中线归属正确不代表同列其他采样行均正确；
- 检查后播放器可再次盖上；
- 窗口命中与显示器上的合成帧不是相同的观察量。

所以归属校验是有效过滤，不能称为像素真值的充分证明。

**建议：**短时捕获一个小参考块，检查全部使用区域并做前后归属校验、样本离散度判定。保留最近可信颜色，丢弃疑似污染样本；不能只因颜色变化大就拒绝，因为任务栏确实可能合法变色。对不确定样本短时重试，而不是重新给几何收缩加等待门。

### 3.3 并发采色缺少顺序保证

`refresh_clock_overlay_appearance` 每次调用都 spawn 线程，包含 0ms 与 400ms 延迟请求。锁只保护 LAST_PUSHED_BG，不保证各次采样、发送事件和前端应用按同一时序。

例如旧线程取得颜色 A 后暂停，新线程取得 B 并发出，旧线程恢复后仍可发送 A；前端事件不带版本，会照单应用。该路径存在，是否导致当前偶发色差还需日志确认。

**建议：**单个长期工作线程合并刷新请求；按退出轮次、显示配置版本和采样序号丢弃过期结果。延迟请求在执行时读取最新状态，只充当重试，不携带旧状态覆盖新结果。

### 3.4 发送成功不等于前端已经显示

LAST_PUSHED_BG 在 emit 前更新，emit 错误被忽略。前端还同时存在初始化/主题 invoke 与事件推送，初始请求的旧响应也可能晚到覆盖更新值。

**建议：**统一颜色消息结构 `{epoch, seq, sampled_at, bg, fg}`；先注册监听再请求快照；所有响应都走同一版本判断。日志区分采样成功、发送成功、前端接收与应用。前端确认可以衡量消息延迟，但真正呈现时间仍由同步画面确认。

## 4. 第一条优化线：可信颜色尽早就绪

### 4.1 近期改动组合

1. 任务栏正常可见时保存可信颜色与参考区域，并标记采样年龄、主题/DPI/显示器上下文。
2. 进入全屏时沿用最近可信颜色；不要把“全屏之后才发现进入”误称为已经在进入前完成采样。
3. 退出首次发现参考区域属于任务栏时，提交高优先级采色请求；与 UIA 几何管线并行，但在同一个采色工作线程内串行执行。
4. 快速回摆期间限频刷新并合并重复请求；避免每个 WinEvent 创建线程。
5. 400ms 后重试作为兜底，不承担首次正确颜色的责任。
6. 失败保持可信旧色，并记录失败原因；不将未知像素当作新色推送。

### 4.2 能解决什么，不能保证什么

它能减少错色、采样不可用、旧结果晚到和换色启动过晚。进入前颜色只是预测：全屏期间壁纸、焦点或系统效果变化后，旧色可能不再匹配。

因此预采样是首帧备用值，不是无条件准确的答案。也不建议为了等新色而延迟时钟出现或收缩，这会重新引入用户已经明确不希望的等待。

## 5. 第二条优化线：降低 UIA 延迟，而非宣称 70ms 不可压缩

当前定位会尝试 HWND，创建 UIA 对象，从任务栏根查 AutomationId，再按多个类名 FindFirst。`src=uia-class` 的总耗时可能包含前面失败的尝试和树查找，不能全部当作读取矩形的固有成本。

**建议先分段测量：**初始化/创建对象、找任务栏元素、各条件查找、CurrentBoundingRectangle、校验与写入，分别计时。

如果主要成本是重复发现：

- 在专用 COM 工作线程保持 UIA 对象和已验证的时钟元素，正常时直接读取当前矩形。
- 元素失效、Explorer 重启、显示配置变化时重新发现；不要无限信任旧元素。
- 实验订阅 BoundingRectangle 属性变化作为唤醒信号，同时保留低频兜底；实际 Shell 提供者是否可靠发事件需要测量。
- 需要多属性时可批量缓存减少跨进程调用，但缓存矩形必须更新，不能把旧值当新观测。

微软提供了 UIA 的缓存机制与专用线程注意事项：[缓存属性与控件模式](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-cachingforclients)、[UIA 线程问题](https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-threading)。这些支持优化方向，不保证本机具体加速比例。

无论是否优化成功，UIA 都不得进入低级鼠标钩子/WinEvent 消息泵执行。

## 6. 更深入的架构方案比较

| 方向 | 能从结构上消除什么 | 仍然存在什么 | 建议 |
|---|---|---|---|
| 当前单窗口＋可靠采色 | 采色乱序、关键时刻采样不可用 | 颜色近似、WebView resize 与绘制时序 | 第一优先，改动较小 |
| 固定文字窗口＋独立原生不透明背景/遮盖窗口 | 文字因遮盖伸缩而 resize；背景换色不必经过 React/WebView | 仍需匹配任务栏底色；两个窗口的 z 序与输入协调 | 第二阶段原型，先测再迁移 |
| 可靠抑制原生时钟文字绘制、保留布局，再画自定义内容 | 对原生残块做动态并集遮盖的需求 | 需证明具体 Windows 版本上可行，恢复与兼容性复杂 | 单独可行性研究，不能承诺 |
| 独立固定位置时钟卡片/自有背景 | 与原生任务栏像素完全一致的需求 | 改变产品视觉与接管方式 | 只有接受产品变化时考虑 |
| 永久扩大覆盖整个并集 | 扩展/收缩边界移动 | 长期遮住音量/其他托盘元素 | 不推荐 |

### 6.1 固定文字与背景分离的可行原型

文字窗口保持固定物理矩形和布局宽度；背景窗口负责认可区域及临时 mask，以原生绘制快速换色和改变遮盖范围。前端透明部分不负责掩盖原生文字，背景必须完整遮严。

原型需要验证：背景是否真的更早呈现、文字是否不再抖动、退出时两个窗口是否同序恢复、点击与悬停是否仍正确、自动隐藏跟随是否一致。遵守项目禁止跨进程 owner 和 AttachThreadInput 的约束。

该方案是“分离绘制时序”的根本优化，不是“动态任务栏背景永远完美匹配”的保证。两个窗口的管理复杂度可能抵消收益，因此只在现有链路测出 WebView 呈现/resize 确为瓶颈时推进。

### 6.2 真正取消遮盖：可行性门槛

从需求上讲，只有原生文字不再在外部位置出现，才可以取消对它的动态遮盖。但本机探测走 uia-class，并没有已证实可独立控制绘制的经典时钟 HWND。

不能把 UIA 元素当成可调用 ShowWindow 的独立窗口，也不能假定关闭系统时钟仍保留原来的布局槽位。全局日期格式设空会影响其他应用，不适合作为默认方案。Explorer 注入/内部 Shell 修改也不应直接作为本项目的常规修复。

可行性研究只回答：是否存在本系统受支持且可恢复的方式，在保留需要的任务栏布局和交互时抑制原生内容？无法证明则停在研究结论，继续优化外部覆盖。

## 7. 不建议优先尝试的办法

- 再增加固定等待时间：与“早点收缩”的用户目标冲突。
- 直接加 CSS 缩放/淡出动画：不控制外层 HWND 的原子变更，淡出还可能暴露原生文字。
- 把 `translateZ(0)` 或 `backface-visibility` 当字体抖动根治：需要 A/B 画面验证，不能从 CSS 属性名推导结果；固定文字布局比盲目提层更明确。
- 单纯把轮询提高到很高频率：如果昂贵部分是反复查 UIA 树，应先减少查找；采色并发更多也可能恶化乱序。
- 默认改为渐变采色：先证明当前主题下局部空间差异明显；否则增加模型复杂度，解决不了错误时刻采到视频色。

## 8. 下一步实施与验收顺序

### 第一步：诊断最小闭环

给每次进程运行生成 session ID，记录 build、PID、单调时间、退出轮次；墙钟只用于人工查看，避免再次被 NTP 回拨误导。

颜色记录：请求来源、参考坐标、归属检查、采样结果或失败原因、seq、发送结果、前端应用 seq。几何记录与视频共享可对齐的单调时间。

画面覆盖退出前后短窗口，用实际可持续的 60fps 或更高采集；31fps 能查明显残块，但不能证明不存在一帧级抖动。保留同位置正常帧作为参考。

### 第二步：优先做颜色链改造

稳定参考区域＋单工作线程＋版本丢弃＋首次可见请求。保留现有几何策略不变，比较有无改造的同场操作。

分别记录：首次有效颜色到达、错误颜色持续、颜色回退次数、采样失败率、原生残块帧数、文字位置变化。不能只以“shrink 后几毫秒”验收颜色问题。

### 第三步：以数据决定是否做架构改造

- 若主要失败是样本无效/乱序：继续颜色链，不拆窗口。
- 若正确色已到前端但画面迟迟更新，或文字随 mask resize 抖动：做固定文字＋原生背景原型。
- 若主要残余仍是首读慢：做 UIA 元素复用/事件唤醒实验。
- 若回摆显著拉长所有过程：进行应用开/关的原生基线对照，再决定是否调整 z 序控制。

建议至少覆盖长全屏、短全屏、连续快速切换、冷启动第一次退出、主题/壁纸变化和跨零点布局变化；每种固定若干次并报告失败次数，而不是单次 ffplay 成功就归档根治。自动隐藏和不同 DPI 单独回归。

## 9. 最终推荐

**最有性价比的下一步是颜色采集与交付的可靠性重构，而不是继续压缩收缩动作。** 当前代码中稳定采样点、并发采色顺序、前端版本一致性都存在明确改进空间。

如果用户未来要求更高的视觉一致性，再用原型评估固定文字窗口＋原生背景层。若要求所有任务栏材质和系统过渡中严格零差异，外部采样遮盖不能给出普遍保证，需要研究取消原生内容绘制或接受不同的产品呈现方式。

目前维持暂停是合理选择；上述建议是恢复工作的路线图，不是本轮已经执行的改动。
