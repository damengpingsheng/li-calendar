# 全屏退出时钟瞬态错位：第四阶段评审与可能原因

评审基线：`e946915`；实现包含 `c34c23f`（并集遮盖）和 `8a599f0`（覆盖探测去抖）。

依据：更新后的问题档案、当前 Rust/前端代码、`D:\agents_tmp\menu_dbg.log` 中的带时间戳记录。本轮仅做静态分析与日志核对，未操作播放器复现，未修改应用代码。日志行号对应评审时文件。

## 1. 结论

z 序恢复滞后是当前优先验证的假设，但“任务栏退出瞬间重申 topmost”不是日志已经证明的事实，更不是唯一解释。

目前更可能是多个机制叠加：

1. 覆盖判定、去抖与 z 序恢复绑在一起，推迟了覆盖层恢复；
2. 左侧任务栏探测点与右侧时钟区域的遮挡情况并不一致；
3. 并集遮盖自身引入了宽度变化、相邻图标遮挡，以及一个位置与尺寸可能不一致的代码路径；
4. 窗口外框正确时，仍未排除 WebView 背景绘制/合成未就绪。

不宜将完整异常序列归为一个“真正根因”。建议分别度量：原生时钟先出现的时长、自定义文字出现的时刻、遮盖扩展/收缩、喇叭被遮时长。

## 2. 新证据能说明什么

带时间戳日志比之前更有用。例如：

```text
[03:56:32.839] phase->normal
[03:56:32.840] apply ... after=(3618,2076,3818,2160)
[03:56:33.058] shrink requested
[03:56:33.063] apply ... after=(3660,2076,3818,2160)
```

这支持“本轮切换后约 223ms 执行收缩”，不能说明用户看到原生时钟的起点，也不能直接证明 z 序。

日志没有同步记录原始三点命中结果、窗口相对 z 序、播放器/覆盖层 topmost 属性、实际退出操作时刻和画面。因此：

- 状态翻转循环可能来自判定波动，也可能包含用户真实反复切换；需要逐轮操作标记才能区分。
- 阶段日志变干净说明去抖减少了输出状态翻转，不等于底层观测已稳定。
- `after` 是操作后外框采样；`ok` 仍是 GetWindowRect 成功，不是 SetWindowPos 成功。
- 在正常恢复时刻外框正确，不能排除采样之间的异常、其他尺寸路径和绘制问题。

## 3. 可能原因与优先级

### 3.1 高优先级：恢复 z 序被去抖延迟

`update_clock_overlay_visibility` 的 Visible 分支在等待不足 300ms 时直接返回，确实不会恢复 topmost。

但 300ms 是最短等待条件，不是完成时限：如果仅有 500ms 轮询，首次发现 Visible 后可能要等下一轮才能放行；首次发现本身也有轮询延迟。原始 Covered 再出现会重置等待。

这能够解释“原生时钟先出现，稍后覆盖层才出现”的放大现象，前提是这段期间覆盖层确实处于任务栏下方或未形成可见背景。

需要区分两种 z 序机制：

- 任务栏相对覆盖层上升；
- 插入到播放器之后时，播放器已变成非 topmost 或其相对层级变化，覆盖层被排到不合适的层级。

代码注释“全屏窗口必然 topmost”不能作为所有过渡时刻的运行时保证。应记录两个窗口的 WS_EX_TOPMOST 及实际相对顺序。SetWindowPos 的插入目标与 topmost 分组有关，具体规则见 [微软 SetWindowPos 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowpos)。

此外，`LAST_FOLLOW_POS` 只比较上次请求参数，不能发现外部窗口改变 z 序后相同目标需要重新应用。恢复不应只依赖“请求值改变”。

### 3.2 高优先级：全任务栏探测代理不了时钟区域

当前探测位于任务栏横向 10%/25%/40%，任意一点命中任务栏即 Visible，否则取第一个外部根窗口作为盖住者。

播放器缩回时，这些位置可能仍被播放器覆盖，而右侧时钟已经露出；也可能反过来。命中差异不一定是 API 抖动，可能是采样位置真实处于不同遮挡区域。

因此，“任务栏被盖”和“时钟区域应当保持在播放器下面”不宜视为同一个布尔值。应同步记录时钟内容区、扩展遮盖区和原有三点的命中对象，确认具体失配。

### 3.3 明确代码风险：Covered 分支能产生位置和尺寸不配套

当前 Covered 分支默认：

```text
x = endorsed.left
y = endorsed.top
size = None
```

仅当 `union(endorsed, native_observed) != endorsed` 时，才改用并集位置并显式设置尺寸。

存在如下可达顺序：

1. 已展开 mask=(3618,2076,3818,2160)，实际宽度 200；
2. 阶段仍为 Covered（包括退出去抖期间），新样本已经回到 endorsed；
3. 后续一次 Covered 调用发现并集等于 endorsed；
4. 返回 x=3660、宽高=0，最终使用 SWP_NOSIZE；
5. 窗口左缘移动到 3660，却保留宽度 200，右缘变成 3860。

同时 g.mask 可能仍保存原并集，造成状态记录与实际窗口不一致。前端相对窗口右对齐，文字也可能随实际右缘移动。

这是从代码可推出的风险，尚未在本轮日志中确认发生，不能当作已复现根因。应优先补测试或执行日志验证。期望规则：保持 mask 就同时保持其完整位置和尺寸；收缩则在同一受控路径应用完整 endorsed。

### 3.4 明确代码风险：遮盖数据跨轮复用及单样本收缩

- `native_observed` 在被盖时赋值，但进入新一轮 Covered 未清空或验证其轮次/年龄，新一轮可能直接用上轮 3618 扩展。
- `note_probe` 在 Normal 下只要一次 `rect==endorsed` 就置 `shrink_requested=true`；不存在文档所称的多次稳定确认。
- 一个后续不一致样本不会立即撤销已置位的收缩请求，存在消费过时请求的可能。
- `relocate` 先复制状态、释放锁、执行窗口操作，再修改状态；并发重新进入 Covered 时，旧 Normal 操作没有轮次校验。

这些都可能放大快速反复切换中的扩展/收缩，并不是 z 序修复能自动消除的。

### 3.5 中优先级：背景绘制与合成恢复

窗口使用透明宿主，背景依靠 WebView 页面绘制。GetWindowRect 正确无法确认新增背景区域或原内容是否已实际显示。

若异常帧中覆盖层已经位于任务栏上方，却仍可见原生内容，应继续检查背景绘制/合成，而不能继续归因于 z 序。可用固定不透明诊断色做对照，并同步记录画面；前端完成一次回调不等于显示器已呈现那一帧。

### 3.6 喇叭被遮至少部分是 N1 的直接行为

并集向左扩大 42px，在该区域存在喇叭图标时，遮盖它是实现的直接结果，不需要额外解释为系统异常。

但“原生残块与喇叭永远不可两全”仍说得过满：原生边界框覆盖该区域，并不证明同一帧中这里确实存在原生文字像素。当前遮盖也可能使用上一轮原生矩形。先确认异常帧的实际像素，再讨论遮盖范围的不可避免性。

## 4. 对 F1 的评价

F1 把几何稳定等待与可见性恢复分开，方向合理，适合做单变量实验。但 `WindowFromPoint(时钟中心) 的根窗口 != last_cover` 不是充分条件：

- 可能命中覆盖层自己，此时不应无条件反复置顶。
- 可能命中其他应用、菜单或另一个全屏窗口，不能因为不是旧播放器就抢到它们上面。
- 中心点不被盖，不代表整个内容区和扩展区均已不被盖，过早置顶可能压到仍在显示的视频部分。
- last_cover 可能过期或失效，且当前左侧探测器选择的盖住者不一定代表时钟区域。

建议 F1 实验至少分类：命中任务栏、命中本覆盖层、命中旧盖住者、命中其他窗口、未知；仅针对明确需要恢复覆盖的情况操作，并记录操作前后相对 z 序及 SetWindowPos 的真实返回结果。

不能承诺“几十毫秒内不可感知”：事件是否到达、点位是否代表视觉曝光、背景是否已绘制都影响结果。

文档“唯一 z 夺回点”也需收窄：Normal 下 `relocate_clock_overlay_endorsed` 仍会重申 topmost。真正问题是恢复条件、调用时机及外部 z 序变化未被观测。

## 5. 建议的验证顺序

### 第一步：先区分层级问题与绘制问题

每轮记录用户退出操作标记；从退出前到退出后短时采样：

- 单调时间、线程、调用来源和原始探测结果；
- overlay/tray/player 句柄、矩形、可见状态、topmost 属性及相对 z 序；
- endorsed/mask/native_observed 的完整矩形、观测时间、轮次；
- SetWindowPos 返回值与操作后矩形；
- 同步局部连续画面。

WindowFromPoint 是窗口命中工具，不能独立充当像素真值；微软对不同命中函数的说明见 [窗口命中测试差异](https://devblogs.microsoft.com/oldnewthing/20101230-00/?p=11873)。

判据：

| 异常帧证据 | 优先解释 |
|---|---|
| tray 在 overlay 上方，恢复 z 后原生内容立刻消失 | z 序恢复不足 |
| overlay 已在上方，背景仍未遮严 | 绘制/合成或透明区域 |
| 实际右缘由 3818 变成 3860 等 | mask 位置尺寸不一致路径 |
| 新轮次无新观测就扩展 | 遮盖样本跨轮复用 |
| 左侧三点仍 Covered，但右侧内容区已露出 | 探测区域与控制目标失配 |

### 第二步：做 F1 单变量对照

保持内容、mask 和收缩策略不变，仅提前恢复经过验证的 z 序。若“原生先出现”缩短但“过宽/喇叭被遮”仍在，应分别记录两个问题，不把残余宽度变化视为 F1 同一失败机制。

### 第三步：修复独立的遮盖一致性问题

统一使用完整目标 RECT；为观测/收缩操作附加轮次与有效期；明确稳定收缩条件；避免旧任务应用到新一轮。之后再评估是否仍需 8 秒防抖门及其对真实布局变更的代价。

## 6. 文档建议

头部宜写：“未根治：z 序恢复滞后为优先假设，遮盖状态一致性存在待验证风险”。

删去或标为历史推断的措辞：唯一解释、几何管线彻底清白、状态机完全干净、精确原生滞留时长。当前日志可测的是检测和操作的时间差，不是系统布局改变或用户看见内容的精确时刻。

特别应纠正 N1“不是单次探测收缩”的描述：当前代码就是单次相等样本发出请求，随后 relocate 执行；两步调用不等于两次稳定确认。

## 7. 最终判断

最值得验证的主线是：**右侧时钟已经露出，但左侧探测/去抖仍维持 Covered，覆盖层恢复层级被延迟；之后并集遮盖与收缩产生可见宽度变化。**

任务栏重申 topmost 是这一主线中的候选机制，尚未被直接观测。与此同时，Covered 分支“恢复认可左缘但保留扩展宽度”、旧原生样本跨轮复用和单样本收缩是具体代码风险，应与 F1 分开验证和处理。
