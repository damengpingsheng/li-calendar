# 第三代注入时钟：路线决策与实施方案评审

本轮依据：2026-09-12 19:20 后更新的五份根目录文档——《STATE-当前进度》《方案-注入式任务栏时钟第三代》《总结-退出全屏左缘收缩排障R1-R6》《PLAN-时钟覆盖式接管》《对照与取证-ElevenClock稳定性与优效日历机制》。同时核对了本机 SDK 的 xamlom.h、现有天气代码和微软 API 文档。

本轮不实施注入、不修改运行程序。对照实验和优效取证沿用文档记录，未重新运行实验或独立逆向验证全部机制。

## 1. 总体判断

**同意把注入原生 XAML 时钟列为首选 PoC 路线；不同意把设计稿 v1 当作已具备充分可行性依据的完整实施规格。**

路线切换最有力的依据是：用户要横向五段内容、独立样式和点击，而且希望与任务栏自然融合。让面板参与任务栏布局，有机会直接删除采样仿背景、覆盖原生文字和跨窗口跟踪这些职责。这个收益并不依赖“旧方案已经证明无法修好”。

ElevenClock 对照也很有价值：它削弱了“这台机器上任何时钟都无法稳定”的说法，证明独立窗口仍有可行实现。但这并不单独证明必须选择注入，更不能证明 WebView2 就是旧故障唯一根因。**功能适配与实现复杂度是切换理由，故障根因尚未完全收敛是另一个事实。**

建议决策措辞：批准有限范围的通道、布局、交互和恢复验证；通过后才决定正式替换。不要现在就承诺所有旧问题消失、4～5 天完成生产级改造。

## 2. 对照与取证结论需要收窄

### ElevenClock：成功样本，不是根因定案

12 次全屏切换、退出后约 1 秒内恢复、尾段 40 秒以上稳定，支持该版本在这组测试里未出现持续消失。测试期间没有新 0x1CC，不能推导它已经通过同样的资源超时条件。

“问题在我们的实现，不是环境”“持续消失=WebView2+恢复链”的说法仍过强。更准确的是：优先检查我们的实现及它与系统的交互，停止使用环境解释排除软件问题。Qt 也不能未经绘制后端核对就等同于 GDI。

另外，用户仍观察到 ElevenClock 色差，只是消退更快。因此它证明覆盖路线可以更稳定，不证明完全满足无缝融合的体验目标。

### 优效：支持路线先例，不证明全部设计细节

模块加载、时钟区域归属以及 TextBlock/Dispatcher 符号共同提供了较强的进程内改造证据。但符号名不能独立证明：

- 精确加载通道和完整 COM 生命周期；
- 自建五段面板能自动挤开全部相邻元素；
- 无需 tooltip 处理；
- 强杀后 DLL 安全无害；
- 我们可以直接复制其卸载模式。

尤其文档列出了 `RestoreSuppressedClockToolTips` / `RestoreClockHoverTracking`，这反而提示成熟实现也在专门管理 tooltip 和悬停。不能下一份方案又写“原生事件，所以 tooltip 对抗自然消失”。

## 3. 实施前必须修正的设计问题

### P0：D1/D2 混合了两套加载模型

方案先用远程线程 LoadLibrary，再要求 DLL 内调用 InitializeXamlDiagnosticsEx，且标注“必须在 explorer 内调用”。微软接口本身接收目标 PID、待注入 DLL 和实现 IObjectWithSite 的 COM CLSID；它就是诊断会话入口，不应把额外的远程加载视为必需前置。

M0 应优先验证“外部宿主建立诊断会话→加载 TAP DLL→COM 类工厂创建对象→SetSite 获取 IXamlDiagnostics→订阅视觉树服务”的完整最小闭环。当前设计缺了 DllGetClassObject、IClassFactory、IObjectWithSite、订阅/退订和引用所有权这些关键内容。远程加载可作为独立备选，不能在没有必要性证据时叠两条路径。

这不是保证外部诊断入口在本机一定可用，而是把需要验证的真实接口模型放到 M0。直接 LoadLibrary 成功只证明 DLL 加载，不证明已经获得 XAML 访问能力。[微软 InitializeXamlDiagnosticsEx](https://learn.microsoft.com/en-us/windows/win32/api/xamlom/nf-xamlom-initializexamldiagnosticsex)

### P0：静态 CRT 与 DisableThreadLibraryCalls 明确冲突

D4 要求 /MT，而 §4.2 要求 DllMain 调用 DisableThreadLibraryCalls。微软明确说明静态 CRT 依赖线程附加/分离通知，不应这样调用。应修正这一组合，DllMain 尽量只保留必要初始化，业务启动交给明确的初始化入口。[微软 DisableThreadLibraryCalls](https://learn.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-disablethreadlibrarycalls)

C++/WinRT 是合理选择，但“使用 C++、静态 CRT、try/catch 就比 Rust 安全”并非成立的保证。异常边界处理不了所有内存错误、进程终止和死锁。

### P0：恢复界面与卸载 DLL 必须拆开

当前“恢复 TextBlock→FreeLibraryAndExitThread”严重不完整。XAML 事件、定时器、诊断 COM 对象、排队 dispatcher 回调和工作线程都可能继续引用 DLL 中的代码。

至少需要：停止接收新工作→使旧代回调失效→停定时器/解绑事件/退订诊断→在所属 UI 线程移除面板并恢复属性→排空在途回调→释放引用和工作线程→确认满足模块卸载条件。任何一环没有确认，不能强行卸载。

FreeLibraryAndExitThread 只减少模块引用并退出调用线程，不会替应用清理上述对象，也不保证模块一定从列表消失。[微软 FreeLibraryAndExitThread](https://learn.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-freelibraryandexitthread)

PoC 可以先允许“已恢复原生界面、停止活动、模块暂留”，再单独证明安全卸载。正式产品仍需完成其声明的退出和卸载能力。不能为了满足 M0 的“模块无残留”验收而提前 FreeLibrary。

### P0：先证明长条布局，不要假定自动撑宽

StackPanel 的期望尺寸仍受祖先容器、模板、列宽、MaxWidth、裁剪和任务栏可用空间约束。能够改原生 Text，不等于能把它扩成五段独立面板。

建议 M1 后立即做静态长条验证：主任务栏容器的实际布局约束、175% DPI、低分辨率、托盘图标增加、最长天气文字、自动隐藏和主题切换。必须定义最大宽度、截断与段隐藏优先级，时间段始终保留。空间不足没有明确策略，自动布局也会产生拥挤或裁剪。

此外，“M1 元素定位失败→退回只改 Text”不是有效退路：定位不到对象就无法改 Text。只改 Text 是 M2 面板扩展失败时的功能降级，且不满足完整五段需求。

### P0：输入和 tooltip 不能等到删旧代码后再验证

Tapped/RightTapped 是路由事件，父级系统时钟可能仍处理输入。需要验证事件处理标志、父级已处理事件、点击穿透、键盘/触摸以及系统日历是否被同时打开。[微软 XAML 路由事件](https://learn.microsoft.com/en-us/windows/uwp/xaml-platform/events-and-routed-events-overview)

XAML UI 回调内只能投递有界的异步消息，不能同步等待 pipe 或宿主。跨进程弹原生菜单还涉及前台获取、坐标换算和显示时机；不能假定现有菜单函数原样复用就完全正确。

现有 AGENTS 明确记录了 tooltip 和菜单的真实时序约束。新机制可以替代旧实现，但必须先证明行为等价，再退役旧分支。

## 4. 应补齐的工程约束

| 项目 | 需要补充 |
|---|---|
| XAML 树生命周期 | 不只监视 Explorer PID；同 PID 内任务栏/模板重建也要处理。按元素实例与代次管理，避免旧回调修改新树或重复插入 |
| 恢复策略 | 记录原始 Visibility、局部值/绑定及修改过的容器属性；不能一律 Visible 或恢复默认值。只撤销自己仍拥有的改动，避免覆盖系统后续更新 |
| 原生文字刷新 | 改一次 Text 保持 10 分钟可能被系统刷新覆盖。M1 要验证正常更新、主题切换、模板重建时的行为，避免又开始高频抢写 |
| 心跳 | pipe 断开可触发恢复，另用单调时间心跳兜底。读管道不能阻塞存活检测；UI 线程卡住时，不能保证 30 秒内完成恢复 |
| 协议 | 版本、会话标识、消息长度上限、更新序号、ready/restored/failed 确认、断线重连；ready 必须说明是加载成功还是面板可用 |
| 连接边界 | 按用户/会话限定管道访问和端点身份；PID 后缀只是命名，不是身份校验。拒绝非预期客户端和超大/无效消息 |
| 同机共存 | 文档称优效 DLL 仍驻留，不能直接假定无害。PoC 要记录已有任务栏修改组件，先建立无竞争基线，再测试冲突时安全退出 |
| 语言边界 | 网络和复杂计算放 Rust 合理；DLL 仍须解析协议，不能写成“解析全部在 host”。任何解析均有大小限制和异常处理 |
| 时间更新 | 定时器读取当前系统时间，不靠 tick 累加；处理休眠唤醒、时区/时间调整、日期切换 |

天气迁移工作量也被低估：当前 `src/http/weather.ts` 是前端动态 script 加载、window 变量和 localStorage 缓存，本轮 Cargo.toml 未发现所称现成 reqwest/tyme 依赖。迁到 Rust 涉及数据获取、解析、城市配置和缓存迁移，不是简单接现有后端接口。日历库应先固定版本，核对闰月、节气时刻、假日数据覆盖年份；不要在基础通道验证前把这些一起搬入。

“无符号依赖”是 XamlDiagnostics 路线的潜在优点，但它仍依赖树结构、接口和系统行为，不能直接推导跨版本韧性最好。公开诊断 API 也不等于微软支持把 Explorer 时钟当插件扩展点。

## 5. 调整实施顺序

| 阶段 | 验证目标 | 继续条件 |
|---|---|---|
| A：只读通道 | TAP/COM 初始化、正确识别主任务栏时钟、退订与失联停止活动 | 不改变任务栏也能反复建立和结束会话，无重复订阅 |
| B：可逆修改 | 一次样式/文本修改与原样恢复；宿主退出/被杀；树重建 | 恢复行为可靠，不依赖重启 Explorer 才能恢复界面 |
| C：静态五段 | 使用假数据验证布局、宽度上限、输入和 tooltip | 长条需求成立，原生菜单/系统日历不重复触发，全屏和 DPI 通过 |
| D：数据接入 | 接 Rust 日历/天气、配置、时间变化 | 断网/坏数据不影响时钟和 Explorer |
| E：完整生命周期 | 重连、任务栏重建、安全卸载、版本不匹配、长期运行 | 真实操作验收通过，失败时有可重复验证的原生回退 |
| F：正式切换 | 停用旧时钟路径，稳定观察后删除 | C～E 均通过，保留明确回退方式 |

**原方案在 M2 后就删除旧菜单/tooltip 分支过早：此时 M3 输入、M4 生命周期尚未验证。** 可以先用互斥开关停用旧路径，避免两套同时操作；通过完整验收后再删。

反过来，也不必把修好旧 WebView 看门狗当注入 PoC 的前置。其所属模块会退役，优先保证旧路径能够干净停用。若仍作为用户日常回退版本使用，再按需要修它。前端响应心跳不等于呈现心跳，这个概念在新对照文档中也应纠正。

4～5 天只能作为乐观 PoC 预算；在通道、布局和卸载尚未验证前，不能作为完整交付承诺。最有价值的早期输出是 A～C 是否成立及明确失败原因，而不是先写完整注入器、天气服务再发现面板无法占据需要的宽度。

## 6. 文档整理建议

目前新结论主要是追加到顶部和后记，但 STATE 的后续步骤仍写“GDI 唯一路径”，总结开头仍写“环境级限制”，PLAN 仍有“不可动摇”的旧 WebView 决策。应把它们明确放到历史归档，当前状态只保留一套生效结论，避免下一轮继续执行过时路线。

建议当前决策统一为：

> 横向五段需求使原生 XAML 改造成为首选验证路线。先完成诊断通道、可逆生命周期、静态长条和输入验证；未通过前保留旧实现但互斥运行。注入路径预期消除覆盖层特有的同步职责，同时引入 Explorer 内生命周期与系统版本适配责任。正式切换由 PoC 证据决定，不以旧方案“绝对无解”为前提。

**这次切换方向合理。要避免重演上一轮，关键是把“有商用先例”与“本方案的每个细节已经成立”分开，并把恢复、输入和长条布局提前设为准入条件。**
