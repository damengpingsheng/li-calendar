# 方案：时钟时间/日期字号与格式可调（六段统一）——2026-09-19

三轮可行性讨论（初评 → 对方评审 → 复评）收敛后的实施前方案。**本文档即开工依据，等待指令。**
代码行号以 2026-09-19 `squashed` 分支（tap v63 / host v63）为准。

---

## 8. 实施记录（2026-09-19 当日开工，tap v64）

按本方案完成六段统一实施，产物=**tap v64 + host v64**：

- **tap.cpp（v64）**：`PanelStyle`/`g_seg`/`g_segText` 等全部 4→6；`SEGNAME[6]`（time=4，date=5）；默认 `order={0,1,2,5,3,4}`（行内视觉与 v63 一致：行2=日期、农历）。原生 Time/Date **不再 reparent/包装**（dateWrap 退役），改原位隐藏：`hideNative`（SegWanted 时快照+Collapsed；段缺席恢复原生可见兜底）；`PanelTick` 每秒复核隐藏（`re-hide` 日志=拉锯观测点）；`PanelFree(true)` 回写快照恢复。v35 自我 REM 抑制（`g_selfReparent`/`SelfReparentPositive`）随 reparent 退役。`PanelReflow`：time/date 永不隐藏（候选=数据段 0..3），行预算下限=该行时钟段期望宽（capw 让位）；time/date 段 `MaxWidth=capw`。新增 `c1t` 命令（text-only 快路径，mode12；段缺席且面板要求在场→退化全量 PanelBuild）。
- **host（Rust）**：`TAP_VER=64`/管道名 b64/`tap_dll_path`→tap64；`ClockbarStyleConfig` 增 `timeFormat`/`dateFormat`；`style_ext_json` 六段化（colors/sizes 覆盖 6 段，hide/rows 仍 4 数据段）+ `migrate_order_5to6`（五元素旧配置在首个行 2 段前插 date=视觉不变）；data 线程 c1set **恒带当前时间/日期文本**（会话引导即时有值），比较口径=不含时钟字段的 base（秒级变化不触发全量重发）；新增 `spawn_tick_thread` 走时线程（250ms 轮询+变化才发，独立于天气 HTTP；`send_c1t` 幂等不等待 ack；`TICK_REINIT` 与 DATA_REINIT 同源置位独立消费；断管 2s 退避+管道自愈补发）；`c1tack` 走 v63 独立原子模板计数（`c1t_ack_count`，仅诊断）。
- **设置页**：`ClockbarStyle` 类型增 `timeFormat`/`dateFormat`；默认 order 六元素；`normalizeStyle` 五→六迁移（与后端同规则）；时间/日期格式预设下拉（时间 4 档/日期 6 档）；六段表统一——time/date 行显示/所在列禁用（恒显恒行），排序/颜色/字号可用。
- **格式 token**（host `format_time_text`/`format_date_text`，长 token 优先逐位匹配，字面字符直出）：时间 `HH H hh h mm m ss tt(上午/下午)`，缺省 `HH:mm`；日期 `yyyy yy MM M dd d ddd(周六) dddd(星期六)`，缺省 `yyyy/M/d`。输出剔除引号/反斜杠/控制符（tap 限界 JSON 双端防御）。

**待真机验证项**（部署后日志+用户实测）：①原生 Time/Date 隐藏无拉锯（`PANEL tick: re-hide` 日志应零出现或极少）；②悬停 tooltip 行为；③主题翻转/休眠唤醒/断管重连后时钟即时恢复；④时间字号 2× 垂直空间是否裁切。

### 8.1 v64a（终版 tap v65）：系统分钟回写 Date 可见性的渲染前压回

v64 首次部署实测发现：**系统每分钟 :01 回写原生 Date 的 Visibility=Visible**（非高频拉锯，固定每分钟一次）——tick 1s 复核意味着原生日期行每分钟闪现 ≤1s。修复：`RegisterPropertyChangedCallback(VisibilityProperty)` **渲染前即时压回**（回调在 DP 变更通知内同步执行，Collapsed 回写不产生可见帧；`g_visGuard` 防重入；段缺席/面板不在场时不干预；PanelOrphan 重置注册标志防新代元素漏注册）。tick 复核降级为兜底。tap 因旧模块被 explorer 钉住升版 v65（host 管道 b65/`tap_dll_path` 同步）。

**部署后实测（2026-09-19 22:00~22:03，tap v65 日志）**：
- `VIS re-hide seg=5 pre-render` 每分钟恰一次（:00.7），`PANEL tick: re-hide` 兜底**零出现**——闪烁消除；
- 六段面板构建正常（用户旧配置 festival@日期行迁移正确：行2=日期|节日|农历，行1=天气|时间）；
- 走时链健康：c1t 分钟级到达、c1tack 应答、REINIT 补发路径实测通过；
- 旧 tap v64 随旧宿主退出走 AUTO 恢复（`PANEL free (restoreNative=1)`），原生元素无残留接管。



---

## 1. 需求

任务栏时钟的**时间与日期**两项：

1. 字号可调（独立于其他段）
2. 文案格式可调（如 `HH:mm` / 12 小时制 / `M月d日 周三` 等）

## 2. 现状（已核实代码事实）

tap.dll 注入 explorer，改造原生任务栏时钟 XAML 树：

- **时间/日期是原生 TextBlock**（`g_timeRef`/`g_dateRef`）：系统 VM 每秒写 Time.Text，日期文案跟随系统区域设置。tap 将其 reparent/包装重排，但**文案与字号均不可调**。
- **自建 4 段**（天气/节日/节气/农历）已有全套可调项：每段字号倍率 `size_<id>`（0.5~2.0）、颜色、行归属、顺序、间距（[data.rs:208](../src-tauri/src/clockbar/data.rs#L208)）。
- 段模型：`order[5]`（0..3=数据段，4=Time；[tap.cpp:1152](../src-tauri/clockbar/tap/tap.cpp#L1152)）；**Date 不在 order 内**，经 dateWrap 固定在第二行首位。
- 字号计算：`ApplySegLook` 活读原生 `Time.FontSize`（行 2 用 `g_dateFontSize` 只读快照）× `fontscale` × `size[i]`（[tap.cpp:1211](../src-tauri/clockbar/tap/tap.cpp#L1211)）。只写自建段，原生零写入。**无累乘缺陷**（读原生、写自建，原生不变则重复调用幂等）。
- host 数据线程：单循环 sleep 1000ms，天气 `fetch_now_any()` **同步内联**（30min 刷新/失败 30s 重试，[data.rs:357](../src-tauri/src/clockbar/data.rs#L357)）；c1set 只在行变化时发送；REINIT 信号强制重发。
- tap 更新路径：c1set 一律 mode10 → `PanelBuild(false)` **全量**（重放外观/摘挂全部子项/喂 PanelReflow 预算隐藏，[tap.cpp:2296](../src-tauri/clockbar/tap/tap.cpp#L2296)）。
- 通信：全局消费游标 `CURSOR` 并发 `wait_for` 会**互吃消息**（B 阶段血泪 #7）；v63 起菜单类 ack 走 `push_line` 顺手解析的**独立原子**（[session.rs:40-84](../src-tauri/src/clockbar/session.rs#L40)）；`send_c1set` 经 `wait_for` 最长等 15s（[session.rs:342](../src-tauri/src/clockbar/session.rs#L342)）。
- 恢复：`Snap` 只快照 Text 路由（`origText/propIdx`，[tap.cpp:742](../src-tauri/clockbar/tap/tap.cpp#L742)）；`PanelFree` 恢复原生元素**位置**（Time/Date 原位回插，[tap.cpp:2210](../src-tauri/clockbar/tap/tap.cpp#L2210)）；断线 `AutoRestoreOnDisconnect`。
- 项目纪律（tap.cpp 文件头 15-19 行）：**「凡系统持有写权的属性永不进控制回路」**、v34 根治「对原生元素零布局属性写入」。

## 3. 决策（路线裁定）

| 路线 | 结论 | 理由 |
|---|---|---|
| A. 周期性 settext 覆盖原生文案 | **否决** | 与系统每秒写 Time.Text 竞态、顺序不确定会闪字；`CmdSet` 为一次性设计（`already_modified` 护栏），改造后仍躲不开竞态 |
| 中间方案. 只写原生 FontSize | **跳过** | 需新做快照/恢复/跨代重放一整套，且终将被六段方案废弃（重复实现+恢复负担）；写原生 FontSize 踩「零写入」纪律灰区（是佐证非证明——不宣称必然出问题） |
| **B. 六段统一（自建时间/日期）** | **采纳** | 时间/日期变自建段，文案由 host 按用户格式下发，字号/颜色/行归属全走现成段系统；复用度最高，无竞态 |

统一后段模型 = **4 扩展段 + time + date = 6 段**（初评误算为 7，已纠正：现 order 含 time 共 5，Date 是第 6 个元素而非第 7）。

**字号联动改为独立**：段字号基准 = **定位时快照的系统字号**（勿活读，防未来写回累乘；`g_dateFontSize` 已有只读快照先例），各段独立倍率；`timeSize`/`dateSize` 只是多两个倍率字段。用户「只想调大时间数字」时天气等段不应被联动放大。

## 4. 设计要点

1. **六段模型全链路**：`order[5]→[6]`；tap 侧 `SEGNAME`/`segs[4]` 解析表/`g_segText[4][64]`/`show·colors·sizes·rows[4]` 定长数组、`JGetOrder` 校验、reflow 隐藏优先级 `seq[]`；host 侧 `SEG_IDS` 扩容+order 合法性校验（[data.rs:183](../src-tauri/src/clockbar/data.rs#L183)）；旧 `clockbarStyle` 配置迁移。
2. **格式化在 host 侧**：时间/日期文本按用户配置格式化后经 pipe 下发；秒显可选。设置页提供格式选择控件。
3. **走时线程独立**（不进天气循环）：对齐墙钟计算下一刷新点（秒显=每秒，否则每分钟）；天气同步 HTTP 会拖住循环（已核实），时钟必须独立跳动。
4. **响应分发走 v63 模板**：走时命令 ack 由 `push_line` 顺手解析进独立原子（同 `TTC_ACK`），**不得使用全局 `CURSOR`/`wait_for`**（与数据线程互吃响应）；文本命令天然幂等（下条覆盖上条），ack 可降级为日志。host 侧两线程并发写 pipe 需串行化（包互斥）。
5. **重连补发与会话代次**：走时线程挂 REINIT 类信号，管道重建后立即补发当前时间；旧会话尾巴更新不得污染新会话。
6. **text-only 快路径**：c1set 现走全量 `PanelBuild`，每秒一次不可接受。新增轻量命令：只 `Text()` 赋值，**不拆挂、不重排、不重放样式**。但 text-only ≠ 冻结布局：`Text()` 自动 InvalidateMeasure → `SizeChanged` → 现有 `PanelReflow` 链路保持畅通，宽度变化（分钟数字/日期变长）由 XAML 自然度量；快路径不得阻断该链。
7. **宽度预算优先级**：时间/日期为预算**第一优先**（先保证期望宽，剩余额度分给扩展段）；超预算裁扩展段、不裁时钟；极端情况 capw 为时间让位增长。豁免自动隐藏 ≠ 不被裁切，需显式设计。
8. **隐藏豁免**：时间/日期段不吃 `SegTextBlank` 空白拆建规则、不吃 reflow 自动隐藏（继承现「时间段永不丢」语义，[tap.cpp:1754](../src-tauri/clockbar/tap/tap.cpp#L1754)）。
9. **原生 Time/Date 隐藏**：Visibility 一次性写入 + 进快照恢复。**拉锯风险为设计假设待真机验证**（v33「隐藏不复活」为早期实证，文件头较新表述已演化为「系统写 Visibility 与我们同向，拉锯不存在」——方向一致才是无拉锯原因；Time 与 Date 需分别重验）。
10. **悬停/tooltip**：Win11 时钟悬停 tooltip 疑似绑定原生文本，隐藏原生后行为**待真机核实**（标记为待验证，非确定事实）。

## 5. 实施顺序

1. **阶段 0：原型验证（风险最集中，先行）**
   - ① 自建时间/日期段全生命周期：建/gen 重建/跨代重放/断线恢复；
   - ② 走时线程与 pipe 独立存活 + 重连补发 + 响应独立原子；
   - ③ 隐藏原生 Time/Date 无拉锯（真机）。
   - 三件过关才进阶段 1；过不了则回本文档重审。
2. **阶段 1：六段模型全链路**（tap 协议/校验/数组扩容 + host 配置/迁移 + text-only 快路径 + 预算优先级）。
3. **阶段 2：设置页**（时间/日期字号滑条 + 格式选择 + 段样式区并入现有五列网格）。
4. **真机验收**（按 AGENTS.md 桌面集成类全项）：主题翻转、休眠唤醒、断管重连、悬停/tooltip、左右键点击、右键菜单不受影响；合成注入不能替代真实点击验收。

验证成本评估：tap 侧生命周期与真机验证为成本大头，设置页/配置为体力活；整体按**中等规模改造**排期。

## 6. 三轮讨论收敛记录（摘要）

- **R1（初评）**：字号「小改即可」、路线 B「直接复用现成段系统」两个乐观判断；order 误算 5→7。
- **R2（评审）**：纠正为 6 段；恢复非现成（Snap 只覆盖 Text 路由）；联动是产品选择非优点；走时不得被天气阻塞；每秒需轻量路径。补证：写原生 FontSize 踩零写入纪律灰区。
- **R3（复评）**：累乘为中间方案设计风险、非现存缺陷（六段方案下该问题消失，仅留「基准快照优于活读」卫生习惯）；走时线程响应分发须走 v63 独立原子模板；text-only=不拆挂而非不布局（XAML 自动度量承接大半）；历史注释不当作当前验证结果（Time/Date 隐藏拉锯、tooltip 绑定均降级为待验证假设）；「纯粹负资产」措辞纠偏——跳过中间方案的充分理由是重复实现+恢复负担。

## 7. 边界

- 本方案不触碰 AGENTS.md 结构性禁区（跨进程 owner / AttachThreadInput / 失焦隐藏菜单 / 退出硬退路径）。
- 「零写入」纪律在原生 Visibility 上让位于「同向写入」（隐藏方向与系统一致），且必须进快照恢复。
