# 方案：注入式任务栏时钟（第三代架构）v2

> **v2 修订说明（2026-09-12）**：本版按 `REVIEW-第三代注入时钟路线与方案评审-2026-09-12.md` 重写。v1→v2 关键修正：①加载模型改为**外部诊断会话 attach（TAP 模式）**——`InitializeXamlDiagnosticsEx` 本身就是注入通道（接收目标 PID+TAP DLL+CLSID），废弃「远程线程注入+进程内再调初始化」的双路径设计；②`/MT`+`DisableThreadLibraryCalls` 冲突修正；③**卸载序列完整化**（分期验收，PoC 阶段允许模块暂留）；④长条布局从「自动撑宽假设」改为**显式宽度管理职责 + 提前验证**（Windhawk mod 源码 `MaxWidth` 处理为反证）；⑤输入/tooltip 验证提前（C 阶段准入）；⑥天气迁移工作量修正（前端 JSONP 实现，后端无 HTTP 基建，**后置 D 阶段**）；⑦阶段计划改为 A~F；⑧协议加固（版本/会话/序号/上限/重连）；⑨归因措辞收窄（ElevenClock=成功样本而非根因定案）。
> 本文档取代 v1（v1 在 git 历史 `efd2c9f`）。状态：**待批准后从 A 阶段开工**。

---

## 0. 统一决策声明（唯一生效结论）

> 横向五段需求使原生 XAML 改造成为首选验证路线。先完成诊断通道、可逆生命周期、静态长条和输入验证；未通过前保留旧实现但互斥运行。注入路径预期消除覆盖层特有的同步职责（采样仿背景/盖原生文字/跨窗口跟踪），同时引入 Explorer 内生命周期与系统版本适配责任。正式切换由 PoC 证据决定，不以旧方案「绝对无解」为前提。

前置事实链（详见 `对照与取证-ElevenClock稳定性与优效日历机制-2026-09-12.md`）：ElevenClock 同机对照稳定（12 次全屏进出零持续消失——**成功样本，不断层根因**；测试期无新 0x1cc，TDR 条件未复现）；优效日历=注入改造原生时钟 XAML（模块/渲染归属/符号三重实锤，但**不证明其每个设计细节可直接复制**）；原始需求=横向长条（PLAN §1.1）。

**本轮补充查证（2026-09-12 晚，写 v2 前）**：
- 本机 SDK `xamlom.h`（10.0.26100）确认 `InitializeXamlDiagnosticsEx(endPointName, pid, wszDllXamlDiagnostics, wszTAPDllName, tapClsid, wszInitializationData)` 签名与 `IVisualTreeServiceCallback2` 声明；
- 引擎 DLL `xamldiagnostics.dll` **不在系统目录**，位于 SDK：`D:\environment\WindowsKits\10\bin\x64\XamlDiagnostics\xamldiagnostics.dll`（379KB）——**部署依赖**：应用需按 SDK 再分发条款随包携带或定位（A 阶段验证其必需性）；
- Windhawk mod 源码含 `MaxWidth` 设置/`stackPanel.MaxWidth()`/清理 `FrameworkElement::MaxWidthProperty()`——时钟容器宽度受约束是**成熟实现的既证事实**，不是猜想。

## 1. 目标与非目标

**目标**：
1. 任务栏时钟区为横向长条：天气 | 节日 | 节气 | 农历 | 时间，各段独立样式、独立点击；
2. 显示走 explorer 原生 XAML 合成管线——材质、z 序、全屏行为、DPI 原生；
3. 数据链全 Rust（tyme4rs 农历/节气/节日；天气后置）；时钟条不依赖 WebView；
4. 左键弹松鼠日历窗口、右键弹既有原生菜单（行为等价性须经验证，不假定）；
5. 生命周期完整：可逆恢复（不依赖重启 explorer）、宿主死亡自恢复、explorer 重启重注入、可彻底退出。

**非目标**：Win10 经典任务栏；副显示器任务栏；本方案不承诺「所有旧问题消失」——旧故障根因未完全收敛，切换理由是功能适配+可靠性预期，验收以 A~F 证据为准。

## 2. 总体架构（TAP 模式）

```
┌────────────── liCalendar.exe（Rust host）──────────────┐
│ clockbar/ 会话管理器                                     │
│  ├ session.rs：LoadLibrary(Windows.UI.Xaml.dll) →        │
│  │   GetProcAddress(InitializeXamlDiagnosticsEx) →       │
│  │   attach(explorer PID, TAP dll 路径, CLSID)           │
│  ├ pipe.rs：\\.\pipe\lical-clockbar-vN（行 JSON 协议）    │
│  ├ watch.rs：explorer PID 监视（重注入）+ 树重建事件      │
│  └ data.rs：tyme4rs（时间/农历/节气/节日）→ 分期接天气    │
└───────────────┬─────────────────────────────────────────┘
                │ ①系统注入 TAP DLL 并按 CLSID 激活
                │ ②SetSite(IXamlDiagnostics*)
                │ ③named pipe 双向
                ▼
┌────────────── explorer.exe ────────────────────────────┐
│ lical_clock_tap.dll（C++/WinRT，最小代码面）              │
│  ├ DllGetClassObject → ClassFactory → TAPObject          │
│  │   (IObjectWithSite/IUnknown，无注册表依赖)             │
│  ├ SetSite：持 IXamlDiagnostics → 订阅视觉树回调          │
│  ├ 元素跟踪（代次令牌管理树重建）                          │
│  ├ 五段面板 + 显式宽度策略（上限/截断/段隐藏优先级）        │
│  ├ DispatcherTimer 读系统时钟（不累积 tick）               │
│  ├ Tapped/RightTapped → 回调内仅投递有界异步消息           │
│  └ 恢复器：原始值快照 → 精确撤销（分期：界面恢复/活动停止/  │
│     引用释放/模块卸载）                                    │
└─────────────────────────────────────────────────────────┘
```

## 3. 技术决策

| # | 决策 | 依据与修正 | 备选 |
|---|---|---|---|
| D1 | **加载 = 外部诊断会话 attach（TAP 模式）**：host 调 `InitializeXamlDiagnosticsEx`，系统负责把 TAP DLL 注入 explorer 并激活 COM 对象 | API 官方语义即「调试工具入口」（接收 pid+TAP dll+CLSID）；无远程线程=杀软面更小；TAP 对象经 `SetSite` 拿 `IXamlDiagnostics` | 若对 explorer attach 被拒（A 阶段实测）：CreateRemoteThread+LoadLibraryW 注入后进程内调同一 API；再不行：SetWindowsHookEx（优效 SystemClockHook 同款） |
| D2 | **XAML 访问 = IXamlDiagnostics/IVisualTreeServiceCallback2** 枚举+跟踪 | SDK 公开头文件；无符号依赖（**潜在**优点——仍依赖树结构/系统行为，不宣称跨版本韧性最好） | 符号 hook（Windhawk 式，需 PDB 基建，版本适配税重） |
| D3 | **内容 = 隐藏原生 TextBlock + 插入自建面板，宽度为显式管理职责**：定义最大宽度、截断规则、段隐藏优先级（时间>农历>节气>节日>天气），逐项实测祖先容器约束 | Windhawk mod 手工管理 MaxWidth 的既证事实；「自动撑宽」不作假设 | 功能降级：仅改原生 TextBlock 文字（定位成功但面板插入失败时；不满足五段需求，仅保底时间+农历一行） |
| D4 | **DLL = C++/WinRT，/MT 静态 CRT，DllMain 仅返回 TRUE**（**不调用 DisableThreadLibraryCalls**——静态 CRT 依赖线程通知，微软文档明确冲突）；不承诺「C++ 天然安全」：全入口异常包裹只是必要非充分条件，纪律见 §6 | 崩溃责任最小化；TAP 模式下 DllMain 无业务 | — |
| D5 | **数据 = 分期**：先时间+农历/节气/节日（tyme4rs，**锁版本**+数据覆盖验证：闰月/节气时刻/假日数据年份，与前端 lunar-typescript 抽查对照）；**天气后置 D 阶段**（现状为前端动态 script 读 window 变量+localStorage——Rust 侧为新实现：拉取 weather.com.cn 的 JS 赋值脚本文本并解析，含城市配置与缓存迁移） | 评审 §4 指正工作量误判 | — |
| D6 | **输入 = XAML 事件直连，回调内仅做「拷贝参数→投递有界队列→返回」**，绝不同步等 pipe/host；菜单/弹窗由 host 在自己进程执行（跨进程前台获取沿用既有 ALT 解锁方案，**等价性 C 阶段验证**：路由事件父级处理、系统日历飞出面板是否同开、tooltip 行为、键盘/触摸） | 评审 P0-5；优效存在 `RestoreSuppressedClockToolTips`——tooltip 对抗**不作自然消失假设** | — |
| D7 | **恢复语义 = 原始值快照，精确撤销**：修改前记录 Visibility/局部值/绑定/容器属性的当前值；只撤销仍属于自己的改动（代次令牌校验），不覆盖系统后续更新 | 评审 §4 | — |
| D8 | **卸载 = 完整序列**：停止接收新工作→作废旧代回调→停定时器/解绑事件/退订诊断→UI 线程移除面板并恢复属性→排空在途回调→释放引用与线程→满足条件才 FreeLibrary；**分期验收**：PoC 先证「界面已恢复+活动已停+模块暂留无害」，再单独证安全卸载 | `FreeLibraryAndExitThread` 只减引用+退线程，不清理对象 | — |

**A 阶段待实测的诚实未知清单**（文档语焉不详处，不预设）：
1. `endPointName` 的语义与格式（trace 端点串）；2. attach explorer 是否需要额外条件（完整性级/会话）；3. `wszDllXamlDiagnostics` 指向 SDK 的 `xamldiagnostics.dll` 是否必需、能否随应用分发（SDK 再分发条款核对）；4. CLSID 是否需要注册表（参数显式传 CLSID+DLL 路径，预期走 `LoadLibrary+DllGetClassObject` 免注册，待证）；5. 回调中定位时钟元素的字段判别式（TypeName/Name/AutomationId 实测值）；6. explorer（非 UWP 宿主）的 XAML 初始化时机。

## 4. 模块设计

### 4.1 会话管理器（Rust，`src-tauri/src/clockbar/`）

- `session.rs`：attach/detach；detach 走协议 `restore`（非强杀）；attach 幂等（模块枚举防重复）；**同一 explorer PID 内树重建**也触发会话级重置（DLL 报 `tree_rebuilt`）；
- `watch.rs`：explorer PID 监视（`GetShellWindow` 归属进程轮询+WinEvent 双保险）；PID 变化=重启→延时 3s 重 attach；
- `pipe.rs`：服务端；DACL 限当前用户会话；客户端身份校验（首条消息带进程签名：PID+映像路径回验）；
- `data.rs`：tyme4rs 计算（日界/节气时刻触发刷新）；时段格式下发；数据带序号防乱序。

### 4.2 TAP DLL（`src-tauri/clockbar-tap/`，产物 `lical_clock_tap.dll`）

- **COM 入口**：`DllGetClassObject`→`IClassFactory`→TAPObject（`IObjectWithSite`+`IVisualTreeServiceCallback2`）；`SetSite` 保存 `IXamlDiagnostics`、发 `ready`（含义=「面板可用」，加载成功与面板可用是两件事，协议区分 `loaded`/`ready`）；
- **元素跟踪+代次**：每次树重建递增 generation；所有回调/定时器/事件闭包持代次令牌，执行前校验——旧代回调一律丢弃；识别失败→静默+`track_fail`（原生时钟兜底，不重试）；
- **面板**：宽度策略按 D3；样式由 host 下发；时间段秒级/分钟级由配置（秒级走本地 `DispatcherTimer`，**每次读系统时钟**，处理休眠唤醒/时区调整/日界）；
- **恢复器**：D7 快照语义；host 心跳丢失（pipe 断开即触发，另加单调时钟 30s 兜底；pipe 读在工作线程、永不阻塞 UI）→ 恢复序列 D8；
- **协议解析**：DLL 内也解析（评审 §4：不能写「解析全在 host」）——行缓冲上限 8KB、深度/字段数限制、任何解析异常丢弃该行并计数上报；
- **日志**：环形缓冲+落 `D:\agents_tmp\clockbar_tap.log`（与现有 dbg_log 体系并行）。

### 4.3 协议 v2（行 JSON，UTF-8）

```
host→tap  {"v":2,"t":"init","session":"<uuid>","style":{segments:[...]},"timeFmt":"HH:mm","showSeconds":false}
host→tap  {"v":2,"t":"data","seq":N,"segments":{"lunar":"七月十四","term":"白露","festival":"中秋","weather":"..."}}
host→tap  {"v":2,"t":"restore"} / {"t":"ping"}
tap→host  {"v":2,"t":"loaded","winver":"26200","gen":1}          // COM 激活+订阅成功（≠面板可用）
tap→host  {"v":2,"t":"ready","gen":1}                             // 元素定位+面板构建成功
tap→host  {"v":2,"t":"tree_rebuilt","gen":N} / {"t":"track_fail","gen":N,"reason":"..."}
tap→host  {"v":2,"t":"tap","segment":"time","button":"left|right","x":..,"y":..}
tap→host  {"v":2,"t":"restored","gen":N} / {"t":"pong","seq":N} / {"t":"parse_err","count":N}
```
断线重连：tap 侧 pipe 断开→启动恢复；host 侧断开→等待 watch 重 attach。消息超限/版本不符→丢弃+上报。

## 5. 阶段计划（A~F，每阶段有继续条件）

| 阶段 | 内容 | 验收门槛（不达标不进下一阶段） | 估时 |
|---|---|---|---|
| **A 只读通道** | TAP attach/激活/订阅/退订；树枚举日志（候选元素+字段值）；**无竞争基线**：记录当前已驻留的任务栏修改组件（优效 hook 仍在 explorer！），本阶段不与其并发改造 | 不改变任务栏可反复建立/结束会话 ≥20 次、无重复订阅、无 explorer 异常；§3 未知清单 6 项有实测答案；与优效驻留共存观察无冲突 | 1~1.5 天 |
| **B 可逆修改** | 改一次 Text（含记录原始值）；恢复验证；host 退出/被杀两路径；树重建（全屏切换触发）代次重置 | 恢复不依赖重启 explorer；连续全屏进出 ×30（矩形验证法）无残留改动、无重复插入；系统自身刷新/主题切换/模板重建后我们的修改仍按代次正确管理（不被覆盖后反复抢写） | 1 天 |
| **C 静态五段+输入** | 假数据面板；**宽度约束实测**（175% DPI/低分辨率/托盘图标增多/最长天气文案/自动隐藏任务栏/主题切换）；截断与段隐藏优先级；输入/tooltip 等价性 | 长条在全部约束场景可读不裁切（超限按优先级截断，时间段永不丢）；托盘图标避让、「显示桌面」条不丢；左键只开我们的日历、**系统日历飞出面板不同开**；右键菜单行为与现状等价（前台/时序/tooltip——逐项对照 AGENTS 既有验收清单）；键盘/触摸至少不破坏 | 1~1.5 天 |
| **D 数据接入** | tyme4rs（锁版本+覆盖验证）→ 时间/农历/节气/节日；然后天气（新实现） | 数据抽查 20 个敏感日期与 lunar-typescript 一致（闰月/节气日/除夕/调休）；断网/坏数据→时钟段不受影响、explorer 无感知 | 1 天 |
| **E 完整生命周期** | explorer 重启重注入（×5）；宿主崩溃自恢复（30s 内）；正常退出全清理（含注册表时间格式恢复）；安全卸载证明（D8 终点）；长期运行（隔夜）；**互斥开关**接入（注入时钟与旧覆盖层二选一，不同时操作） | 全部场景可重复通过；卸载后模块列表无残留（此条从 M0 移到 E，分期完成） | 1 天 |
| **F 正式切换** | 真实使用观察期（用户日常，含 PotPlayer 全屏习惯负载）→ 稳定后退役清理 PR（§7 清单） | 用户验收（复用 30 次全屏压力 + 日常数天） | 0.5 天+观察期 |

**与 v1 的排序差异**：长条布局、输入等价性、恢复可靠性全部前移为 C/B 准入条件；旧代码删除从「M2 后」推迟到 F；旧路径在 E 前仅用互斥开关停用不删除。

**旧看门狗两缺陷**：不作为本方案前置（所属模块将退役）；若观察期用户仍日常使用旧版，再按需修（复验期间双版本并存由互斥开关管理）。

## 6. 安全与稳健纪律

1. **explorer 崩溃责任**：DLL 只做「枚举-改属性-建面板-投事件」；解析/计算/网络全在 host（DLL 内仅限界解析）；全入口 try/catch(...)；任何异常→恢复+躺平（不重试）；C++/WinRT lambda 全部经代次校验包装；**绝不持有任何跨调用裸指针假设**（元素引用一律 winrt strong/weak ref）；
2. **UI 线程纪律**：XAML 只在 dispatcher 线程碰；事件回调只投递不等待；pipe 在工作线程、非阻塞读；
3. **版本适配**：判别式集中在元素识别一处；失配=静默+原生兜底+`track_fail` 上报；D2 备选路线保留；
4. **杀软**：TAP 模式无远程线程、无全局钩子，预期面最小；Defender 实测+白名单；分发再评估签名；
5. **共存**：A 阶段建立基线前不与优效驻留 DLL 并发改树；若用户需同时开优效，明确记录其 hook 在场时的行为后再定共存策略；
6. **多用户**：pipe DACL 限当前用户会话；注入仅定向 explorer 当前会话 PID。

## 7. 与现有代码的关系

**E 阶段前**：旧覆盖层保留但受互斥开关停用（同一时间只有一套操作任务栏时钟）。
**F 阶段删除**（一次性 PR）：`clock_overlay.rs` 几何状态机/遮盖/采样色彩链/原生遮盖条/z 自愈/TDR 看门狗（R4~R9.4 机制）；`ClockOverlayWindow.tsx`；钩子时钟区路由/tooltip 压制/悬停守护分支；注册表时间格式写入（Phase 3 退役）。
**始终保留**：`clock_context_menu.rs`（触发源改为 pipe tap 事件）；钩子框架（桌面日历等职责，摘时钟分支）；主日历窗口/设置/桌面日历；前端 TS 侧不动。
**新增**：`src-tauri/src/clockbar/`（Rust）+ `src-tauri/clockbar-tap/`（C++ DLL）+ 构建接入（MSVC 环境）+ SDK `xamldiagnostics.dll` 部署项（条款 A 阶段核对）。

## 8. 许可与合规

Windhawk mod（GPL v3）只读参考不复制（其符号 hook 路线与本文 XamlDiagnostics 主路线本就不同）；优效为闭源商用同技术先例；XamlDiagnostics 为 SDK 公开调试接口——「公开 API 的工具化用途」，微软未承诺其为扩展点，属可接受灰色（商用先例充分）；注入目标为用户本人会话的 explorer，无未授权访问。

## 9. 风险登记

| 风险 | 等级 | 缓解 |
|---|---|---|
| DLL 缺陷致 explorer 崩溃 | 高 | §6 纪律全套；B~E 每阶段含压力回归；躺平策略 |
| 外部 attach 对 explorer 不适用（未知清单 2/3/4 项不利） | 中 | A 阶段第一时间实测；两条备选通道就位 |
| 长条在真实约束下不可用（祖先容器硬约束） | 中 | C 阶段准入即答案；降级形态（D3 备选）预先定义 |
| 输入/菜单/tooltip 等价性不成立 | 中 | C 阶段逐项对照 AGENTS 既有验收；失败项修复后才进 D |
| Windows 更新破坏元素识别 | 中 | 判别式集中+静默兜底+`track_fail` 遥测（本地日志） |
| 杀软误报 | 中 | TAP 模式最小面；白名单；后续签名 |
| tyme4rs 数据与前端不一致/假日覆盖不足 | 低 | D 阶段锁版本+抽查验收，不足则补数据表 |
| 与优效驻留 DLL 冲突 | 低 | A 阶段基线；不并发改树 |

## 10. 估时（PoC 预算口径，非交付承诺）

A 1~1.5 天 / B 1 天 / C 1~1.5 天 / D 1 天 / E 1 天 / F 0.5 天+观察期 → **合计 5.5~7 天**（v1 的 4~5 天为乐观值，按评审修正）。最有价值的早期产出=A~C 的成立性与失败原因，而非完整产品。

## 11. 用户决策记录（2026-09-12 已确认）

1. ~~批准 A 阶段开工~~ → **暂不开启 A 阶段**（用户尚有细节待确认，确认后开工）；
2. 段顺序默认「天气|节日|节气|农历|时间」（C 阶段后做成可配置）；
3. 时间段**不显示秒**（分钟级刷新）；
4. 注入随自启**做成设置开关，默认开启**（不跟随应用自启硬绑定——应用自启但用户可关注入开关，关闭时仅显示原生时钟）；
5. 优效日历**保留安装**：日常可用；**B 阶段起的测试时段必须关闭其进程**（其 hook DLL 驻留 explorer 至 explorer 重启——测试基线需在关闭优效后重启一次 explorer 建立）；「与优效共存」从低风险升级为**待验证项**（PoC 范围外，正式使用期观察）。
