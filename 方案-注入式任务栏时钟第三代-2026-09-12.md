# 方案：注入式任务栏时钟（第三代架构）

> 状态：**设计稿 v1（2026-09-12），未实施**。前置结论见 `对照与取证-ElevenClock稳定性与优效日历机制-2026-09-12.md`：ElevenClock 同机对照稳定（推翻「环境级无解」）、优效日历 = 注入改造原生时钟 XAML（符号级取证）、原始需求 = 横向长条（PLAN §1.1）。
> 参考资料源：
> - Windhawk `taskbar-clock-customization` mod 源码 v1.8（5985 行，GPL v3，已存 `D:\agents_tmp\windhawk-taskbar-clock-customization.wh.cpp`——**只作机制参考，不抄代码**，见 §9 许可）；
> - 优效日历 `win11_hook_x64.dll` 符号骨架（`TrackClockTextBlock`/`ApplyStyleToTrackedTextBlocks`/`RunOnUiElementDispatcher`/`YXCalendarTaskbarTap`/`InitializeXamlDiagnosticsEx`/`RequestWin11HookUnload`）。
> 一手源码调研结论（本文多处引用）：Windhawk mod 的机制 = 符号 hook（微软公共 PDB）`SystemTray::implementation::DateTimeIconContent::OnApplyTemplate` 等 → `QueryInterface(FrameworkElement)` → `VisualTreeHelper` 遍历找 `ContainerGrid` 内 TextBlock → 改 Text/样式；优效的机制 = XamlDiagnostics 枚举（无符号依赖）+ 元素跟踪 + 样式快照应用。

---

## 1. 目标与非目标

**目标**：
1. 任务栏时钟区为**横向长条**：天气 | 节日 | 节气 | 农历 | 时间，从左到右，各段独立样式、独立点击行为；
2. 显示走 explorer 原生 XAML 合成管线——材质、z 序、全屏行为、DPI 全部原生，**R1~R9.4 的整组问题结构性消失**（无采样/无遮盖/无跟踪/无 WebView 呈现风险）；
3. 数据链全 Rust（农历/节气/节日/天气），不经 WebView，宿主侧可单测；
4. 点按时钟弹松鼠日历窗口、右键弹既有原生菜单——交互行为与现状对齐；
5. 生命周期完整：应用退出/崩溃后任务栏恢复原生时钟；explorer 重启后自动恢复注入；可彻底卸载。

**非目标**：
- Win10 经典任务栏支持（本机 26200；如需可后续加 SystemClockHook 式子类化路径，本期不做）；
- 副显示器任务栏时钟（Win11 默认仅主任务栏显示时钟，跟随系统行为）；
- 保持覆盖式覆盖层运行（该路线整体退役，见 §8）。

## 2. 总体架构

```
┌──────────────── liCalendar.exe（Rust host，既有进程）────────────────┐
│                                                                      │
│  ┌──────────┐   ┌──────────────┐   ┌─────────────────────────────┐  │
│  │ 数据服务  │→│ pipe server   │   │ 注入宿主 injector            │  │
│  │ tyme4rs + │  │ \\.\pipe\     │   │ explorer PID 监视 + 启停/重注入│  │
│  │ reqwest   │  │ lical-clockbar│   │ (CreateRemoteThread 风格)    │  │
│  └──────────┘   └──────┬───────┘   └──────────────┬──────────────┘  │
└────────────────────────┼──────────────────────────┼─────────────────┘
                         │ named pipe（双向，JSON 行协议）
                         ▼                          ▼
┌──────────────── explorer.exe（x64）────────────────────────────────┐
│  lical_clock_hook.dll（新建，C++/WinRT，最小代码面）                  │
│   ├ bootstrap 线程：InitializeXamlDiagnosticsEx → IXamlDiagnostics  │
│   ├ IVisualTreeServiceCallback2 枚举/跟踪时钟 TextBlock（按类型/名） │
│   ├ 面板构建：隐藏原生两 TextBlock → 插入 StackPanel×5 段            │
│   ├ DispatcherTimer 秒级走字（本地，不经 IPC）                       │
│   ├ Tapped/RightTapped → pipe → host（复用原生菜单/日历窗口）        │
│   └ 降级/恢复：宿主心跳丢失 → 恢复原生 TextBlock → 自卸载            │
└─────────────────────────────────────────────────────────────────────┘
```

四个部分：**注入宿主**（Rust，在现有 liCalendar 进程内）、**注入 DLL**（新建 C++ 工程）、**数据服务**（Rust）、**协议**（命名管道）。

## 3. 关键技术决策

| # | 决策 | 理由 | 备选（失败时切换） |
|---|---|---|---|
| D1 | **注入通道 = 远程线程 + LoadLibraryW**（对 explorer 指定 PID，用户会话内中完整性级，无权限障碍） | 最小依赖、时点可控（应用启动时注入）；Windhawk 自身的 mod 加载同风格 | SetWindowsHookEx（目标线程=explorer UI 线程，优效 SystemClockHook 组件同款）；XamlDiagnostics 注册表 autoload（**未验证是否存在**，PoC 顺带查） |
| D2 | **XAML 访问 = XamlDiagnostics 路线**：DLL 内调 `InitializeXamlDiagnosticsEx`（Windows.UI.Xaml.dll 导出，SDK 头 `xamlom.h` 有 `IXamlDiagnostics`/`IVisualTreeServiceCallback2` 声明）→ 枚举元素树回调 → `GetIInspectableFromHandle` 拿真实对象 `.as<TextBlock>()` | **无符号依赖**——不依赖微软 PDB 与内部修饰名，跨小版本更新韧性最好；优效同路线（`InitializeXamlDiagnosticsEx` 符号实锤）；比 Windhawk 的符号 hook 维护面小 | 符号 hook 路线（Windhawk 式：msdl 下载 PDB + 修饰名 hook `DateTimeIconContent::OnApplyTemplate`——精确但每版要跟） |
| D3 | **内容形态 = 隐藏原生两 TextBlock（Visibility::Collapsed）+ 同容器插入自建面板**（水平 StackPanel：天气 TextBlock+FontIcon / 节日 / 节气 / 农历 / 时间） | 长条五段的完全体；布局引擎自动撑宽、托盘图标自动避让（XAML 布局参与是注入路线的核心红利）；原生元素保留在树上=恢复=把 Visibility 切回 | 降级模式：只改原生 TextBlock 的 Text（Windhawk mod 层面，宽度自动增长但不支持分段样式/分段点击）——作为 M2 失败时的保底形态 |
| D4 | **DLL 语言 = C++/WinRT**，静态链接 CRT（/MT），x64-only | explorer 内代码崩溃=任务栏崩溃，选最小运行时与最成熟 WinRT interop；C++/WinRT 头在 Windows SDK；优效/Windhawk 同选型；避免 Rust panic（abort 即杀 explorer）与 CRT/WinRT 混链风险 | Rust（windows-rs 可行但 panic/ unwind 策略、异常跨 COM 边界都需额外防御，收益低） |
| D5 | **数据链 = 全 Rust**：`tyme4rs`（6tail，与前端 lunar-typescript 同作者后继版，农历/节气/节日/法定假日全支持）+ 既有 HTTP 依赖取天气 | 时钟条数据不再依赖任何 WebView；可单测；主日历窗口 TS 侧不动 | 保底：无头 WebView 计算再喂后端（ reintroduces WebView 到关键路径，仅作 D5 不可行时的退路） |
| D6 | **输入 = XAML 事件直连**：每段 Tapped/RightTapped → pipe → host 执行既有动作（左键开日历窗、右键弹 `clock_context_menu` 原生菜单） | 比现在 LL 钩子吞键路由干净一个量级：不吞系统输入、无 tooltip 对抗（tooltip 由我们的元素自己定义）、无重弹机制；`YXCalendarTaskbarTap` 证明 COM/事件路由可行 | — |

**D2 的诚实标注**：`InitializeXamlDiagnosticsEx` 的准确签名/调用时机（须在 explorer 进程内、XAML 已初始化后调用）、`IVisualTreeServiceCallback2` 回调里定位时钟元素的字段（TypeName/Name/AutomationId 具体取值）——文档语焉不详处以 PoC M1 实测为准，方案按该路线设计但不预设细节。

## 4. 模块设计

### 4.1 注入宿主（Rust，`src-tauri/src/clockbar/`）

- `injector.rs`：`OpenProcess` → `VirtualAllocEx`+`WriteProcessMemory`（DLL 路径）→ `CreateRemoteThread(LoadLibraryW)` → 等待句柄确认加载；DLL 路径放安装目录。注入前后用模块枚举校验（防重复注入）；
- `watch.rs`：explorer PID 监视（WinEvent `EVENT_OBJECT_DESTROY`/轮询 `GetShellWindow` 归属进程双保险），PID 变化=explorer 重启 → 延时 3s 重注入（等任务栏 XAML 树重建）；
- `pipe.rs`：`\\.\pipe\lical-clockbar` 服务端（多客户端实例名，实例=PID 后缀防串扰），行协议见 §5；
- 生命周期挂钩：应用正常退出→发 `restore+unload`；应用崩溃→DLL 侧心跳超时自恢复（§4.4）；开机自启动沿用现状。

### 4.2 注入 DLL（C++ 工程 `src-tauri/clockbar-hook/`，产物 `lical_clock_hook.dll`）

- **bootstrap**（`DllMain` 仅 `DisableThreadLibraryCalls` + 起线程，其余全部在线程内做）：连 pipe → `RoInitialize` → `InitializeXamlDiagnosticsEx` → 注册 `IVisualTreeServiceCallback2`；
- **元素跟踪**（优效 `TrackClockTextBlock` 同型）：回调里按元素类型（TextBlock）+ 祖先链特征（时钟容器 DateTimeIconContent/ContainerGrid——M1 实测确定判别式）识别两枚原生时钟 TextBlock；树重排（全屏切换/分辨率变化）触发回调重认；
- **面板构建**：在时钟容器内 `StackPanel(Orientation=Horizontal)`，五段各为 `StackPanel`（图标+文字纵向）或纯 `TextBlock`，样式（字体/字号/颜色/间距）由 host 下发；原生两 TextBlock `Collapsed`；
- **秒级走字**：`DispatcherTimer` 本地更新时间段（不经 IPC），其余段收 host 推送时更新；全部 XAML 操作经 `RunOnUiElementDispatcher`（优效同型）——**只在 dispatcher 线程碰 XAML**；
- **输入**：每段挂 `Tapped`/`RightTapped`，事件里只做一件事：`pipe.send({tap: segment})`；菜单/弹窗全部由 host 侧既有代码执行；
- **异常边界**：所有入口 `try/catch(...)` 包裹（含 dispatcher 回调/lamba），任何异常→恢复原生 TextBlock + 停止一切操作 + 日志（不重试——explorer 里宁可躺平不可挣扎）；
- **自卸载**：`restore` 消息或心跳超时（host 5s 间隔，30s 无心跳）→ 恢复 → `FreeLibraryAndExitThread`（优效 `RequestWin11HookUnload` 同型；强杀 host 不清理的教训见优效取证）。

### 4.3 数据服务（Rust，`src-tauri/src/clockbar/data.rs`）

- 时间段：DLL 本地生成（格式串 host 下发，如 `HH:mm`）；
- 农历/节气/节日：`tyme4rs` 计算，日界（00:00）与节气时刻自动刷新；
- 天气：沿用现有后端 HTTP 基建拉取（每 30min），文案=「图标代号+温度+摘要」；
- 节日优先级：法定假日在前，其次传统节日，无节日时显示次日的「X 休/X 班」提示（调休提醒，数据 tyme4rs 有）；
- 输出聚合为 JSON 行推 pipe；段内容与顺序可配置（设置项，后续接设置 UI）。

### 4.4 生命周期状态机

| 场景 | 行为 |
|---|---|
| 应用启动 | injector 注入 → DLL 连 pipe → host 下发样式+首帧数据 → 面板出现 |
| 应用正常退出 | host 发 restore+unload → DLL 恢复原生时钟 → 自卸载 →（现有流程）恢复注册表时间格式（Phase 3 退役项一并执行） |
| 应用崩溃/被强杀 | DLL 心跳超时 30s → 恢复原生时钟 → 自卸载；下次启动应用重新注入 |
| explorer 重启 | watch.rs 检测新 PID → 重注入；期间原生时钟照常（天然安全网） |
| Windows 更新改 XAML 树 | 元素跟踪失败/判别式失配 → DLL 保持静默（原生时钟原样）+ 日志；发布适配补丁 |
| DLL 自身异常 | 恢复原生 + 躺平（§4.2 异常边界） |

## 5. 管道协议（行分隔 JSON，UTF-8）

```
host→dll  {"t":"style", segments:[{id,color,fontSize,fontWeight,gap,...}]}   // 注入成功后一次
host→dll  {"t":"data",  segments:{weather:"☁ 28° 小雨", festival:"中秋", term:"白露", lunar:"七月十四", timeFmt:"HH:mm"}}
host→dll  {"t":"restore"}                                                     // 恢复并卸载
host→dll  {"t":"ping"}                                                        // 5s 心跳
dll→host  {"t":"ready","winver":"26200"}                                      // XamlDiagnostics 初始化+元素定位成功
dll→host  {"t":"tap","segment":"time","button":"left"|"right","x":..,"y":..} // 点击路由
dll→host  {"t":"track_fail"}                                                  // 元素定位失败（进入静默）
dll→host  {"t":"pong"}
```

时间字串 DLL 本地渲染（秒级不走 IPC）；其余段变化才推（天气 30min 级、农历日界级）。

## 6. 安全与稳健

1. **explorer 崩溃责任**是最大风险面。对策：DLL 只做「枚举-改属性-建面板-转事件」，解析/计算/网络全在 host；C++ 最小面 + 静态 CRT；全入口异常包裹；绝不重试自愈（躺平策略）；发布前用本机全屏压力回归（§7 M4）+ explorer 重启循环测试连跑验证；
2. **杀软误报**：无签名 DLL 注入 explorer 可能被行为引擎标记（Windhawk/优效同样面对）。对策：个人使用先实测 Defender 白名单；将来如分发再评估签名。诚实标注：这是路线固有成本；
3. **版本适配**：判别式集中在「元素识别」一处（§4.2），不散落；树结构变化表现为「静默+原生时钟」而非崩溃；符号 hook 备选路线保留为 D2-Fallback；
4. **性能**：dispatcher 上只做属性赋值；秒级一次 Text 赋值与系统时钟原生刷新同量级；无像素采样、无轮询；
5. **多用户/权限**：仅当前用户会话、仅 explorer PID 定向注入，绝不全局。

## 7. PoC 里程碑与验收

> 方法论沿用本轮对照实验：**每步用可观测证据验收，不做无验证推进**；全屏压力测试复用 `eleven_stress3/4.ps1` 的「矩形验证 + frame_capture」套路。

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0 通道打通** | host 注入 DLL 进 explorer；DLL 连 pipe 打日志；`restore` 干净卸载 | 注入后任务栏无任何可见变化、无卡顿；卸载后 explorer 模块列表无残留；连做 20 次注入/卸载循环 explorer 存活 |
| **M1 元素定位** | XamlDiagnostics 枚举，日志输出树中候选元素（类型/名/AutomationId），确定时钟 TextBlock 判别式；改一次 Text（追加「测试」二字）持续 10min | 判别式在全屏进出×10、explorer 重启后仍稳定命中；改动只在时钟上、恢复无痕 |
| **M2 长条面板** | D3 完全体：五段面板、自动撑宽、分段样式；先静态假数据 | 托盘图标自动避让不重叠；「显示桌面」条不被挤丢；全屏进出×30（矩形验证）时钟无消失/无错位/无色差（frame_capture 对比基线）；explorer 重启后面板回来 |
| **M3 数据与交互** | tyme4rs 数据链 + 秒走字 + 左键开日历窗 + 右键弹既有原生菜单 | 数据正确性对照前端 lunar-typescript 输出抽查 20 个日期（含节气日/除夕/闰月）；点击/右键行为与现状一致；菜单期间无 tooltip 对抗问题（原生事件，预期自然消失） |
| **M4 生命周期加固** | explorer 重启重注入、host 崩溃自恢复、正常退出全清理（含注册表时间格式恢复）、应用开机自启后自动注入 | 模拟杀 host→30s 内任务栏恢复原生；杀 explorer×5→每次 10s 内长条回来；正常退出→任务栏与注册表完全回原样；完整 30 次全屏进出回归零异常 |

M0/M1 失败的退路：M0 失败→改 SetWindowsHookEx 通道；M1 失败→改符号 hook 路线（需加 PDB 下载基建）或降级 D3 为「改 Text 模式」。

## 8. 与现有代码的关系

**退役**（注入路线 M2 验收通过后删除，一次性 PR）：
- `clock_overlay.rs` 的几何状态机/遮盖/实时跟踪/采样色彩链/原生遮盖条/z 自愈/TDR 看门狗（R4~R9.4 全部机制）；
- `ClockOverlayWindow.tsx`（时钟条不再用 WebView）；
- 钩子的时钟区点击路由/tooltip 压制/菜单悬停守护分支（`OVERLAY_TAKEOVER`、`NATIVE_DISMISS_PENDING`、`move_cursor_to_dismiss_tooltip` 等——原生 XAML 事件替代）；
- 注册表时间格式写入（Phase 3 退役，恢复系统默认格式）。

**保留**：
- `clock_context_menu.rs`（原生右键菜单，改由 pipe tap 事件触发）；
- 鼠标钩子框架（桌面日历 WIN+D 恢复等其他职责保留，仅摘除时钟分支）；
- 主日历窗口/设置/桌面日历/天气 HTTP 基建（前端 TS 侧不动）；
- UIA 探测体系降级为可选诊断（不再承担定位职责）。

**新增**：`src-tauri/src/clockbar/`（injector/watch/pipe/data）+ `src-tauri/clockbar-hook/`（C++ DLL 工程，构建接 MSVC——环境已具备，见项目 AGENTS 构建注意事项）。

## 9. 许可与合规

- Windhawk mod 为 GPL v3：**只读机制参考（符号名/树遍历思路/验收方法），不复制代码**；我们主路线（XamlDiagnostics 枚举）与它（符号 hook）实现路径不同；优效为闭源商用同技术先例；
- XamlDiagnostics 是 Windows SDK 公开头文件（`xamlom.h`）中的诊断接口——属「调试用途的公开 API」灰色但广泛商用先例（优效）；风险=微软未来收紧，D2 备选路线对冲；
- 本方案为用户本机自用软件的功能实现，注入目标是用户自己的 explorer 进程，无未授权访问问题。

## 10. 风险登记

| 风险 | 等级 | 缓解 |
|---|---|---|
| DLL 缺陷致 explorer 崩溃 | 高 | §6.1 全套（最小代码面/异常包裹/躺平策略/M4 压力回归） |
| Windows 更新破坏元素识别 | 中 | 判别式集中一点；失配=静默+原生时钟兜底；备选符号路线 |
| 杀软误报 | 中 | Defender 实测白名单；后续签名 |
| XamlDiagnostics 接口细节与预期不符（签名/时机） | 中 | M1 就是专门验证它的门；备选路线就位 |
| tyme4rs 输出与前端 lunar-typescript 有差异 | 低 | M3 抽查对照验收 |
| 天气/长条内容挤压托盘图标 | 低 | XAML 自动布局 + M2 验收项显式检查 |

## 11. 工作量估算

| 阶段 | 估时 |
|---|---|
| M0+M1（通道与定位，含踩坑预算） | 1~1.5 天 |
| M2（面板） | 1 天 |
| M3（数据与交互） | 1 天 |
| M4（生命周期加固+回归） | 0.5~1 天 |
| 退役清理 PR + 文档 | 0.5 天 |
| **合计** | **4~5 天**（与 GDI 改造估算同量级，但能力上限与可靠性收益完全不同） |

## 12. 决策点（需要用户确认的）

1. **走本方案**（vs 备选：GDI 覆盖层+注册表占位——仅当拒绝 explorer 注入风险面时选）；
2. 长条**段顺序与内容**默认值：本文按「天气|节日|节气|农历|时间」，可在 M3 后用设置调；
3. 时间段是否显示**秒**（显示=秒级刷新走 DLL 本地定时器，不增加 IPC；不显示=分钟级）；
4. 开机自启动注入是否跟随现有应用自启设置（默认：是）。
