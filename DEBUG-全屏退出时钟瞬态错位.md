# 问题档案：全屏退出瞬间时钟位置偏移（瞬态错位）

> 状态：**已缓解未根治** · 2026-09-08 · 待用户下令后继续
> 关联：`PLAN-时钟覆盖式接管.md` Phase 1 追加章节 · 当前代码 commit `406223d`

---

## 1. 问题现象（用户视角）

PotPlayer / Edge 退出视频全屏时：

1. ✅（已解决）时钟**立即出现**——z 序潜入方案生效，无空窗、无延迟；
2. ❌（本档案问题）出现的瞬间**位置偏左约 42px**，右缘露出一条原生时钟残影（能看到 "1:05 / -9-8" 碎片）；
3. 修复前残影持续 **≤2 秒**（等周期重探自愈）；现在压到 **~150ms**（两针延迟治疗），仍可察觉。

## 2. 根本原因（⚠️ 降级为待验证假设——2026-09-08 外部评审+代码复核后修正）

**现象层事实（无争议）**：恢复瞬间覆盖层用了**全屏期间缓存的旧矩形**（left=3618），原生时钟实际在 (3660,2076)-(3818,2160)，右缘露出 42px 条带。

**评审修正（原"根本原因"章节的方向描述写反了）**：数据是 全屏期缓存 3618 < 退出后实际 3660——全屏期间时钟在**左**位、正常态在**右**位，与原稿"全屏右移贴条、退出左移"相反。原稿的方向叙述是无数据支撑的推测，撤销。3618 的**真实出处尚未归因**，以下为代码复核确认的事实与候选：

### 代码复核确认的事实

1. **COM 缺陷（已证实）**：`mouse_hook.rs` 的 2s 周期线程直接调 `update_clock_area_cache()`，该线程**无 `CoInitializeEx`**（COM 按线程计；`refresh_clock_area_cache` 才带初始化）。本机 XAML 任务栏下经典时钟类名不存在 → `find_clock_window` 失败 → `CoCreateInstance` 无 COM 必败 → **周期线程永远走注册表回退**，实为"注册表回声"而非探测器。
2. **缓存写入来源至少四个**（无版本控制，后写覆盖先写）：
   - 周期线程（2s）：无 COM → **注册表回声**；
   - `refresh_clock_rect_if_in_taskbar`（钩子点击路径）：HWND + `GetWindowRect` 直接写缓存，**不持久化**、无注册表兜底；
   - `refresh_clock_area_cache`（治疗线程/attach/Tauri 命令，有 COM）：真 UIA 探测 + 持久化；
   - UIA 的 DPI 逻辑坐标修正路径（`validate_or_scale_clock_rect`）可能产出瞬态修正值。
3. **注册表 ClockRect 不是独立真值**——它就是成功探测的持久化副本（原稿把它列为对照物属循环论证）。

### 候选假设（待埋点归因）

| # | 假设 | 判据 |
|---|---|---|
| H1 | 钩子点击路径（HWND 来源）写入了某个"时钟代表窗口"的过期/错位矩形 | 日志见 hook-hwnd 来源写入 3618 |
| H2 | 全屏期间 UIA 真实探测到全屏布局（时钟确实在 3618），退出后布局恢复 3660，缓存滞后 | 日志见 uia 来源在覆盖期间写 3618 |
| H3 | UIA 逻辑坐标 + DPI 修正的瞬态产物 | 日志见修正前后坐标跳变 |
| H4 | 注册表在某个瞬态被写入了 3618，后经回声进入缓存 | 日志见 registry 来源写 3618 |

**结构约束不变**：UIA 不能进 WinEvent 回调线程（=钩子泵线程）；探测须等退出动画稳定；WebView2 一切隐藏/离屏手段都触发合成暂停（覆盖层只能在场+事后纠位）。

## 3. 已实施的缓解（现状，commit 406223d）

- **触发点**：`update_clock_overlay_visibility` 检测到 z 序状态从「潜入（below>0）」转换为「常规（-1）」的瞬间；
- **动作**：spawn 治疗线程（独立线程，不碰泵线程），两针延迟重探重贴：**+150ms / +600ms**（第一针避开退出动画未稳瞬间，第二针兜底）；
- **防重入**：`EXIT_HEALING` AtomicBool，治疗期间不重复 spawn；
- **实测**：+100ms 仍有残影（第一针未落地），+400ms 完全对齐——残影窗口从 ≤2s → **~150ms**。

- **实测**：+100ms 仍有残影，+400ms 完全对齐。注意名义针点（150/600ms）只是 sleep 起点，UIA 探测耗时另计，实际落针晚于名义值——残影窗口的真实边界在 (100, 400] ms 区间，"~150ms" 为粗略说法。

## 3.5 候选方案（评审后修订版）

| # | 方案 | 思路 | 预期 | 评审要点 |
|---|---|---|---|---|
| A | 加密治疗针（50/150/400/800ms） | 更密追赶 | 残影 ~50-100ms | 治标；数据源/并发问题不解决时可能抖动；50ms 是探测**开始**时间非完成时间 |
| B | 右缘锚定 | correct_right = tray_right − strip_w（最近对齐态缓存），纯算术即位 | 原判"零残影"**需修正**：播放器缩回时覆盖层先于回调自然露出，反应式移动必有一到几帧残影；唯一零残影变体是**潜入期预停预测位**（覆盖层反正被盖着） | 前提"右缘恒贴条"必须先跨多次切换证明；假设失效需显式检测，不会自动无害回退 |
| C | 潜入期间加密重探 | 500ms 重探 | 有限 | 若正确布局只在退出时产生，采得再勤也拿不到未来布局；且周期线程 COM 缺陷修复是前提 |
| D | 掩盖式底窗 | 任务栏色实底窗盖住原生时钟 | 遮不住错位 | **底窗若沿用错误矩形仍盖不住右侧残影**（评审正确指摘）；且只遮内容不修位置 |
| **E** | **潜入期冻结（新增，候选首选）** | 被盖期间**冻结重定位**——覆盖层保持进全屏前的位置（那本来就是正确位），全屏期探测值不消费不挪窗 | 退出露出即正确位，**零残影**、零算术假设 | 若全屏期间任务栏真重排（图标增减），保持旧位至治疗针 150ms 追平——仅罕见场景退化为现状；实现最小 |

**修订推荐**：先诊断归因（H1-H4），再在 E（冻结，若 H2 成立即全屏期布局值混入缓存）与 B（右缘锚定，若证明右缘稳定且 H1/H3 成立）之间定案；A 作为通用兜底保留。

**→ 归因结果：H2 成立（全屏期布局值被如实消费），E 方案实施。** 实现要点：
- `COVERED_FROZEN_POS`：进入覆盖首帧 `GetWindowRect` 记录窗口实际位置（用户最后看到的正常位）；
- 被盖期间 `relocate_clock_overlay_from_cache` 直接跳过（不消费全屏期布局矩形）；
- 退出覆盖时优先恢复冻结位（无视当时的缓存值），治疗针照旧刷新缓存；
- 实测（诊断日志+截图）：`freeze at (3660)` → 全屏期 4 次 3618 探测被忽略 → `unfreeze restore (3660)` → **+100ms 截图位置对齐、内容完整，零残影**。
- 诊断日志已加标记文件门控（`D:\agents_tmp\clockrect_debug` 存在才写），避免周期探测刷爆 menu_dbg.log。

## 5. 验证工具（复现/回归用）

- `D:\agents_tmp\zslip_test.ps1`——ffplay 全屏进出 + 时钟中心点 WindowFromPoint 命中翻转 + 翻转后 100/400ms 截图（本轮主验证脚本）；
- `D:\agents_tmp\content_ready_test.ps1` / `render_latency_test.ps1`——历史迭代版本；
- `D:\agents_tmp\test_video8.mp4`——8 秒测试视频；
- 对照物：`HKCU\Software\liCalendar\ClockRect`（缓存矩形）vs `verify_overlay.ps1` 枚举的覆盖层实际矩形 vs 截图中原生时钟残影位置。

## 6. 相关提交

- `8a432e5` 全屏判定改像素级（WindowFromPoint 三点测任务栏被盖）
- `1f85c6a` 全屏进出显隐事件驱动（前台窗口位移）
- `2924a26` **z 序潜入方案**（零空窗）
- `406223d` 退出瞬态错位自愈提速（≤2s → ~150ms）
