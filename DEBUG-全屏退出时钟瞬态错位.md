# 问题档案：全屏退出瞬间时钟位置偏移（瞬态错位）

> 状态：**N1 受控遮盖已实施并自动化验证通过，待 PotPlayer 真机复验**（2026-09-09，commit 见 §0.8）
> 关联：`PLAN-时钟覆盖式接管.md` Phase 1 追加章节 · 评审记录：`REVIEW-全屏退出时钟瞬态错位-第三阶段分析.md`

---

## 0. 归因第一阶段结论（2026-09-08 诊断埋点实测，假设 H2 成立）

诊断埋点（`clockrect:` 日志）抓到完整因果链：

```
uia rect=(3660,2076,3818,2160)   ← 正常态：时钟在右位
uia rect=(3618,2076,3769,2160) ×4 ← 全屏期间：时钟真实移到左位，缓存如实跟踪
```

- **全屏期间原生时钟真实移位 3660→3618**（任务栏布局随全屏变化），HWND 探测如实报告，缓存如实跟踪——第一阶段错位 = 消费了全屏期布局值；
- ⚠️ 勘误（第二阶段评审后）：本机 `find_clock_window` **能**找到时钟窗口（26200 存在经典类名 HWND，周期线程每 2s 经 HWND 路径成功探测——日志中 4 条 3618 即其写入）。§2 中"本机永远走注册表回声"的前提已被日志证伪，COM 无初始化问题降级为**潜在可移植性问题**（换机器可能触发）；
- 注册表 ClockRect 不是独立真值（是成功探测的持久化副本），不能作对照物。

## 0.5 真机复验失败 + 第二阶段归因（2026-09-08 深夜）

**E 方案（潜入期冻结）ffplay 自动化回归通过（+100ms 零残影），但 PotPlayer 真机复验问题仍在。** 用户多轮真机测试的日志轨迹：

```
freeze at (3660,2076)              ← 每次进入覆盖都冻结在正确位 ✓
unfreeze restore (3660,2076)       ← 每次退出都恢复到正确位 ✓（冻结本身无一次失败）
reveal-consume cache=(3618,...)    ← 但退出后缓存仍残留全屏期布局值（多次出现）
reveal-consume cache=(3660,...)    ←（有时缓存已新鲜——2s 探测相位运气）
```

**残余机制（第二阶段归因）**：冻结保护了**被盖期间**，但退出后**缓存里仍残留全屏期布局值（3618）**，而正常态的三个消费方照旧信任缓存：

1. 2s 周期 `relocate_clock_overlay_from_cache`——把覆盖层从 3660 拉回 3618；
2. 治疗针（+150/+600ms）——退出过渡期探测可能拿到中间态 3618，同样拉回；
3. 二者把第一阶段的错位在退出后**重新引入**，直到下一次探测写回 3660。

即：**E 修对了"潜入期"，没修"退出后"**——缓存的全屏期残留值在正常态仍被消费。

### 修复方向（待继续后实施，候选）

- **P1 布局解耦**：为覆盖层单独维护「正常布局矩形」（仅在任务栏可见+非全屏+连续两次探测一致时更新），与钩子点击路由缓存（需要真实当前布局）彻底分开；
- **P2 退出确认门**：退出覆盖后 N ms 内，探测结果与冻结位不一致时不应用（视为过渡态），一致才恢复消费缓存；
- **P3 最小补丁**：unfreeze 恢复冻结位的同时用冻结位**覆写缓存**，且退出后首针治疗只探测不重贴（第二针起恢复）。

P1 最治本（顺带修掉缓存语义混用），P3 最小改动。倾向 P1+P3 组合。

---

## 1. 问题现象（修复前，用户视角）

PotPlayer / Edge 退出视频全屏时：

1. ✅（已解决）时钟**立即出现**——z 序潜入方案生效，无空窗、无延迟；
2. ❌（本档案问题）出现的瞬间**位置偏左约 42px**，右缘露出一条原生时钟残影（能看到 "1:05 / -9-8" 碎片）；
3. 修复前残影持续 **≤2 秒**（等周期重探自愈）；现在压到 **~150ms**（两针延迟治疗），仍可察觉。

## 2. 根本原因（⚠️ 降级为待验证假设——2026-09-08 外部评审+代码复核后修正）

**现象层事实（无争议）**：恢复瞬间覆盖层用了**全屏期间缓存的旧矩形**（left=3618），原生时钟实际在 (3660,2076)-(3818,2160)，右缘露出 42px 条带。

**评审修正（原"根本原因"章节的方向描述写反了）**：数据是 全屏期缓存 3618 < 退出后实际 3660——全屏期间时钟在**左**位、正常态在**右**位，与原稿"全屏右移贴条、退出左移"相反。原稿的方向叙述是无数据支撑的推测，撤销。3618 的**真实出处尚未归因**，以下为代码复核确认的事实与候选：

### 代码复核确认的事实

1. **COM 缺陷（已证实）**：`mouse_hook.rs` 的 2s 周期线程直接调 `update_clock_area_cache()`，该线程**无 `CoInitializeEx`**（COM 按线程计；`refresh_clock_area_cache` 才带初始化）。本机 XAML 任务栏下经典时钟类名不存在 → `find_clock_window` 失败 → `CoCreateInstance` 无 COM 必败 → **周期线程永远走注册表回退**，实为"注册表回声"而非探测器。
2. **缓存写入来源至少四个**（无版本控制，后写覆盖先写）：
   - 周期线程（2s）：无 COM → **注册表回声**；
   - `refresh_clock_rect_if_in_taskbar`（钩子点击路径）：HWND + `GetWindowRect` 直接写缓存，**不持久化**、无注册表兜底；
   - `refresh_clock_area_cache`（治疗线程/attach/Tauri 命令，有 COM）：真 UIA 探测 + 持久化；
   - UIA 的 DPI 逻辑坐标修正路径（`validate_or_scale_clock_rect`）可能产出瞬态修正值。
3. **注册表 ClockRect 不是独立真值**——它就是成功探测的持久化副本（原稿把它列为对照物属循环论证）。

### 候选假设（待埋点归因）

| # | 假设 | 判据 |
|---|---|---|
| H1 | 钩子点击路径（HWND 来源）写入了某个"时钟代表窗口"的过期/错位矩形 | 日志见 hook-hwnd 来源写入 3618 |
| H2 | 全屏期间 UIA 真实探测到全屏布局（时钟确实在 3618），退出后布局恢复 3660，缓存滞后 | 日志见 uia 来源在覆盖期间写 3618 |
| H3 | UIA 逻辑坐标 + DPI 修正的瞬态产物 | 日志见修正前后坐标跳变 |
| H4 | 注册表在某个瞬态被写入了 3618，后经回声进入缓存 | 日志见 registry 来源写 3618 |

**结构约束不变**：UIA 不能进 WinEvent 回调线程（=钩子泵线程）；探测须等退出动画稳定；WebView2 一切隐藏/离屏手段都触发合成暂停（覆盖层只能在场+事后纠位）。

## 3. 已实施的缓解（现状，commit 406223d）

- **触发点**：`update_clock_overlay_visibility` 检测到 z 序状态从「潜入（below>0）」转换为「常规（-1）」的瞬间；
- **动作**：spawn 治疗线程（独立线程，不碰泵线程），两针延迟重探重贴：**+150ms / +600ms**（第一针避开退出动画未稳瞬间，第二针兜底）；
- **防重入**：`EXIT_HEALING` AtomicBool，治疗期间不重复 spawn；
- **实测**：+100ms 仍有残影（第一针未落地），+400ms 完全对齐——残影窗口从 ≤2s → **~150ms**。

- **实测**：+100ms 仍有残影，+400ms 完全对齐。注意名义针点（150/600ms）只是 sleep 起点，UIA 探测耗时另计，实际落针晚于名义值——残影窗口的真实边界在 (100, 400] ms 区间，"~150ms" 为粗略说法。

## 3.5 候选方案（评审后修订版）

| # | 方案 | 思路 | 预期 | 评审要点 |
|---|---|---|---|---|
| A | 加密治疗针（50/150/400/800ms） | 更密追赶 | 残影 ~50-100ms | 治标；数据源/并发问题不解决时可能抖动；50ms 是探测**开始**时间非完成时间 |
| B | 右缘锚定 | correct_right = tray_right − strip_w（最近对齐态缓存），纯算术即位 | 原判"零残影"**需修正**：播放器缩回时覆盖层先于回调自然露出，反应式移动必有一到几帧残影；唯一零残影变体是**潜入期预停预测位**（覆盖层反正被盖着） | 前提"右缘恒贴条"必须先跨多次切换证明；假设失效需显式检测，不会自动无害回退 |
| C | 潜入期间加密重探 | 500ms 重探 | 有限 | 若正确布局只在退出时产生，采得再勤也拿不到未来布局；且周期线程 COM 缺陷修复是前提 |
| D | 掩盖式底窗 | 任务栏色实底窗盖住原生时钟 | 遮不住错位 | **底窗若沿用错误矩形仍盖不住右侧残影**（评审正确指摘）；且只遮内容不修位置 |
| **E** | **潜入期冻结（新增，候选首选）** | 被盖期间**冻结重定位**——覆盖层保持进全屏前的位置（那本来就是正确位），全屏期探测值不消费不挪窗 | 退出露出即正确位，**零残影**、零算术假设 | 若全屏期间任务栏真重排（图标增减），保持旧位至治疗针 150ms 追平——仅罕见场景退化为现状；实现最小 |

**修订推荐**：先诊断归因（H1-H4），再在 E（冻结，若 H2 成立即全屏期布局值混入缓存）与 B（右缘锚定，若证明右缘稳定且 H1/H3 成立）之间定案；A 作为通用兜底保留。

**→ 归因结果：H2 成立（全屏期布局值被如实消费），E 方案实施。** 实现要点：
- `COVERED_FROZEN_POS`：进入覆盖首帧 `GetWindowRect` 记录窗口实际位置（用户最后看到的正常位）；
- 被盖期间 `relocate_clock_overlay_from_cache` 直接跳过（不消费全屏期布局矩形）；
- 退出覆盖时优先恢复冻结位（无视当时的缓存值），治疗针照旧刷新缓存；
- ffplay 自动化实测 +100ms 对齐——**但 PotPlayer 真机复验失败**（见上），引出第二阶段归因。

### E 方案缺陷的最终修复：P1+P2 几何权威状态机（2026-09-09 实施）

评审指出的漏保护路径完全成立：E 的冻结在**第一次恢复调用时即被清空**，下一次 500ms 兜底/WinEvent 调用就走 `clock.left + dx` 消费缓存——若缓存仍是 3618，覆盖层被拉回错位。冻结只保护被盖瞬间，不保护整个退出过渡期。

**实施**（三阶段几何权威）：
- `GEOM` 状态机：`Normal / Covered / ExitPending` + **认可矩形 `endorsed`**（覆盖层位置/尺寸唯一权威，与钩子点击路由缓存彻底解耦）；
- `clock_overlay_note_probe(rect)`：探测结果唯一入口。Covered 期样本忽略；ExitPending 期 `rect==endorsed` → 确认回 Normal；≥1s 且连续两次一致 → 采纳新稳定布局（应对全屏期间真实 DPI/布局变更）；≥5s 兜底采纳；Normal 期不同则即时采纳；
- `relocate_clock_overlay_endorsed`：仅 Normal 阶段应用 endorsed（size+position 一起）；Covered/ExitPending 一律冻结；
- `update_clock_overlay_visibility`：位置一律取 endorsed（+任务栏滑移 dx/dy），缓存矩形只进状态机、永不进几何；
- 退出过渡期的错误值（3618）在 ExitPending 下**结构性不可消费**——确认（probe==3660）前覆盖层始终停在 endorsed 位；
- 诊断：阶段翻转日志（ ungated）+ 应用日志（`clockrect_debug` 标记门控，含操作后实际矩形）。

**ffplay 自动化回归**：`phase->covered (hold endorsed 3660)` → `phase->exit-pending (hold endorsed 3660)` → `exit confirmed (probe==endorsed)`；+100ms 截图对齐、内容完整。（ffplay 为自动化辅助，最终以 PotPlayer 真机为准。）

### 第二次真机反馈 → 采纳门统一（最终形态，2026-09-09）

**真机新现象**：退出全屏瞬间时钟左右边缘都变宽（左缘稍偏左、右缘几乎到桌面最右端），随即极快弹回——**短暂的"超宽矩形"（~3612-3819，宽 ~207）被即时采纳**。

**机理**：ExitPending 的确认（probe==endorsed）在退出动画刚露出 3660 时就发生 → 回 Normal 态 → Normal 态对差异探测是**即时采纳** → 紧接着的过渡期宽矩形直接进 endorsed。ExitPending 只保护了确认前的窗口，确认后的大门仍然敞开。

**修复：采纳门统一化（ExitPending 简化掉）**——任何与认可位不同的探测，必须**连续两次一致且间隔 ≥500ms** 才采纳：
- 2s 探测节奏下，存活不足一个周期的过渡态中间值（宽矩形/全屏期布局值）永远凑不齐两次，**结构性不可被采纳**；
- 真实布局重排（图标增减等持久变化）2~4s 内正常跟进；
- 阶段简化为 Normal/Covered 两种（ExitPending 的语义已被候选门覆盖）。

**回归**：`phase->covered → phase->normal` 全程持有 endorsed 3660，零采纳、+100ms 截图对齐无残影。

**第二次真机复验仍失败（PotPlayer）→ 门槛升级**：PotPlayer 的退出过渡比 ffplay 慢，过渡期布局值可存活数秒——"两次一致且 ≥500ms" 的门被合法骗过（宽矩形被采纳数秒后弹回，即真机所见）。采纳门持久性门槛 **500ms → 8s**（commit `ba74a28`）：任何短于 8s 的过渡态结构性不可采纳；真实布局重排（图标增减/DPI）8s 后正常跟进，代价可忽略。诊断标记文件 `clockrect_debug` 已开启，真机复验时 menu_dbg.log 会记录全部几何应用。

## 0.6 第三阶段归因（8s 门后真机复验仍失败 → 几何管线已证清白，真凶是原生时钟自身滞后）

8s 门版本部署后用户 PotPlayer 复验仍见宽时钟。但这次 `clockrect_debug` 已开启，**用户真机测试留下了完整日志**，几何管线被彻底洗清：

```
phase->covered (hold endorsed 3660)
apply pos=(3660,2076) below=<PotPlayer> after=(3660,2076,3818,2160) ok=true  ← 潜入位正确
uia rect=(3618,...)                                   ← 全屏期：原生时钟真实在 3618
phase->normal (exit cover, hold endorsed 3660)
apply pos=(3660,2076) below=-1 after=(3660,2076,3818,2160) ok=true           ← 恢复位正确（操作后实测）
uia rect=(3660,...)                                   ← 原生布局恢复
```

多轮测试全部如此：**零采纳、零错误应用、每次操作后实际矩形都等于认可矩形**。几何管线不可能产生用户所见的宽时钟。

**真凶（第三阶段）**：Cycle 2 的日志显示，PotPlayer 退出后 `uia 3618` 仍出现**两次**（约 2~4 秒）才恢复 3660——**原生时钟自己在全屏期位置滞留数秒**（XAML 任务栏布局滞后），期间它暴露在我们的覆盖层左侧（我们正确地停在 3660，盖不住 3618-3660 段）。用户看到的"变宽的时钟" = 原生残块（3618-3660 段，左缘稍偏左）+ 我们的时钟（右缘 3818 近屏幕右端）拼成的并集；原生布局追上 3660（恰好钻回我们下方）即"极快弹回"。

**结论链**：我们的覆盖层几何已经无可挑剔；残余现象是**原生时钟自身的过渡滞后**，任何"修正我们自己的位置"的方案都不再相关。

### 候选修复方向（待开工令）

| # | 方案 | 思路 | 代价 |
|---|---|---|---|
| N1 | **并集遮盖** | 检测到真实原生位置（缓存）偏离认可位时，把覆盖层**临时扩展为两者的并集**（不透明背景遮住原生残块；文字右对齐锚定不动，视觉位置不变）；原生布局追上后缩回 | 过渡期覆盖层块面变宽（无文字错位）；实现中等 |
| N2 | 接受现状 | 原生残块仅存在于 PotPlayer 类慢退出后的 2~4s，且是系统自身行为 | 用户已明确不可接受 |

验证判据：真机 PotPlayer 退出全屏，左缘不再有原生残块（可开启 `clockrect_debug` + 连拍对照）。

## 0.7 第三方评审采纳（REVIEW-*.md）

第二阶段归因的方向性修正被评审确认，但评审同时纠正了本档案的多处过度推断，全部采纳：

1. **"原生布局退出后滞留 2~4s"未证实**——两条 3618 出现在 phase->normal **之前**（全屏期采样），不能推算持续时长，更不能对应用户所见残块；改为"恢复过渡期原生内容暴露于认可矩形之外是有几何依据的候选解释，持续时间待同步画面验证"；
2. **"几何管线彻底清白"收窄**——仅"已记录采样时刻外框矩形正确"（after= 只证明读取成功、外框正确，不证明 WebView 内容/不透明背景/叠放关系）；
3. **8s 门是防抖参数**，不是"结构性不可采纳"的强保证（中间探测失败会累计时长；超 8s 的过渡态仍会通过）；且若残余问题是原生内容露出在认可矩形之外，延长门槛毫无帮助；
4. **N1 实现要求**：内容矩形与遮盖矩形分离；遮盖须在被盖期间预备（退出后再准备错过第一帧）；收缩需稳定确认不可凭单次探测。

## 0.8 N1 受控遮盖实施（2026-09-09，自动化验证通过）

按评审要求实现"内容矩形/遮盖矩形分离 + 被盖期预备"：

- **GEOM 扩展**：`native_observed`（被盖期真实原生位置）、`mask`（当前遮盖矩形）、`shrink_requested`（收缩确认标志）；
- **预备时机**：被盖期间 `note_probe` 记录原生真实位置 → `update` 发现其偏离认可位时，窗口即时扩展为**并集**（认可 ∪ 原生观测，实测 3660-3818 ∪ 3618-3769 = 3618-3818 宽 200）——此时窗口仍被播放器遮住，扩展操作不可见，恢复瞬间遮盖已就绪；
- **内容锚定**：文字右对齐于窗口右缘，遮盖只向左扩展、右缘不变 → 文字视觉位置全程不动（本场景原生时钟恒在认可位左侧，右对齐假设成立；若未来出现向右/上下的并集扩展需改为内层固定宽方案，见 REVIEW §5.2）；
- **遮盖保持与收缩**：退出后窗口停在遮盖矩形（topmost，纯背景+原位文字），**直到探测确认原生归位（probe==endorsed）才请求收缩**，relocate 执行收缩（回 endorsed 尺寸+位置）；不凭单次探测立即收缩（评审 §5.4）；
- **诊断**：阶段/遮盖/采纳日志 + 应用日志（含操作后实际矩形，`clockrect_debug` 门控）。

**ffplay 自动化验证日志**：

```
phase->covered (hold endorsed 3660)
apply pos=(3660,2076) size=(0,0) below=<播放器>          ← 潜入
mask prepared union=(3618,2076,3818,2160)                ← 观测到原生在 3618，预备并集
apply pos=(3618,2076) size=(200,84) below=<播放器> after=(3618,...,3818)  ← 扩展遮盖
phase->normal (mask held)
apply pos=(3618,2076) size=(0,0) below=-1 after=(3618,...,3818)  ← 遮盖保持 reveal
shrink requested (native back at endorsed)               ← 原生归位确认
apply pos=(3660,2076) ... after=(3660,2076,3818,2160)    ← 收缩回认可位（尺寸回 158）
```

reveal+100ms 截图：无原生文字碎片、文字位置与正常态一致。（ffplay 自动化辅助；PotPlayer 真机复验待做——长短全屏、快速反复切换，复验时 `clockrect_debug` 标记已开，日志会记录全部几何应用。）

## 5. 验证工具（复现/回归用）

- `D:\agents_tmp\zslip_test.ps1`——ffplay 全屏进出 + 时钟中心点 WindowFromPoint 命中翻转 + 翻转后 100/400ms 截图（本轮主验证脚本）；
- `D:\agents_tmp\content_ready_test.ps1` / `render_latency_test.ps1`——历史迭代版本；
- `D:\agents_tmp\test_video8.mp4`——8 秒测试视频；
- 对照物：`HKCU\Software\liCalendar\ClockRect`（缓存矩形）vs `verify_overlay.ps1` 枚举的覆盖层实际矩形 vs 截图中原生时钟残影位置。

## 6. 相关提交

- `8a432e5` 全屏判定改像素级（WindowFromPoint 三点测任务栏被盖）
- `1f85c6a` 全屏进出显隐事件驱动（前台窗口位移）
- `2924a26` **z 序潜入方案**（零空窗）
- `406223d` 退出瞬态错位自愈提速（≤2s → ~150ms）
