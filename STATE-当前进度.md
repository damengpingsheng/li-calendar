# 会话续作指引：注入式任务栏时钟第三代（当前进度）

> 更新时间：2026-09-14 凌晨（v55：原生日期行回归+段行归属；S 阶段收尾） · 用途：新会话窗口快速接续

## 0. 当前决策（唯一生效，2026-09-14 凌晨更新）

> **S 阶段完成并追加 v55**：时钟段自定义风格上线（五段开关/顺序/颜色/字号/间距+行归属），**原生日期行回归**（时间+日期保持系统样式，农历/内容段为补充）。tap 最终形态 **v55**（v51→v55 五轮迭代修 9 个实测 bug，含 v50 潜伏雷「恢复孤立 Time」与 XAML 双父铁律实证）。详见方案 §3「S 阶段实测结果」。

- **生效方案**：`方案-注入式任务栏时钟第三代-2026-09-12.md`（v2 + §3「A~E、S 阶段实测结果」）；
- **v55 定案（用户信息架构澄清驱动：农历/日期=补充而非替代，保留系统时间+日期+星期原貌）**：
  - **原生日期行回归**：Date 不再摘离（v46 摘离教训只在「藏它」时成立），原生 sp=[横板[时间行段|Time], 横板2[DateWrap|日期行段]]；Date 文本/可见性/配色全系统管理零写入，对齐由自建 dateWrap Border 承担；
  - **段行归属** `row_<id>:1|2`（缺省天气/节日/节气→时间行、农历→日期行、Date 恒为日期行首位）；reflow 两行各自 capw 预算；**普适原则成文：凡系统持有写权的属性永不进控制回路**；
  - **host 恒发全量 style**（hide/rows/colors/sizes/gap 缺省值也发）——「增量字段+缺省不发」=会话内残留，同型 bug 第 5 次后定式；
  - **新血泪**：XAML 双父铁律（Child() 前必须先摘出原父，静默 rc=2 hr=0x80004005）；rows 缺省回退丢 `d` 笔误；部署命令链 `&&` 串接 dll 替换+explorer 重启会被文件锁打断（dll 替换必须在 explorer 死亡窗口内完成）；
  - **已知观察项**：日期行文本=系统注册表 sShortDate 渲染（当前 yyyy/M/d 无星期几；用户要星期几需自行改系统短日期格式加 ddd，注入侧零注册表写入）；GUI 行归属切换/主题翻转/全屏留日常观察（机制与 v54 同源）；
- **S 阶段关键定案**（详见方案 §3 S 小节）：
  - **时间段=原生样式不支持自定义**（S0 决策 a 案，用户确认）；**自定义色优先、缺省=跟随主题**；c1set style 扩展 `order/hide/color_<id>/size_<id>/row_<id>/gap`（总长 700→1200，tap 全字段再校验双端防御）；
  - **生产链**：liConfig `clockbarStyle` → apply_style 全局 → 数据线程每 tick 组进 c1set（**样式变更 ≤1s 生效，零重启**）；设置页 WidgetShowForm「时钟段样式」分组（调序/显隐/行归属/ColorPicker+跟随主题/字号/间距）；`set_clockbar_style` 命令；
  - **【v50 潜伏雷修复】恢复孤立 Time**：原位快照仅在扫描有效时更新（v52 现场复现退出时钟消失、v54 真实退出验证修复，v55 Date 同规则）；
  - **【E5 晨检产出】VM 停写复发**（21:04 冻结于 20:46）：**零会话扰动环境**——会话循环归因推翻，共同点只剩 tap 驻留+reparent；处置=explorer 重启恢复；归 F 长稳观察；**跨夜日界翻转已补验**（00:03 初三→初四 ✓）；
  - **session.rs 顺手修复**：loaded 谓词硬编码 "ver":49 永不命中（对齐 TAP_VER）；
- **工件**：tap v55（TAPVER 55；b51~b54 迭代版本）；部署=`D:\Program Files\li-calendar\`（liCalendar.exe + lical_clock_tap55.dll，旧版留存回滚）；证据 `D:\agents_tmp\s_stage_20260913\`（s1_*/s3_*/s4_* 截图+日志）+ `clockbar_tap_b51~55.log`；
- **测试基线现状**：liCalendar 运行中（注入开、explorer 11688、tap v55 驻留；双行：18.7℃晴[红#e24e4e]+时间 / 2026/9/14+农历；注册表 sShortDate=yyyy/M/d、H:mm 未动）；PotPlayer 常驻勿扰；**用户深夜在使用电脑，勿做 GUI 自动化**；
- **遗留（F/观察）**：①VM 停写归因未定案（归 F 长稳）②19:11 静默退出未复现 ③设置窗非最大化时小组件控件点击命中性一般（体验备注）④标记文件 toggle 钩子+panic 钩子 F 清理 ⑤时钟键盘唤起飞出/低分辨率/DPI 压测/触摸（C/D 遗留照旧）；
- **E 阶段定案不变**（E0~E4/E6 关闭；A/B/C/D 定案不变，血泪全集内置 v55）；**F 阶段=真实使用观察期+退役清理 PR（方案 §5/§7）**。

- **E 阶段关键定案**（详见方案 §3 E 小节）：生产会话管理器=主进程 `src-tauri/src/clockbar/`（mod/ffi/pipe/session/watch/data/weather；tyme4rs `=1.5.0` 入主 crate；tap DLL 随安装目录分发）；注入开关=liConfig `clockbarInjectionEnabled`（默认开）+设置页开关；pipe DACL+客户端身份校验；【新血泪】**沉降期注入中毒**（explorer 出生 ~4s 内注入→诊断端口永久不就绪；修复=tap 沉降期长预算 3600×1s+宿主年龄门 <45s 不注入）；【v50 定案】**安全卸载形态**（自持引用替代自钉，unload 后模块列表零残留）；陈旧消息污染（管道未连接先清历史）；注入模式注册表零写入；E6 互斥两态验证过（19:11 一次静默退出未复现，panic 钩子待捕获）；
- **D 阶段关键定案**（详见方案 §3 D 小节）：数据链=tyme4rs `=1.5.0` 锁版本（农历冬月/腊月映射、节气仅节气日、节日补充表、24 例对照一致）；c1set 空段口径=发单空格（S 起空段 tap 侧直接不建元素，22px/10px 节奏问题已修）；天气端点=`d1.weather.com.cn/weather_index` 纯 HTTP+Referer，城市解析 env→缓存→IP 定位，失败一律 `--` 占位；probe 子命令 ddata/dverify/dhold/dprops；
- **C 阶段关键定案**：对原生元素零布局属性写入（自建横板+Time reparent；v55 起 Date 不再摘离改 dateWrap 收编）；winRT 直改生产通道；capw 预算+Width 硬钳制（勿信 MaxWidth）；输入/tooltip 拦截=透明 Border；管道双端 Peek 轮询+UI 线程零管道 I/O；
- **【事件】时钟 VM 停写（16:36 与 21:04 两次，未定案）**：原生 Time 元素 Text 停走（dprops/props 巡检实锤），无创不可逆，explorer 重启恢复；第二次复发在**零会话扰动**环境——会话循环归因被推翻，共同点只剩 tap 驻留+reparent；F 长稳持续观察，复现即 dprops 判别；

---

> R7 的驱动评审：**REVIEW-退出全屏左缘收缩-R1-R6新一轮分析.md**
> R8 的驱动评审：**方案-退出全屏时钟残余色差与根本优化方向-R7.6评审.md**（另一 agent 分析，结论=优先做颜色链可靠性重构）
> 总方案/架构/排障史：**PLAN-时钟覆盖式接管.md**
> 本 bug 的完整归因链：**DEBUG-全屏退出时钟瞬态错位.md**（§0~§0.10）

---

## 1. 一句话现状

**S 阶段完成（v55）**：任务栏时钟段自定义风格（五段开关/顺序/颜色/字号/间距/行归属）+ **原生日期行回归**（时间+日期保持系统样式，农历与日期并排补充）；E 阶段全部关闭（E5 跨夜项已补验，VM 停写归 F 长稳）；下一阶段 F=真实使用观察期+退役清理 PR。旧覆盖层（R1~R9.4）维持已部署版本，与注入时钟互斥。

## 2. R7~R8 速查（详见总结文档 §3）

| 轮 | 修的什么 | 关键证据 |
|---|---|---|
| R7 | 评审 C/D/E：收缩后跟踪中断（观测窗 6s）/扩展滞后一轮/非原子应用 | 23:49 日志 5 次收缩 1~6ms 落位 |
| R7.1 | Covered 相位 relocate 即时展开（修扩展暴露窗 150~300ms） | 用户复验①「现象还是差不多」→ 定位 |
| R7.2 | 全屏位形记忆 last_native_layout（Covered 当轮预测展开） | 01:57 帧取证：暴露窗 191~323ms 内原生残块+托盘气泡可见 |
| R7.3 | ATTACHED 门（attach 前禁止几何应用，修 298×91 过宽启动闪现=「右缘偶发异常」元凶） | 02:03:19 日志 after 右缘 3947 |
| R7.4 | 根除收缩→重建死循环（Visible 分支只信当轮观测） | 02:16-17 诊断行 150ms 连环互搏 |
| R7.5 | 底色动态跟随：异步采样+≥3RGB 才 emit+采样点归属校验 | 三竞态采出 #00FFFD/#67EDE8/#C8F2EF 全实锤 |
| R7.6 | 换色时机三层：Visible 切换点 / 遮盖在场每针 / 收缩后 400ms 兜底 | 用户复验④「色差收缩完成后才消失」→ 复验⑤「有改善仍偶现」→ 暂停 |
| **R8** | **色彩链可靠性重构（评审 §3.1~§3.4 全证实修复）**：①采样锚点改当前实际应用矩形右缘（旧锚点跟随全屏位形→退出期采样列落进遮盖内部→归属校验拒绝=关键期采样结构性不可用）②采样三道闸（读前 9 点归属+读后复验+离散度>48 丢弃）③单 worker 合并请求串行推送（根除旧线程晚到）④seq 版本化（事件+命令响应带 seq，前端单调守卫）⑤换色时机提前+attach 点火+正常态 15s 可信色保鲜 | 部署日志：`appearance push seq=1 src=attach bg=#E7D8CD`、`trusted-15s: unchanged (d<3)` 每 15s 稳定；复验⑥：色差变小仍偶现、收缩更快仍可察觉、新报右缘出屏 |
| **R8.1** | **复验⑥驱动双修复**：①几何交叉竞态根除——日志实锤（00:56:49.120）：WinEvent 线程按读取时仍在场的遮盖贴 212 宽与收缩线程尾随 SWP_NOSIZE 常规跟随交错→窗口=「endorsed 左缘+遮盖宽度」(3649..3861) 右缘出屏 21px，变化检测元组记 NOSIZE 标志→错误尺寸判「无变化」无限驻留（实测 16.6s）；修=GEOM_APPLY_LOCK 串行化（锁序本锁→GEOM，relocate 拆持锁本体+锁外 update 防死锁）+update 各分支弃 NOSIZE 恒传全尺寸（任何后写者下一针自愈）②退出期采样翻转治理——过渡态亚克力（透出内容从视频切回壁纸）使活跃采样翻转推送（seq=2~11 全 src=z-reclaim，#F1E4DB↔#E7D8CD）=偶现色差主源；修=撤销退出窗口期全部活跃采样（信任 15s 保鲜稳态色），收缩后 0/400/1500ms 三连采 | 复验⑦：右缘出屏**不再复现**；色差仍有且有时持续 |
| **R8.2** | **闭环色差校准 servo**（修「细微色差持续不恢复」）：该偏差在「推送色→屏幕呈现」之间（WebView 色彩管理/采样条与时钟区横向底色差），开环结构性观测不到（采样与推送自洽、日志全 unchanged）；修=采样同读内部点（覆盖层右缘内 2px padding 纯背景三点中位=推送色实际显示效果，归属须命中覆盖层自身 HWND），新推送=旧推送+(外-内) 收敛 <2 RGB 死区；命令路径同步 LAST_PUSHED_BG；日志带四色值 | 部署 01:32:47 后 102 次读数全 d=0；稳态抓屏证实覆盖层==任务栏（R 侧 231/B 侧 205）——稳态无罪 |
| **R8.3** | **撤销收缩即时采样毒源**（复验⑧「还是一样」）：post-shrink-now（delay=0）在退出动画期屡采过渡色推送（#F0E4DB Δ9-14、#E8D9CE Δ1-3=「很细微」色差），连续测试时 400/1500ms 纠正针被下一次全屏盖住拒绝→驻留到 trusted-15s（最坏 ~6.5s）=「持续不恢复」；修=撤销该时针（R8.1 同款教训：过渡态不采样），收缩后 400/1500/4000ms 延迟三连采+退出观测窗内保鲜 15s→3s | 测试时段日志 seq=1~3 过渡色推送+连环 no sample 实锤 |
| **R8.4** | **主参考迁移：条带降级**（复验⑨「还是一样」+新观察「退出后先无色差→很快有色差并保持」）：取证①宽域横向剖面=壁纸梯度存在但时钟区局部 1~2RGB 非色差源②受控实验=白窗体垫任务栏后亚克力不透色（垫底假说排除）③日志=F0E4DB 在条带稳态持续 4s+ 归属全过=条带自有状态（悬停高亮/材质态）分叉主色被平涂；修=主参考改左侧净列（左缘外 2px 起每 8px 一列最多 5 列，托盘归属+三点离散度≤6 滤图标/高亮，取最近净列均值——与覆盖层下方同表面），条带降级回退+诊断对照（日志 `left=#.. strip=#.. (d=N)` 双读数） | 部署日志：`primary=#E9D9CC left=#E9D9CC strip=#E7D8CD (d=2) inside=#E9D9CC d=0` 双参考工作正常 |
| **R8.5** | **渐变背景双端点**（复验⑩「还是有」）：四色值揭示覆盖层下方表面本身是横向渐变（E9D9CC→E7D8CD 壁纸透出永在；亮态 F1E1D4→F1E4DA 更陡），平涂必与一侧边缘差 1~4=「细微色差一直保持」；修=推送 (bgLeft,bg) 双端点+前端 `linear-gradient(90deg)` 复现，闭环修正量同步双端（内部点配对右端）；serde camelCase 修命令响应 bgLeft 命名+listen 注册后重拉 invoke（attach 首针事件可能早于监听注册） | 抓屏验证：覆盖层 3660→3800 平滑 (233,217,204)→(231,216,205)，左缘配左邻±1、右缘配条带 0 差——渐变真实渲染 |
| **R8.6** | **恢复高频跟随**（复验⑪录屏定案）：ffmpeg 抽帧逐帧量化——稳态净列对比无差（渐变成功），但进出全屏边界帧覆盖层与原生分叉 7~24 亮度持续 0.25~0.5s+=采样滞后窗；认知修正=推翻 R8.1/R8.3「过渡毒色」结论（F1E1D4 等是表面真实材质状态，受控实验证实亚克力只透壁纸），不跟随才是色差来源；修=恢复 z-reclaim/visible-switch/mask-held-tick 逐针采样+新增常规态 visible-tick 每针轻采（d≥2 死区节流稳态零推送），保留左列主参考/双端渐变/闭环/延迟三连采/trusted 兜底 | 部署日志：visible-tick 每针运行稳态 d=0；复验⑫：色差残留频率降低（材质切换滞后+死区，近物理下限）；收缩抖动仍可感知 → R8.7 |
| **R8.7** | **收缩动画化**（复验⑫第二段录屏+日志对齐）：02:15:16~35 十九秒 7 次 covered↔shrink 循环（快速切换全屏），每次=遮盖左缘 42px 两次瞬移；时机已机械极限（1~6ms 落位），感知问题在「跳变」本体；修=遮盖收缩（左移≥12px）锁内清状态+锁外 4 步×25ms 滑入（SetWindowPos [x,right] 右缘恒定，中间矩形恒⊇endorsed 原生零暴露），SHRINK_ANIMATING 原子门使 update/relocate 动画期间让位防打断，终态精确落位+wrapper update 收敛；展开保持瞬发（残块暴露优先） | **复验⑬：与上一版无可感知变化**——暂停。复审修正：该结论只否定这一具体动画实现，且「架构内无进一步空间」措辞过强（颜色算法仍有确定性缺陷，见 R8.8） |
| **R8.8** | **颜色算法修复包实施**（暂停后复审 `REVIEW-R8.7残余问题根因与根治边界` 两缺陷源码证实后恢复）：①双端直接更新 `appearance_update` 纯函数（cand=本轮双端采样，d=与上次推送最大通道差）——修左端点冻结（采样不参与，渐变斜率冻结）+None 误收敛（R8.5 丢失回退，恰发生在退出/再入关键时段）+积分累积，inside 降级诊断量（insideErr 非零告警）②动画加固：SHRINK_ANIMATING 持锁内置位（复审 §6.3 竞态间隙）+滑入每步核对 GEOM 重新 Covered 即中止让位（复审 §6.2）③左采样列屏幕坐标固定列（右缘-175~-200px 六候选）+ 屏幕锚定渐变（198px 宽右缘 -8px，窗口只裁切不重映射，复审 §7）④单测 4 例锁定语义（含复审 §2 原始反例） | 部署日志：`converged right=#E7D8CD left=#E8D9CC strip=#E7D8CD (d=1) inside=#E7D8CD (err=+0,+0,+0)`——固定列/直接更新/诊断链全工作；**复验⑭「还是一样」→ 三机制定案见 R8.9** |
| **R8.9** | **分歧门+keepalive 提速**（复验⑭第三段录屏 10:52:01 逐帧定案，残余=三机制叠加）：①左固定列污染推假渐变——seq=103 bgLeft=#F5E6DC vs strip=#E7D8CD(d=15)，固定列 3619~3629 恰在托盘图标区，均匀亮 15 的悬停高亮可通过离散度过滤→陡峭假渐变=「亮色块+竖向接缝」②左右分歧期翻转推送——seq=120~123 四推四色翻转，left/strip 分歧 d=8~25③几何抖动致 WebView 透明帧——f10 帧覆盖层缺失、原生注册表时钟可见，日志窗口位置/z 序全正常=窗口在位但表面透明（212↔170 抖动+半秒 8 推渲染器丢帧）；修=分歧门（left/strip 同时在场且通道差>8 不推送保留可信色，封死①②两类污染；材质整体切换两边同步移动 d 小不受影响）+keepalive 200ms→100ms（透明帧减半） | 部署日志：converged right=#E2DBD4 left=#E9DACC strip=#E2DBD4 分歧门内正常跟随；复验⑮「还是有问题」→ 录屏定案残余主体=覆盖层整块消失（WebView 透明帧）→ R9.0 |
| **R9.0** | **平移式遮盖（移除全部 resize，根治透明帧）**（复验⑮第四段录屏 13:44:53 定案）：R8.9 后残余主体=覆盖层整块消失、原生注册表时钟透出（6.1s/14.1s 帧单次~130ms），日志证实消失帧窗口位置/z 序全正常=窗口在位但 WebView 表面呈透明帧；根因=宽度反复变化（遮盖 212↔常规 170）迫使表面重分配，keepalive 无法根治；修=遮盖从并集放大改为认可矩形**等宽平移**到全屏位形左缘（全屏时钟宽 151~163<认可宽 158~170 等宽即可完整盖住），窗口尺寸全程恒定零 resize，三处 mask 生成点+滑入动画同步改纯平移，前端渐变回退 100% 映射；代价=回摆期 ≤220ms 的 42px 残条暴露（远小于整块消失） | 部署验证 14:07 attach/采样/渐变正常；复验⑯「时钟明显从左向右移动」→ R9.1 |
| **R9.1** | **原生遮盖条（fixed-WebView 两窗分离）**（复验⑯第五段录屏定案）：R9.0 平移的代价兑现——时钟文字整体从左向右滑 42px 明显可见（平移移动的是含文字的整个窗口），而旧 resize 的代价是透明帧；共同根源=遮盖职责落在 WebView 窗口上；修=两窗分离：WebView 覆盖层**恒定于 endorsed 位**（零移动零 resize 零动画，文字永不滑动/表面永不重分配，两大残余同根除）+ 原生遮盖条（Win32 实色小窗，GDI 填渐变左端采样色，全屏位形下亮出于 native.left..endorsed.left 恰好盖住原生时钟超出段）；分歧门/颜色联动/z 序同策略保留；R8.7 动画+SHRINK_ANIMATING 退役 | 部署日志正常，单测 4 例通过；复验⑰「几次全屏退出后时钟彻底消失持续不变」→ 被埋自愈见 R9.2 |
| **R9.2** | **z 自愈**（复验⑰澄清：非闪现是持续消失、应用未崩溃）：进程存活/日志活跃/几何日志全正常但屏幕不可见 + 录屏任务栏重排特征（图标增减）→ Shell 重排重申任务栏 topmost 槽位把覆盖层压到任务栏之下，外部 z 扰动不改状态五元组、变化检测永不重申=被埋直到重启；修=常规可见态每针 WindowFromPoint 自检覆盖层中心归属，非本窗口即重申 topmost+清缓存（z self-heal 日志）；附渲染停滞鉴别日志（右缘 padding 像素 vs 推送右端差>12 记 surface stall）区分被埋与渲染器丢帧 | 部署日志正常，overlay-diag 周期转储上线；复验⑱仍消失→WER 实锤显示驱动 ResourceTimeout（TDR），自愈看门狗见 R9.3 |
| **R9.3** | **TDR 自愈看门狗**（复验⑱第六段录屏+WER 取证：终级根因=显示驱动 ResourceTimeout，LiveKernelEvent 0x1cc 三天 9 次签名一致，PotPlayer 全屏切换 GPU 压力触发；TDR 时显示栈重置→WebView2 GPU 表面丢失渲染管线卡死→覆盖层透明/原生时钟透出/重探线程卡死 DWM 调用=日志静默，一切自愈失效）；修=独立看门狗线程（不触 GPU）每 5s 检查 diag 新鲜度，静默>20s→隐藏/重显覆盖层+location.reload() 强刷渲染管线；边界=WebView2 被 TDR 完全打死时 reload 可能无效（环境级限制，GPU 驱动更新是根本缓解） | 部署日志正常；复验⑲发现 R9.2 GetPixel 探针持锁阻塞缺陷 → R9.4 移除 |
| **R9.4** | **移除 R9.2 引入的 GetPixel 挂死探针**（复验⑲「现象几乎一样」+日志取证）：R9.2 的 z 自愈像素鉴别（GetPixel 屏幕 DC 采样）在显示驱动高压/卡顿状态下无限阻塞，且位于持有 GEOM_APPLY_LOCK 的 update 路径内→整个几何系统挂死（16:56 会话：重探线程静默、TDR 看门狗连续 reload 无效）；修=移除该探针，z 自愈保留 hit-test（窗口管理器调用不触 GPU） | 部署日志正常；**待复验⑳**；根治路径=GDI 原生时钟（总结 §3.25） |
| **R9.2** | **z 自愈**（复验⑰澄清：非闪现是持续消失、应用未崩溃）：进程存活/日志活跃/几何日志全正常但屏幕不可见 + 录屏任务栏重排特征（图标增减）→ Shell 重排重申任务栏 topmost 槽位把覆盖层压到任务栏之下，外部 z 扰动不改状态五元组、变化检测永不重申=被埋直到重启；修=常规可见态每针 WindowFromPoint 自检覆盖层中心归属，非本窗口即重申 topmost+清缓存（z self-heal 日志）；附渲染停滞鉴别日志（右缘 padding 像素 vs 推送右端差>12 记 surface stall）区分被埋与渲染器丢帧 | 部署日志正常；复验⑱仍消失→WER 实锤显示驱动 ResourceTimeout（TDR），自愈看门狗见 R9.3 |
| **R9.1** | **原生遮盖条（fixed-WebView 两窗分离）**（复验⑯第五段录屏定案）：R9.0 平移的代价兑现——时钟文字整体从左向右滑 42px 明显可见（平移移动的是含文字的整个窗口），而旧 resize 的代价是透明帧；共同根源=遮盖职责落在 WebView 窗口上；修=两窗分离：WebView 覆盖层**恒定于 endorsed 位**（零移动零 resize 零动画，文字永不滑动/表面永不重分配，两大残余同根除）+ 原生遮盖条（Win32 实色小窗，GDI 填渐变左端采样色，全屏位形下亮出于 native.left..endorsed.left 恰好盖住原生时钟超出段）；分歧门/颜色联动/z 序同策略保留 | 部署日志正常，单测 4 例通过；复验⑰「几次全屏退出后时钟彻底消失持续不变」→ 被埋自愈见 R9.2 |

## 3. 部署与调试设施

- ✅ R8.9 已提交（完整链：R8=`dbc2b61`~R8.8=`4b920a4`、R8.9=`84b16f2`；squashed 分支，未推送）、已部署（cmp 字节一致校验）、进程正常运行
- 单测：`cargo test --release --lib` 4 例（appearance_update 语义锁定，含复审 §2 原始反例）
- `D:\agents_tmp\clockrect_debug` 标记**当前已开启**（geom_log 门控诊断行；不需要时删除该空文件即可）
- appearance 诊断行带 `src=`：attach / z-reclaim / visible-switch / mask-held-tick / visible-tick / post-shrink-400/1500/4000 / trusted-15s；读数含 `left/strip (d=N)/inside(err)/pushed` 多值（R8.8 起 insideErr 非零=渲染偏差告警，不进控制回路）
- 取证手段（本轮验证有效）：PowerShell SetProcessDPIAware + CopyFromScreen 抓屏逐列剖面（脚本 `measure_seam.ps1`）；受控实验（WinForms 白窗体垫任务栏后验证亚克力不透窗口）；**用户录屏 + ffmpeg 抽帧逐帧量化**（色差类问题最强取证）
- 工具脚本：`clock_baseline_probe.ps1`（A 基线对照）、`frame_capture.ps1`（31fps 帧取证）、`capture_with_potplayer.ps1`（一键录制）
- 3 分钟心跳自动化已不存在（此前已清理）

## 4. 下一步（恢复时）

1. **F 阶段（真实使用观察期）**：用户日常使用注入时钟（v54+自定义样式）；观察项=VM 停写复发（props 巡检在跑）/19:11 类静默退出（panic 钩子待捕获）/样式设置日常使用反馈；到期走退役清理 PR（方案 §5/§7：标记文件 toggle 钩子+panic 钩子清理、tap50 等旧 DLL 清理、eunload 探针留存评估）。
2. 开发迭代纪律（A~S 血泪全量）：改 DLL 必须 TAPVER 递增 + 重启 explorer 清驻留 + **新鲜 explorer 沉降期 ~4.5min**；批处理纯 ASCII+`/utf-8`；树判别式改动先过 sim.cpp 回放仿真；回调内零引擎调用零锁等待；**UI 线程零管道 I/O**；**tap/host 两侧读循环都必须 PeekNamedPipe 轮询**；**对原生元素零布局属性写入**；溢出度量=子项期望宽之和+可见段间 gap（v53 修订，勿信 MaxWidth）；**style 增量字段必须「会话开始复位+缺省值恒发」二选一防残留**（v51/v53 两次同型教训）；**explorer 优雅关闭优先**（taskkill 无 /F + 10s 宽限，无效才 /F；/F 后 Winlogon 未自动拉起 shell 需手动 `start explorer.exe`）；**合并迭代禁无谓重启**。
3. 构建：liCalendar=`cargo build --release --features tauri/custom-protocol`（前端改动先 `pnpm build`，部署后校验产物+清限定本应用的 msedgewebview2）；探针 crate `cd src-tauri/clockbar && cmd //c D:\agents_tmp\c_stage_20260913\build_host.cmd`；tap `cd src-tauri/clockbar/tap && TAPVER=N cmd //c build_tap.cmd`；测试主入口探针 `dhold <secs> [style-json]`/`c1hold <secs> '<json>' [diag]`（S 阶段实测：c1hold+diag 的 c0tree 回读可核对边距）；诊断 `ddata`/`dprops`；天气测试钩子=env `LICAL_WX_HOST`/`LICAL_CITYID`/`LICAL_WX_DEBUG=1`。
4. S 阶段遗留=C/D 遗留清单见方案 §3 对应小节；GUI 自动化经验：设置页 ⚙ 按钮命中区小（非最大化窗口建议先最大化），组件页脚悬停出「设置/退出」按钮。

## 5. 关键环境事实（新会话必读）

- 部署路径 `D:\Program Files\li-calendar\liCalendar.exe`；构建命令与 MSVC 环境见项目 AGENTS.md（必须带 `--features tauri/custom-protocol`，**改前端后必须先 `pnpm build`**）；构建产物 `src-tauri\target\release\li-calendar.exe`
- 部署后重启前清理 WebView2 **限定本应用进程**（按命令行含 li-calendar 过滤）
- **日志锚定必须按最近一次 `attached at attempt` 行号**——NTP 回拨致时间戳撞车，昨天的旧日志会冒充新回归（已踩坑一轮）
- 端到端量化 ≈220ms（150ms sleep + ~70ms uia-class 探测，本机 HWND 快速路径恒不命中）；评审保留意见：70ms 含重复树查找，未必是下限
- 时钟矩形本机实测：正常 (3660,2076,3818,2160) 与 (3649,2076,3819,2160) 随时间文字宽度摆动（**宽值⊇窄值**，R8 评审修正）；全屏位形对应 (3618..)/(3607..)；显示器 3840×2160 @175%
- R8.4 后采样主参考=覆盖层左侧净列（左缘外 2px 起每 8px 一列），条带（显示桌面细条）降级回退+诊断；R8.5 后覆盖层背景为双端点渐变（任务栏底色有壁纸透出的横向渐变，平涂必差 1~4）
- **任务栏底色非常数**：材质双态切换（全屏进出/最大化窗口开关，±9~24 亮度，秒级）+ 壁纸横向渐变；受控实验证实亚克力只透壁纸不透后面的窗口
- PotPlayer：`F:\Software\PureCodec20260630\PureCodec\x64\PotPlayerMini64.exe`（用户测试用，勿自动化）
- 排障方法论：每轮修复必须有日志/像素证据；结论措辞留余地；用户感知描述精确到「哪个动作的时刻」再归因；真机现象与 ffplay 回归是两套判据（ffplay 无退出动画）；**色差类问题首选用户录屏+ffmpeg 逐帧量化取证**；撤销一个机制前先确认它响应的现象是真污染还是真变化（R8.1/R8.3→R8.6 翻案教训）

## 6. 相关文件清单

| 文件 | 内容 |
|---|---|
| PLAN-时钟覆盖式接管.md | 总方案（架构+实施全集+排障史索引） |
| DEBUG-全屏退出时钟瞬态错位.md | 本 bug 完整归因链 §0~§0.10 |
| REVIEW-退出全屏左缘收缩-R1-R6新一轮分析.md | R7 驱动评审（C/D/E 证实修复；A 未做） |
| 方案-退出全屏时钟残余色差与根本优化方向-R7.6评审.md | R8 驱动评审（颜色链优先/四风险/实施顺序） |
| src-tauri/src/window_manager/windows/clock_overlay.rs | 覆盖层核心（状态机/遮盖/R7 全部机制/R8 色彩链） |
| src/windows/ClockOverlayWindow.tsx | 覆盖层前端（clock-appearance 事件监听 + R8 seq 守卫） |
