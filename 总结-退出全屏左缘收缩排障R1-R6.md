# 总结：退出全屏左缘收缩感知排障（R1~R8.7 全记录）

> 更新时间：2026-09-12 凌晨 · 状态：**应用户要求暂停**（R8.7 已部署；复验⑬：收缩动画化与上一版相比无可感知变化）
> **暂停后复审（同日）**：另一 agent 评审 `REVIEW-R8.7残余问题根因与根治边界-2026-09-12.md` 指出颜色算法仍有可直接修复的确定性缺陷（渐变左端点不跟随采样、inside=None 误收敛等），经逐条对照源码**复核成立**——原「已达架构极限」定案修订，恢复时第一步为 R8.8 颜色算法修复包（见 §3.17、§7.0）
> 上游文档：`PLAN-时钟覆盖式接管.md`（总方案）、`DEBUG-全屏退出时钟瞬态错位.md`（§0~§0.10 前期归因）、`STATE-当前进度.md`（会话续作）、`REVIEW-退出全屏左缘收缩-R1-R6新一轮分析.md`（R7 的驱动评审）、`方案-退出全屏时钟残余色差与根本优化方向-R7.6评审.md`（R8 的驱动评审）、`REVIEW-R8.7残余问题根因与根治边界-2026-09-12.md`（暂停后复审，含 38.7s/38.9s 证据帧）
> 本文覆盖：左缘收缩感知问题（四阶段现象对照中的第④项）从 R1 到 R8.7 的完整尝试、修改、证据与现状

---

## 1. 问题定义

覆盖式时钟接管 Phase 0/1 完成后，PotPlayer 退出全屏仍残留四段式瞬态：

1. 原生时钟先显示一段时间 → 2. 我们的时钟才出现 → 3. 边缘过宽 → 4. 收缩回正常。

R1~R3（前置阶段，commit `c2eeed1`）解决 ①②③ 并经真机复验通过；④「左缘收缩可感知」成为最后一个 residual，本文档记录针对 ④ 的全部迭代。

**几何背景**（本机实测，3840×2160 @175%）：

- 认可矩形（正常位）：`(3660,2076,3818,2160)` 宽 158
- 原生时钟全屏位形：`(3618,2076,3769,2160)` 宽 151——左缘差 **42px**
- N1 受控遮盖：全屏/退出期把覆盖层扩展为并集 `(3618,2076,3818,2160)` 宽 200，盖住处于全屏位形的原生时钟；退出后原生归位，遮盖收缩回认可矩形。**左缘的扩展与收缩就是这套机制的工作痕迹。**

## 2. 基线：R1~R3（前置会话，已验收）

| 修复 | 内容 | 来源 |
|---|---|---|
| R1 | 遮盖保持期位置+尺寸成套应用（杜绝「认可左缘+遮盖宽度」的右缘 3860 冲进「显示桌面」区） | 第四阶段评审 §3.3 |
| R2 | 新一轮 Covered 清空旧轮观测/收缩状态；收缩需连续两次探测确认 | 评审 §3.4 |
| R3 | 去抖等待期分类化提前夺 z（时钟中心命中任务栏/自己才 `HWND_TOPMOST`） | 评审 §4 F1 |

真机复验（22:49 时段）：①原生时钟几乎不可见 ②我们的时钟出现干脆 ③右缘正常——**全部通过**；④用户反馈「左边缘有明显收缩过程」。

## 3. 本轮迭代（R4 → R5 → R6）

### 3.1 R4「归位即缩」（`14ebbce`）——假设部分错误

**用户诉求**：遮盖保持期不附加保守等待，收缩时点 = 原生归位时点（0.3~0.5s 太长）。

**改动**：
- `note_probe` 权威提前确认：Covered 态收到 UIA 读数 == 认可矩形且 mask 已展开 → 跳过覆盖探测去抖直接切 Normal 并置收缩请求（UIA 是定位原生时钟的同一权威来源）；
- 收缩确认从「连续两次」降为「UIA 单次确认」（`shrink_confirm` 字段删除）；
- 遮盖保持期 UIA 轮询 500ms→150ms，且每针重探+重贴。

**验证**：ffplay 回归通过（fast-path 触发后 9ms 完成收缩）。真机复验：**仍可察觉**。

**真机日志（23:39）证伪了归因**：
- 收缩动作本身已是 9ms 级，不是问题；
- 真正发生的事：**退出后原生时钟自己在 3618↔3660 间回摆 4 趟、持续约 5 秒**（PotPlayer 退出过程驱动任务栏布局反复切换）。完整读数序列：
  `40.220 归位 → 40.806 回全屏位 → 41.336 归位 → 41.954 回 → 42.352 归位 → 44.103/44.452 又回 → 44.693 最终归位`
- R4 在 42.35 第一次收缩（当时确实归位），44.1 原生又跑回全屏位、被迫再次扩展——最终收缩 44.709，用户感知为「撑了很久才缩」；
- 另有两个附带缺陷：回摆证据被相位翻转清空/覆盖（41.336 的 3660 读数覆盖 40.806 的 3618），遮盖多次晚展开、原生残块暴露（40.575/41.722/43.864 三个窗口）；mask 在场即 150ms 加密导致**整场全屏播放都在跑 150ms UIA**（开销 bug）。

### 3.2 R5「单周期化」（`f4a9b6e`）——方向相反，核心已撤销

**假设**：把 N 次跳变压成一次（展开一次→稳住全程→收缩一次）可降低感知。

**改动**：
- `SETTLE_STABLE_MS=1200` 收缩 settle 门：「探测 Visible + UIA==认可位」持续 ≥1.2s 才收缩；
- 回摆证据跨相位翻转保留（Covered 重入不再清 `native_observed`；Normal 相位非认可位读数也喂遮盖数据）；
- Visible 分支新增遮盖展开路径（探测 Visible 但原生观测≠认可位 → 立即展开，堵残块暴露窗）；
- 轮询节奏函数化 `clock_overlay_reprobe_interval_ms()`（修掉整场加密 bug）。

**验证**：ffplay 回归一次展开/一次收缩。**但用户澄清**：可感知的不是收缩动作，而是「**收缩开始时刻**太晚」——视频退出后明显经过一段较长时间左缘才开始缩。R5 的 settle 门让原生归位后左缘还要撑 ≥1.2s，与诉求正反。

**保留遗产**：证据跨相位保留、Visible 分支展开路径、轮询节奏函数——这三者被 R6 继承。

### 3.3 R6「实时跟踪」（`b66cfb3`）——当前方案

**设计原则**：遮盖扩展与否**逐读数对齐原生真实位置**，不做任何时间维度的守门。

**改动**（相对 R5）：
- UIA 读到原生 == 认可位 → 立即置 `shrink_requested` 并在同轮询周期内收缩（时点 = 归位时点 + 一次读数量化 ≤150ms）；
- UIA 读到非认可位 → 撤销收缩请求并喂 `native_observed`（扩展）；
- 回摆期轮询加密补充条件：相位瞬时回 Covered 但 **3s 内见过 Visible** → 保持 150ms（修 R4 实测 42.5→44.1 的 1.6s UIA 空窗）；稳态全屏（Covered+mask+3s 无 Visible）维持 500ms/2s，UIA 开销不随片长增长；
- R5 的证据保留与 Visible 分支展开路径原样保留。

**验证**：
- ffplay 回归（01:20 时间线）：20.371 covered → 21.070 mask 扩展（唯一一次）→ 28.792 收缩请求 → **28.793 收缩落位（1ms）** → 稳态；
- **真机复验（用户）**：「基本上退出全屏左缘就往回缩了，比原来快很多，**但还是能察觉到**」→ 用户决定暂停，接受现状。

### 3.4 R7「评审驱动修复」（当前方案）——修 review 证实的三个代码级风险 + 补时延证据

**背景**：新一轮评审（`REVIEW-退出全屏左缘收缩-R1-R6新一轮分析.md`）质疑「物理下限」结论，列出按优先级排序的候选原因；其中三个静态代码风险经本轮逐一核对**全部证实**并修复，A（z 序是否参与回摆）按评审意见留给对照实验。

| review 条目 | 核对结论 | 修复 |
|---|---|---|
| C：首次收缩后跟踪过早结束 | **证实**。收缩清空 mask/native 后：轮询条件 `Normal if mask.is_some()` 失配 → 掉回 500ms/每 4 针才 UIA；`note_probe` Normal 相位偏离读数仅当 mask/native 在场才喂遮盖数据——两条件收缩后都不成立，回摆读数只进 8s 候选路径，再扩展最坏 ~2.5s（23:39 首个收缩后回摆仍持续 ~2.4s） | **退出观测窗** `exit_watch_until`：真实收缩（遮盖→无）后 6s 内轮询维持 150ms（interval 函数新增第④加密条件）、Normal 相位偏离读数也喂 `native_observed` 并撤销收缩。观测窗只延长跟踪，**绝不重新构成收缩门槛** |
| D：扩展与收缩不同轮即时 | **证实**。relocate 在 `mask.is_some() && !shrink_requested` 直接 return，新偏离要等下一轮 update（+150ms） | relocate 新增**即时扩展路径**：不收缩时若 `native_observed` 偏离认可位且新并集 ≠ 现遮盖，立即 `apply_mask_geometry` 应用（与收缩同轮、对称） |
| E：收缩拆两次窗口调用 | **证实**。`set_size`→`set_position` 两连调用，中间态（先缩宽、左缘未动）右缘瞬停 3618+158=3776 冲进托盘区，随后跳 3818 | `apply_overlay_geometry` 改**单次 SetWindowPos 原子应用**（SWP_NOZORDER，位置+尺寸成套；HWND 不可得才回退 Tauri 两连） |
| B：150ms 只是 sleep 参数 | 证实（事实性）。端到端还含探测耗时与调度 | 探测日志补 `src=` 与 `ms=`（区分 hwnd 快速路径 / uia-autoid / uia-class）；见下方实测 |
| A：z 序控制参与回摆 | 未证实（需对照实验） | **不改代码**。备好只读基线脚本 `D:\agents_tmp\clock_baseline_probe.ps1`（应用开/关各跑一轮 PotPlayer 退出，对齐比较 3618↔3660 回摆频率） |

**验证（ffplay 23:23 时间线，R7 部署后）**：
- 29.410 进全屏潜入 → 29.634 首读 3618（src=uia-class，ms=73）→ 29.785 **一次展开** union(3618..3818) → 稳态遮盖全程保持；
- 37.996 读到 3660（归位）→ 37.997 收缩请求+收缩 → **38.002 落位 (3660,2076,3818,2160)**（单次原子应用，`after=` 校验正确）；
- **观测窗生效**：收缩后 150ms 快节奏持续至 44.1（≈6s）→ 45.193 回落 500ms → 47.264 回落 2s 稳态——与设计完全一致；
- **时延实测（B 的答案）**：本机探测 src 恒为 **uia-class**（HWND 快速路径从不命中——Win11 26200 无经典时钟窗口，`find_clock_window` 失败），每针 ~65-75ms ⇒ **实际读数量化 ≈ 150ms sleep + 70ms 探测 ≈ 220ms**，文档原先的「≤150ms」偏乐观。
- **真机复验①（23:49~23:50，R7）**：R7 修复逐项被日志证实工作——回摆 5 趟，5 次收缩全部在读到归位读数后 **1~6ms** 落位，观测窗内无降频空窗。但用户感知「现象还是差不多」：**残余感的主体是每趟摆动的扩展暴露窗**——重探线程调用序为 update（tick N）→UIA 读数→relocate，Covered 相位下 relocate 原先直接 return，新偏离要等下一轮 tick（+150~300ms）才展开，期间原生时钟左段残块露出。busy 期 UIA 探测还会变慢（ms=110~135，稳态 65~75）。
- **R7.1（本轮补丁）**：relocate 新增 **Covered 相位即时展开**——mask 未展开而 native 观测偏离认可位时立即调 `update_clock_overlay_visibility` 当轮展开。ffplay 回归（00:06）：读到偏离 → **+2ms** mask prepared → +6ms 落位（R7 时 +150~300ms）；一次展开/一次收缩维持。**暴露窗从 ~150-300ms 压到 ~2-6ms**（UIA 探测的固有 ~70ms 延迟在读数之前，不可再压）。
- **几何事实更新**：跨零点布局重排实测——23:59:59 认可位 (3660,2076,3818,2160) → 00:00:01 起 (3649,2076,3819,2160)，8s 采纳门于 00:00:09 合法放行；全屏位形同步变为 (3607,2076,3770,2160)，偏差幅度不变（左缘 42/49px）。
- **真机帧取证②（01:57，R7.1，31fps 逐帧+日志对齐）**：两轮快速进出全屏。帧 246/247（遮盖展开前的暴露窗）清晰可见**原生时钟文字残块（灰色「周四」）+托盘音量气泡**；帧 283（最终态）与 9 秒前基准帧仅差「分钟走字」像素。结论：**残余感知主体=进全屏时等 UIA 首读的 191~323ms 暴露窗**（展开/收缩动作本身与色差在像素上不可辨）。
- **色差量化排除（评审 F）**：扩展段 (3612~3647) 与采样源 (3821+) 实测色差 <2 RGB 单位——横向渐变假设排除。
- **R7.2 全屏位形记忆**：`last_native_layout`（跨退出轮保留的最后非认可位观测）——Covered 探测当轮即按记忆展开遮盖，不等首针 UIA（02:13:50 诊断行证实命中即展开）。
- **R7.3 ATTACHED 门**：attach 前窗口保持 builder 逻辑尺寸（170×52 逻辑 = **298×91 物理** @175%），visibility 兜底/WinEvent 若在 attach 重试间隙应用几何（NOSIZE+SHOWWINDOW），窗口以过宽尺寸闪现、右缘伸进「显示桌面」区（02:03:19 实测 after=(3649,2076,3947,2167)）——**疑似用户报告「右缘偶发异常」的元凶**。attach 前只维护状态机不做窗口操作。
- **R7.4 收缩→重建死循环根除**：诊断行实测（02:16-17）exit_watch 观测窗内 visible-branch 用记忆 fallback 重建遮盖，与下一针收缩同毫秒互搏、150ms 连环（左缘每秒闪 5+ 次）。Visible 分支回归**只信当轮真实观测**的 R7.1 语义；预测展开归 Covered 分支专属（那边有盖住者压着，多盖 42px 底色在安全侧）。
- **endorsed 随时钟内容摆动观察**：认可位在 (3660,2076,3818,2160)↔(3649,2076,3819,2160) 间随时间/日期文字宽度变化摆动（23:59→00:00 跨零点、02:13→02:19 间均实测切换），8s 采纳门每次合法放行。endorsed 锁在两值更宽一侧时安全（全覆盖）；采纳门在「多数派+少数派穿插」下会锁死在旧值（少数派读数反复清候选）——当前无害（两值差 11px 且窄值⊆宽值覆盖），列为观察项。
### 3.5 R7.2~R7.4 暴露窗治理与循环修复

**真机帧取证②（01:57，R7.1，31fps 逐帧+日志对齐）**：两轮快速进出全屏。帧 246/247（遮盖展开前的暴露窗）清晰可见**原生时钟文字残块（灰色「周四」）+托盘音量气泡**；最终态帧干净。结论：**残余感知主体=进全屏时等 UIA 首读的 191~323ms 暴露窗**（展开/收缩动作本身与色差在像素上不可辨）。同场色差量化：扩展段与采样源差 <2 RGB（当时）。

| 修复 | 内容 |
|---|---|
| R7.2 | **全屏位形记忆** `last_native_layout`：跨退出轮保留最后非认可位观测——Covered 探测当轮即按记忆展开遮盖，不等首针 UIA（covered-fallback 诊断行证实命中即展开） |
| R7.3 | **ATTACHED 门**：attach 前窗口为 builder 逻辑尺寸（170×52 逻辑=298×91 物理@175%），兜底线程在 attach 重试间隙应用几何会以过宽闪现、右缘伸进「显示桌面」区（02:03:19 实测 after 右缘 3947）——**「右缘偶发异常」元凶**。attach 前只维护状态机不做窗口操作 |
| R7.4 | **根除收缩→重建死循环**：诊断行实测（02:16-17）exit_watch 观测窗内 visible-branch 用记忆 fallback 重建遮盖，与下一针收缩同毫秒互搏、150ms 连环闪现。Visible 分支回归**只信当轮真实观测**（R7.1 语义）；预测展开归 Covered 分支专属（有盖住者压着，多盖 42px 底色在安全侧） |

### 3.6 R7.5~R7.6 底色动态跟随（当前方案）

**用户复验④报告的两种现象**：①色差小时轻微边框/字体抖动 ②色差大时收缩全程可见；进一步澄清「色差在收缩完成后才消失，从有到无的过程可被肉眼观察到」。

**根因链（全部有日志/像素实锤）**：
1. **底色静态采样**：任务栏底色随亚克力/壁纸/系统状态动态变化（同机实测两时段采出 `#F3F3F3` vs `#E7D8CD`，差 30+RGB），覆盖层此前只在启动/主题切换时采样——色差期遮盖边界全程可见；
2. **采样污染三竞态**（R7.5 开发中逐个实测复现）：①盖住期采样采出视频色 `#00FFFD` ②探测 Visible 后采样前视频盖上采出 `#67EDE8` ③收缩后 39ms 合成残影采出 `#C8F2EF`；
3. **换色时机晚**（R7.5 初版）：采样在收缩后 400ms，遮盖撑着时段仍用旧色——「从有到无」可见。

**修复**：
- R7.5：`refresh_clock_overlay_appearance` 异步采样+变化检测（≥3 RGB 才 emit `clock-appearance` 事件），前端 `ClockOverlayWindow.tsx` 监听即时换色；`sample_taskbar_pixel` 加 **WindowFromPoint 采样点归属校验**（命中任务栏/其子窗口根才采）。
- R7.6：换色时机三层——①退出确认 Visible 切换点（delay=0）②**Visible 分支且遮盖在场每针尝试**（覆盖回摆撑盖主时段，每轮 0.5~1.5s，首针即换新色）③收缩后 400ms 兜底。

**复验⑤结论（用户）**：「有改善，但退出全屏后仍偶现有色差的情况」→ **应用户要求暂停**。残留分析：首轮退出→fast-path 收缩前的窗口（≤220ms 读数量化+渲染）来不及换色，是偶发色差的已知残余候选；若将来恢复，方向是「进全屏前预采/退出首个 Visible 探测点即时采样」。

### 3.7 本阶段日志陷阱（新会话必读）

- **NTP 回拨时间戳撞车**：系统时钟被校准回拨 ~20h，昨天 22:25 的旧日志（R2 时代 `(2x confirmed)` 格式）冒充新回归误导归因一轮——**日志锚定必须按最近一次 `attached at attempt` 的行号**，不能只信时间戳前缀。
- 无 `src=` 的旧格式行 23:53~00:05 段出现之谜**已解开**（时间戳撞车，非 ghost 进程）；超屏几何行已归因（R7.3 修复的启动闪现）。

### 3.8 R8 色彩链可靠性重构（当前方案，评审驱动）

**背景**：R7.6 复验⑤「有改善仍偶现色差」暂停后，另一 agent 评审（`方案-退出全屏时钟残余色差与根本优化方向-R7.6评审.md`）核对采色/几何/前端代码，结论=**最有性价比的下一步是颜色采集与交付的可靠性重构，而不是继续压缩收缩动作**，并点名 4 个代码级风险——本轮逐一证实并修复。

| 评审条目 | 核对结论 | 修复 |
|---|---|---|
| §3.1 采样参考点跟着全屏位形移动 | **证实（功能性 bug）**。旧锚点=钩子缓存右缘，全屏期如实跟踪 3618 布局 → 采样列 3772..3782 落进遮盖 (3618..3818) **内部** → 被自己的窗口归属校验拒绝 → 退出最需要换色的时刻采样结构性不可用、一直用旧色（R7.6 时机层②在这类时刻全部空转） | 锚点改**当前实际应用矩形**右缘（遮盖在场=遮盖矩形，常规=认可矩形；LAST_APPLIED_RECT→endorsed→cache 三级回退）——采样点恒在覆盖层窗口右侧之外 |
| §3.2 归属检查与像素采集不同一时刻 | 证实（TOCTOU）。旧版只查每列中线 1 点，气泡可只盖上/下行 | 三道闸：读前**9 点逐点**归属校验 → 读后复验三列中线 → 块内离散度守卫（spread>48 判污染丢弃）；失败原因全部 geom_log 入诊断 |
| §3.3 并发采色无顺序保证 | 证实。每次调用 spawn 线程，旧线程可在新线程后 emit 旧色 | **单 worker 合并调度**：`APPEARANCE_DUE` 请求队列（最多 8 条，各自到期，400ms 兜底不被即时请求吞掉）+ 永久 worker 30ms 粒度串行消费（最早到期先采）；删除 `CURRENT_BELOW abort`（盖住者必同时盖住采样条，归属校验已覆盖该防御且不再牺牲可用性） |
| §3.4 发送成功≠前端已显示、无版本 | 证实。`LAST_PUSHED_BG` 在 emit 前更新、emit 错误被忽略、初始 invoke 响应可晚到覆盖事件值 | `APPEARANCE_SEQ` 自增，事件 payload 与命令响应都带 `seq`；前端 `appliedSeq` 单调守卫（低于已应用值即丢弃） |
| §1 推荐次序 2「颜色恢复独立于几何探测」 | R7.6 时机层在 fast-path 收缩场景全部错过 | 两个新提前点：**z-reclaim 早信号**（去抖等待期时钟中心已命中任务栏/自己=盖住者让位最早信号）即时采样；**收缩执行点**即时采样（+400ms 兜底保留） |
| §4.1.1 正常态可信色保鲜 | 缺失。旧实现只在退出轮采样，正常态亚克力缓变时覆盖层色渐旧无人纠正；会话首轮退出时底色陈旧=**复验⑤残余的主候选** | **15s 可信色保鲜线程**（仅稳态可见期：ATTACHED+Normal+无遮盖+常规 z）；**attach 即刻采样**（点火 worker + 前端过渡色换真色） |

**R8 部署验证（00:00:40 日志）**：attach attempt 0 → `appearance push seq=1 src=attach bg=#E7D8CD`（新锚点实采成功、与晚间亚克力色一致）→ `appearance run src=trusted-15s: unchanged (d<3)` 每 15s 稳定运行（变化检测节流正常）。真机复验⑥：色差变小仍偶现、收缩更快仍可察觉、新报右缘出屏 bug → 进入 R8.1。

### 3.9 R8.1 几何交叉竞态根除 + 退出期采样翻转治理（复验⑥驱动）

**复验⑥反馈**：①有改善，偶现色差比之前几版小 ②收缩时刻更快但仍可察觉 ③**新 bug：偶现退出后时钟跑到任务栏最右侧**（截图 00:57:14）。

**③右缘出屏——日志实锤的跨线程几何交叉竞态**（00:56:49.119~.120）：

```
[00:56:49.114] shrink (native endorsed)                        ← 重探线程收缩
[00:56:49.119] apply pos=(3607,2076) size=(212,84) below=-1    ← WinEvent 线程按读取时
                                                                 仍在场的遮盖状态贴 212 宽
[00:56:49.120] apply pos=(3649,2076) size=(0,0) after=(3649,2076,3861,2160)
               ↑ 收缩线程尾随的常规跟随带 SWP_NOSIZE 落在 endorsed 左缘 →
                 窗口=「endorsed 左缘+遮盖宽度」，右缘 3861 出屏 21px
```

时钟文字右对齐渲染 → 全部贴到屏幕最右缘（用户所见）；且变化检测元组记的是 NOSIZE 标志而非窗口实际尺寸，后续每针都判「无变化」→ **错误尺寸无限驻留**（实测卡 16.6s 直到下轮全屏的遮盖全尺寸应用才自愈）。卡死期窗口同时盖住采样条，采色归属校验全拒（`pre-check owned=false at (3822)` 连环）——该时段的色差与错位同源。修复：

| 改动 | 内容 |
|---|---|
| `GEOM_APPLY_LOCK` | 几何应用串行锁：relocate 收缩路径与 update 可见性路径互斥（锁序恒为 本锁→GEOM，note_probe 只取 GEOM 无反向依赖；relocate 拆成持锁本体 `relocate_geometry_locked` + 锁外调用 update，避免 std Mutex 不可重入死锁） |
| **弃用 SWP_NOSIZE** | update 常规/屏外/Unknown/Covered 无遮盖分支一律传 endorsed 全尺寸——显式尺寸让任何后写者（即使竞态残留）都在下一针自愈为正确几何；NOSIZE+变化检测的组合正是错误尺寸可驻留的结构性漏洞 |

**①②残余色差与收缩可察觉——同一根因：退出过渡期采样翻转**。00:56 真机日志 `appearance push seq=2~11` 全部 `src=z-reclaim`，在 `#F1E4DB↔#E7D8CD` 间来回推送：退出过渡期任务栏亚克力的透出内容正从视频切回壁纸，**真实任务栏本色本身在变**，退出窗口期的活跃采样（R8 的 z-reclaim + R7.6 的 visible-switch/mask-held-tick）采到并推送过渡色，覆盖层底色自己翻转=用户可见的偶现色差；收缩时刻的「可察觉」同源（左缘 42px 条带从过渡色切到稳态色的瞬间可见）。修复（R8.1 收敛采样时机）：

- **撤销退出窗口期全部活跃采样**（z-reclaim / visible-switch / mask-held-tick 三处调用移除）。退出期信任 15s 保鲜维持的稳态色（=进全屏前的本色，通常与收敛后稳态一致，全程恒定不翻转）；
- **收缩执行点三连采** 0 / 400 / **+1500ms（新增）**：原生归位即采、合成稳定再校、慢收敛亚克力兜底——世界稳定后才采纳新色。

### 3.10 R8.2 闭环色差校准（复验⑦驱动）

**复验⑦反馈**：①右缘出屏**不再复现**（R8.1 竞态修复生效）②仍有细微色差 ③**有时细微色差持续不恢复**。

**归因**：③是关键——「持续不恢复」说明偏差不在采样数据端（保鲜链每 15s 跟随任务栏变色，日志全是 `unchanged`，采样与推送自洽），而在**「推送色 → 屏幕呈现」之间**：候选源 = WebView2/DWM 对 CSS 颜色的呈现与任务栏原生绘制的恒定偏差、采样条（显示桌面）与时钟区的横向底色差等。这类偏差**开环链路结构性观测不到**（它只比较「采样值 vs 推送值」，两者自洽就永不推送）——正好解释「细微、持续、不恢复」。

**修复：闭环色差校准（servo）**。采样时同时读两个点：

- **外部点**（既有）：显示桌面条的任务栏本色，3 列×3 行；
- **内部点**（新增）：**覆盖层右缘内 2px**（padding 纯背景区，避开文字/keepalive 像素）的屏幕像素，三点逐通道中位——即**我们推送色的实际显示效果**。

`新推送 = 旧推送 + (外部色 - 内部色)`——内外之差就是用户肉眼所见色差本身，无论源自色彩管理还是横向底色差，闭环都把它收敛到 2 RGB 死区内（<2 不推送防抖）。内部点归属校验必须命中覆盖层自身 HWND（attach 时缓存；滑动跟随中/被盖时拒绝→该轮退化纯开环）。命令路径（初始/主题加载）同步 `LAST_PUSHED_BG` 保持闭环参照一致。诊断日志带四色值（`sampled=#.. inside=#.. pushed=#..`），偏差一旦出现可直接读数。

**部署验证（01:32:47）**：`appearance run src=attach: converged sampled=#E7D8CD inside=#E7D8CD pushed=#E7D8CD d=0`——内部点读数正常，此刻零偏差（复验⑦所见偏差为条件性出现，出现时闭环在一个保鲜周期内压掉）。待用户复验⑧。

### 3.11 R8.3 撤销收缩即时采样毒源（复验⑧驱动）

**复验⑧反馈**：「还是一样的现象，似乎没有改善」。

**取证两步**：
1. **稳态抓屏**（PowerShell CopyFromScreen + 逐列/逐行剖面）：覆盖层内部与任务栏条带色值**完全一致**（中带 R 通道两侧同为 231、B 同为 205；纵向仅 ±1.5 亚克力噪声、无梯度）——servo 与稳态色彩无罪，本轮 102 次闭环读数全部 d=0 是真的。
2. **用户测试时段日志**（按 attach 行号锚定，NTP 又移位一次）：退出快速连续测试时，`post-shrink-now`（R8 引入的 delay=0 即时针）屡次采到**过渡色**并推送——退出动画期亚克力正从「视频透出」过渡到「壁纸透出」，`#F0E4DB`（Δ9/12/14）与 `#E8D9CE`（Δ1~3，**正是用户说的「很细微」**）被推上覆盖层；随后的 400/1500ms 纠正针在连续测试中频频被下一次全屏盖住拒绝（`no sample`），色差驻留到 trusted-15s 兜底才恢复（实测最坏 ~6.5s）。**连续测试 = 每次退出重新投毒 + 纠正通道被堵 = 用户看到「持续不恢复」**。

**修复**：
- **撤销 `post-shrink-now`**（R8.1 撤销 z-reclaim 等三处的同款教训：过渡态不采样）——收缩后只保留延迟针 **400 / 1500 / 4000ms** 三连采（新增 4000ms 慢收敛兜底）；
- **退出观测窗内保鲜提速**：trusted 循环在 `exit_watch_until`（收缩后 6s）内从 15s 提速到 **3s**——连续测试停止后残留过渡色最迟 3s 纠正。

**部署验证**：attach 正常、闭环 `converged ... d=0`。待用户复验⑨。

### 3.12 R8.4 主参考迁移：条带降级（复验⑨驱动）

**复验⑨反馈**：①还是一样 ②**新观察（频率很低）：退出全屏后本来无色差，很快变成有色差，并一直保持**。

**取证**（本轮做了两个实验 + 日志重读）：
1. **宽域横向剖面**（x=2400..3840 逐列中位色）：任务栏亚克力底色确实存在**横向梯度**（238,224,201 → 231,216,205），但时钟区附近局部只有 1~2 RGB/170px，不构成可见色差；覆盖层与两侧邻居此刻完全一致。
2. **受控实验**（白色窗体垫在任务栏后面含条带区，前后对比剖面）：任务栏亚克力**不透出后面的窗口，只透壁纸**——「PotPlayer 窗口垫在后面导致 tint」假说**排除**。
3. **测试时段日志重读**：`#F0E4DB`（比壁纸 tint 亮 9~14）在条带上**稳态持续 4 秒+**且归属校验全部通过——它是「显示桌面」细条**自己的状态**（特殊交互元素：悬停高亮/材质态），不是主任务栏表面的颜色。而我们以条带为唯一参考，把这个分叉色平涂给整个覆盖层 → 「本来无色差（trusted 色当时正确）→ 采样采纳条带分叉色 → 与周围任务栏色差并保持」。频率低=取决于悬停/状态触发条件。

**修复（R8.4 主参考迁移）**：
- 主参考改**覆盖层左侧净列**（左缘外 2px 起每 8px 一列、最多 5 列；托盘归属 + 三点离散度 ≤6 过滤图标/高亮污染列；取最近净列三点均值）——与覆盖层下方是同一块连续任务栏表面，无条带的特殊状态；
- 条带降级为**回退**（左侧无净列时）+ **诊断对照**（日志双读数 `left=#.. strip=#.. (d=N)`，分叉可直读）；
- 闭环/命令路径消费主色，逻辑不变。

**部署验证**：`converged primary=#E9D9CC left=#E9D9CC strip=#E7D8CD (d=2) inside=#E9D9CC d=0`——双参考工作正常，left/strip 的 2 RGB 梯度差如实呈现。待用户复验⑩。

### 3.13 R8.5 渐变背景（复验⑩驱动）

**复验⑩反馈**：「还是有」。

**归因（R8.4 双读数日志的直接产物）**：稳态四色值显示覆盖层下方任务栏表面本身是**横向渐变**——左邻 E9D9CC、右邻 E7D8CD（跨覆盖层 170px 共 2~4 RGB；壁纸透出的固有效应，永在），亮态材质下更陡（F1E1D4→F1E4DA，d=6）。平涂单色必然与某一侧边缘差 1~4——对敏感眼即「细微色差一直保持」；且渐变态/平涂态间随材质切换，正是「先无色差→很快有色差」的观感来源。测试时段日志另证实任务栏整体在两种材质状态间 ~9 RGB 摆动（跟随滞后 1~3s），属瞬态。

**修复（R8.5 渐变双端点）**：
- 后端推送 `(bgLeft, bg)` 双端点（左=左侧净列，右=条带），闭环修正量同步双端（内部点配对右端；渲染偏差实测 0，均匀假设成立）；变化检测任一端 ≥2 即推；
- 前端 `linear-gradient(90deg, bgLeft, bg)` 渲染（bgLeft 缺省=平涂）；
- 命令响应补 `bgLeft`（serde camelCase）+ **listen 注册后重拉一次 invoke**——attach 首针事件可能早于监听注册（webview 加载竞态），错过即永缺（后续 d<2 不再推送）。

**部署验证（00:16 后抓屏）**：覆盖层内 3660→3800 逐列 (233,217,204)→(231,216,205) 平滑过渡，左缘配左邻（±1）、右缘配条带（0 差）——渐变已真实渲染，边缘色差压到物理极限。待用户复验⑪。

### 3.14 R8.6 恢复高频跟随（复验⑪驱动，录屏逐帧定案）

**复验⑪反馈**：「还是有」，并提供录屏（00:57:32，778×84@20fps，H.264 65kbps）。

**录屏取证（ffmpeg 抽帧 + 逐帧量化）**：
1. **稳态帧净列对比**（自动识别低散度列）：原生侧 E6D7CC vs 覆盖层 E5D6CB——压缩噪声级，**R8.5 渐变在稳态成功**；
2. **进出全屏边界帧**：覆盖层与原生任务栏瞬间分叉 **亮度差 7~24，持续 0.25~0.5s+**——每次进出都有爆发；
3. 结合此前日志（F1E1D4 稳态持续 4s+、双列同变）：**任务栏材质在全屏进出（及任意最大化窗口开/关）时整体切换 ±9~24 亮度**，两种状态都是表面真实颜色。

**认知修正（推翻 R8.1/R8.3 的「过渡毒色」结论）**：当时的翻转推送不是毒药——F1E1D4/F0E4DB 就是任务栏自己的材质状态，**不跟随才是色差来源**：撤销逐针采样后，每次材质切换我们的覆盖层要 400ms~3s 才跟上，这段滞后窗=肉眼可见的分叉爆发（录屏实测 |d| 达 24）。当时真正的 bug 是采样锚点坏（R8.0 已修）+ 无闭环无渐变；如今基础设施全部就位，跟随已无副作用（d≥2 死区自带节流，稳态零推送）。

**修复（R8.6 恢复高频跟随）**：
- `z-reclaim` / `visible-switch` / `mask-held-tick` 三处逐针采样恢复（R8.1 撤销、R8.3 维持撤销的那些时机全部回归）；
- **新增 `visible-tick`**：常规可见态每探针针（150~500ms）轻采一次——材质切换不只在全屏进出，任意最大化窗口开/关都会触发，15s 保鲜跟不上；
- 保留：左列主参考（R8.4）、双端渐变（R8.5）、闭环（R8.2）、收缩后 400/1500/4000ms（R8.3）、trusted 15s/3s 兜底。

**部署验证**：`visible-tick` 每针运行、稳态 d=0 零多余推送。待用户复验⑫。

**复验⑫（第二段录屏 02:14:26）**：①色差残留频率降低但仍在 ②**退出全屏后大部分时候仍能感知收缩「抖动」过程** → R8.7。

**复验⑬（最终）**：「**感觉跟上一版比没有明显变化**」——收缩动画化（4 步滑入）没有带来可感知改善。**结论：感知根源不是跳变的「锐度」，而是收缩过程本身的存在**——42px 的边界移动与材质色变同时发生，无论瞬移还是滑入都能被注意到。在「外部遮盖+采样跟随」架构内，收缩感知已无进一步压缩空间；根治只剩呈现层方案（§7）。**应用户要求，④优化线就此暂停。**

### 3.16 R8 系列阶段总结（暂停时点归档）

**本轮（R8~R8.7，八轮迭代）成果盘点**：

| 修复 | 状态 | 用户确认 |
|---|---|---|
| 退出后时钟跑到屏幕最右缘（几何交叉竞态+SWP_NOSIZE 驻留） | ✅ 根除 | 复验⑦「不复现了」 |
| 采样结构性不可用（锚点跟随全屏位形落进遮盖内部） | ✅ 修复 | — |
| 采样污染（三道闸：全点归属+读后复验+离散度） | ✅ 修复 | — |
| 推送乱序/旧值覆盖（单 worker+seq 版本化+前端守卫） | ✅ 修复 | — |
| 显示偏差不可见（闭环 servo：内部点反馈修正） | ✅ 工作（稳态 d=0） | — |
| 底色渐变平涂差（双端点 linear-gradient） | ✅ 修复（抓屏实证 ±1） | — |
| 材质切换跟踪滞后 0.4~3s（高频跟随恢复） | ✅ 改善 | 复验⑫「频率没之前那么高」 |
| **材质切换瞬间 150~500ms 采样滞后窗 ±9~24** | ⚠️ 架构下限 | 复验⑫⑬「残留」 |
| **死区 1~2 RGB（d<2 不推送）** | ⚠️ 架构下限 | — |
| **收缩过程本身可感知（42px 边界移动+材质色变同发）** | ⚠️ 架构极限 | 复验⑬「动画无改善」 |

**架构极限的物理刻画**（复验⑨⑪两段录屏+日志定案）：
1. 任务栏底色不是常数：壁纸透出固有横向渐变（±2~4）+ 材质双态切换（全屏进出/最大化窗口开关，±9~24，秒级）；
2. 「采样跟随」必有滞后：感知→采样→推送→渲染 ≥150~500ms，材质切换瞬间必有一段分叉可见；
3. 遮盖边界移动本身可感知：无论跳变还是滑入，42px 的边界位移伴随材质色变，肉眼可辨（R8.7 实证滑入不解决）。

**根治唯一路径**：呈现层改造——「固定文字窗+原生绘制背景/遮盖窗口」（背景换色走原生 GDI/DWM 路径不经 WebView、文字窗口恒定几何不随遮盖伸缩）或「抑制原生时钟绘制」可行性研究。见 §7。

### 3.17 复审复核：两项确定性代码缺陷成立，暂停定案修订（2026-09-12）

> 复验⑬归档后，另一 agent 提交 `REVIEW-R8.7残余问题根因与根治边界-2026-09-12.md`。本节为逐条对照源码与证据帧的复核结论。

**复核确认的确定性缺陷（代码可证，非架构极限）**：

1. **渐变左端点不跟随新采样**（评审 §2，`run_appearance_sample`）：`cand_left = prev_left + corr`，本轮采样的 `left` 完全不参与计算 → `cand_left - cand_right ≡ prev_left - prev_right`，渐变斜率冻结在上次推送。左端变亮而右端不变时 corr=0 → d=0 → 误报收敛，左端偏差**永久保留**。日志旁证：seq=64 推送 bgLeft=EDDED2 而当轮采样 left=E8D8CB——推送了从未观测到的捏造值。**可解释「偶发、细微、持续不恢复」。**
2. **inside=None 冻结全部更新并误报收敛**（评审 §3）：R8.5 重写丢失了 R8.2 的 `None → 采样本色` 回退，现为 `corr=0 → cand=prev → d=0 → converged`。内部点被拒（恰发生在退出/快速再入等关键时段）时新色全部丢弃还记「收敛」。注释声称「退化纯开环」与实现不符。
3. **条带经 corr 染动整体**（评审 §4）：`right = strip.unwrap_or(primary)` 且 corr 同步双端——条带特殊状态（R8.4 已证实存在）仍会把主体底色带偏，R8.4 的规避被 R8.5 渐变绕开。
4. **闭环无呈现版本绑定**（评审 §5）：`corr = 本轮采样 - 屏幕当前读数`，若上一次推送尚未呈现，误差被重复累计（理论过冲，实际频率待测）。

**复核确认的静态风险**（评审 §6）：SHRINK_ANIMATING 在锁释放后才置位（存在竞态间隙）；动画无每步取消机制（快速再入时滑向旧终点）；R8.7「过程必然可感知」的归纳超出单次实验证据。

**复核确认的实质优化方向**（评审 §7）：CSS 百分比渐变随窗口宽度**重映射**——mask 期窗口左缘显示的是 3635 列采样色而非 3607 处真色，收缩时整段背景重新拉伸。修复=背景按屏幕坐标定义（payload 带参考 x，`background-size/position` 固定映射，窗口移动/缩放只裁切不拉伸）——**现架构内的实质改进，不呈现层重写**。

**证据帧核实**：`review-evidence-20260912/38.7s.png` vs `38.9s.png`——时钟区浅色矩形块与左邻的竖向接缝在 0.2s 内出现并消退，文字无位移。证实色差主体是**背景色事件**，几何（文字位移）不是主体。视频规格勘误：832×274、74.8s、H.264 ~369kbps（此前记录 836×245/20fps 有误）；该录屏对应 R8.6（动画引入前），不能用于评价 R8.7。

**定案修订**（采纳评审 §11）：原「残余三项均非实现缺陷、已达架构极限、唯一根治=拆原生背景」**措辞过强，予以修订**——几何/采样可靠性问题确已修复，但**颜色算法存在上述可直接修复的确定性缺陷**，「采样跟随架构极限」的结论在缺陷修复前不成立；「固定文字窗+原生背景」降级为**条件性原型方向**（其收益=文字恒定几何/背景独立绘制/两阶段耗时可观测，不承诺根治，且不替代先修颜色算法）。用户暂停决定维持不变。

**恢复时的 R8.8 修复包（按序，全部为架构内小改动）**：
1. 双端直接更新：`cand = 本轮采样`（左端采纳新观测、右端消除积分累积；渲染偏差实测为 0，servo 移除后 inside 保留为诊断日志）——同时修复缺陷 1/2/4；
2. 动画加固：SHRINK_ANIMATING 持锁内置位 + 每步检查 GEOM（重新 Covered 即中止让位）；
3. 屏幕坐标渐变（背景裁切不重映射）；
4. 实验验证：合成输入单测（右不变左变/inside 缺失/条带高亮三个用例）+ 真机对照。

### 3.18 R8.8 颜色算法修复包实施（暂停后恢复，2026-09-12）

> 用户指示「继续吧」，按 §3.17 的 R8.8 修复包实施。前端有改动，`pnpm build` + cargo 构建。

**实施内容**：
1. **双端直接更新**：`appearance_update` 纯函数（候选=本轮双端采样，d=与上次推送最大通道差）替换旧闭环公式——同时修复复审缺陷 1（左端点冻结）与 2（None 误收敛冻结），并消除积分累积（缺陷 4）。`inside` 读数降级为诊断量：日志输出 `insideErr=内部色-推送右端`，非零即渲染偏差告警（部署实测恒 +0,0,0——证实「渲染偏差为 0」假设，servo 移除无损）。
2. **动画加固**：SHRINK_ANIMATING 持锁内置位（修复审 §6.3 锁释放后置位的竞态间隙）；滑入每步核对 GEOM——重新 Covered/新遮盖需求立即中止让位，由正常流程下一针收敛（修复审 §6.2 滑向旧终点）。
3. **屏幕坐标固定列采样 + 屏幕锚定渐变**（复审 §7）：左参考列从「窗口左缘相对」改为**固定屏幕列**（右缘 -175~-200px 六候选，离散度≤6 过滤），跨遮盖/常规两态位置不变；前端渐变图 198px 宽、自右缘 -8px 锚定（`backgroundSize: 198px 100%; backgroundPosition: right -8px top 0`），窗口移动/收缩只**裁切**不重映射；bgLeft 兜底色填补渐变图左侧未覆盖段。
4. **单测**：`appearance_update` 4 例——左端变化必须进入候选（复审 §2 原始反例）、候选恒等于本轮采样、零变化零推送、渐变斜率变化两端独立跟随。全部通过。

**实施插曲**：左列候选窗初版取右缘 -190/-195/-200px，实测全被图标污染（`left=none`，好列在 3644/3639）——修正为 -175~-200px 六候选后恢复。

**部署验证（03:28）**：`appearance run src=visible-tick: converged right=#E7D8CD left=#E8D9CC strip=#E7D8CD (d=1) inside=#E7D8CD (err=+0,+0,+0) pushed=(E8D9CC|E7D8CD) d=0`——固定列采样、双端直接更新、渐变渲染、诊断链全部工作。待用户复验⑭。

### 3.19 R8.9 分歧门 + keepalive 提速（复验⑭录屏取证，三机制定案）

**复验⑭反馈**：「还是一样」，附第三段录屏（10:52:01，832×274@30fps，401kbps——R8.8 版本）。

**录屏+日志逐帧定案——残余现象实为三个独立机制叠加**：
1. **左固定列污染推假渐变**：10:51:36.623 `push seq=103 bgLeft=#F5E6DC left=#F5E6DC strip=#E7D8CD (d=15)`——左列（固定屏幕列 3619~3629，恰在托盘图标区）读到均匀亮 15 的值（悬停高亮类平面高亮可通过离散度过滤），与条带同时读数矛盾，却推了一个陡峭假渐变=**「亮色块+竖向接缝」**。此前 38.7s 证据帧的浅色块同源。
2. **左右分歧期翻转推送**：10:52:05.1-05.5 四次推送（seq=120~123）在 ECE6E1/E3DCD6/E9E7E5/E2DBD4 间翻转，left/strip 分歧 d=8~25——过渡期两采样区读数互相矛盾，翻转推送=背景闪烁。
3. **几何抖动致 WebView 透明帧**：视频 f10 帧（逐帧量化 overlay 缺失、原生注册表时钟「10:52/周六 2026-9-12」可见）——该时刻日志窗口位置/z 序/topmost 全部正常，说明覆盖层窗口在位但 **WebView 表面呈现了透明帧**（几何 212↔170 抖动+半秒 8 次推送，渲染器丢帧/表面重分配未及时呈现）。这才是「时钟消失/跳动」感知的主体，与色差叠加。

**修复（R8.9）**：
- **分歧门**：left 与 strip 同时在场且通道差 >8 时**不推送**（保留上次可信色，日志 `diverged`）——两列是同一连续表面的邻居，正常应一致（基线 1~6，材质整体切换时同步移动）；大分歧=一侧特殊状态，覆盖层下方真色不可知，宁可不跟。同时封死缺陷 1（假渐变）与缺陷 2（翻转推送）两类污染路径；
- **keepalive 200ms→100ms**：几何抖动期的 WebView 透明帧时长减半。

**部署验证（11:07）**：`converged right=#E2DBD4 left=#E9DACC strip=#E2DBD4 (d=8) inside err=+0,+0,+0`——白天暖色下分歧门内正常跟随。待用户复验⑮。

### 3.20 R9.0 平移式遮盖（复验⑮录屏定案：透明帧是残余主体，根治 resize）

**复验⑮反馈**：「还是有问题」，附第四段录屏（13:44:53，R8.9 版本）。

**录屏逐帧定案**：R8.9 后色差块/翻转推送已被分歧门压制，**残余主体是「覆盖层整块消失、原生注册表时钟透出」**（6.1s/14.1s 等帧，实测单次持续 4 帧 ≈130ms，每次全屏切换出现）——逐帧量化+日志对齐证实：消失帧时刻窗口位置/z 序/topmost **全部正常**，覆盖层窗口在位但 **WebView2 表面呈现透明帧**。根因=**窗口宽度反复变化（遮盖 212↔常规 170）迫使 WebView 表面重分配**，快速切换期间渲染器丢帧/新表面未及时呈现；keepalive（100ms 强制重绘）只能缓解不能根治——渲染器丢帧时 React 再怎么触发也没有新帧。

**修复（R9.0 平移式遮盖，移除全部 resize）**：遮盖不再「放大到 212 并集」，改为**认可矩形等宽平移**到全屏位形左缘：
- 全屏位形时钟宽 151~163px < 认可宽 158~170px——窗口 (native.left, 2076, 认可宽, 84) 即可**完整盖住**全屏位形时钟；并集多出的 3770..3819 段只是空任务栏，无需覆盖；
- 窗口尺寸**全程恒定**（170×84），全屏进出=纯平移（3607↔3649），零 resize → 零表面重分配 → 零透明帧；
- 三处 mask 生成点同步改为平移矩形；R8.7 滑入动画改为恒宽纯平移滑入；
- 前端渐变回退为 100% 映射（恒定宽度下映射天然稳定；R8.8 的屏幕坐标锚定废弃——其 CSS 像素换算在 175% DPI 下本就有偏差）；
- 代价（有意接受）：回摆期平移跟踪存在 ≤~220ms 的 42px 原生残条暴露窗（R8.7 滑入动画同样适用，表现为 42px 滑动而非整块消失）；endorsed 窄变体（158px）+ 全屏宽变体（163px）组合有 5px 边缘差——均远小于原透明帧整块消失。

**部署验证（14:07）**：attach/采样/渐变全部正常。待用户复验⑯（重点：全屏切换时时钟是否还整块消失）。

### 3.21 R9.1 原生遮盖条（fixed-WebView 两窗分离架构）

**复验⑯反馈（第五段录屏 14-11-32，R9.0 版）**：色差频率/幅度下降 ✓，但**「退出全屏后时钟有一个明显从左往右移动的过程」**——R9.0 平移的代价兑现：平移移动的是**含文字的整个窗口**，42px 滑动平滑且必然可感知；而更早 resize 方案的代价是 WebView 透明帧。两方案共同根源=**遮盖职责落在 WebView 窗口上**（宽度变化→表面重分配透明帧；位置变化→文字滑动）。

**修复（R9.1 两窗分离——复审 §6.1「固定文字窗+独立背景」的最小形态）**：
- **WebView 覆盖层恒定于 endorsed 位**（3649..3819）：零移动、零 resize、零动画——文字永不滑动、表面永不重分配，此前两大残余同根除；
- **原生遮盖条**：纯 Win32 实色小窗（`liCalClockCover` 类，WM_PAINT GDI 填色），全屏位形下亮出于 (native.left, endorsed.top)→(endorsed.left, endorsed.bottom)——恰好盖住原生时钟超出覆盖层左缘的部分（42/31px）；
- 遮盖条颜色=渐变左端采样色（`cover_set_color` 随推送联动），与覆盖层左缘无缝；z 序与覆盖层同策略（covered 同 dive）；分歧门（R8.9）继续把守污染；组件关闭同步隐藏；
- 遮盖条+覆盖层合计恰为旧并集（3607..3819）——**回摆期双位形同时覆盖能力保留**；
- R8.7 滑入动画/SHRINK_ANIMATING 门/apply_mask_geometry 的覆盖层几何路径整体退役（收缩=隐藏遮盖条一个动作，零几何变化）。

**部署验证（15:18）**：attach/采样/渐变正常，单测 4 例通过。待用户复验⑰。

### 3.22 R9.2 z 自愈（复验⑰澄清驱动：覆盖层持续消失=被埋非闪现）

**复验⑰反馈+澄清**：R9.1 下测试几次全屏退出后，「我们的时钟直接没了，变成了系统原生时钟」**持续存在**（非闪现，需重启应用恢复）；重启是用户主动退出（应用未崩溃，事件日志无崩溃记录）。

**定位**：进程存活、日志活跃（appearance 持续 converged）、几何日志全部正确——覆盖层窗口在位、状态机正常，但屏幕上不可见。结合录屏中任务栏图标行变化（^ 溢出图标消失、新图标出现=**任务栏重排**），定位为：**Shell 重排（全屏进出/图标增减触发）会重申任务栏在 topmost 带内的槽位，可能把覆盖层压到任务栏之下**；外部 z 扰动不改变状态五元组 → 变化检测永不重申 → 覆盖层被埋直到重启。此洞在历史版本同样存在（此前 steady 态从无重申机制），此前部分「偶现色差/时钟异常」报告可能实为被埋。

**修复（R9.2 z 自愈）**：常规可见态（below==-1）每针 `WindowFromPoint(覆盖层中心)` 自检归属——根窗口非本窗口即被埋 → 重申 `HWND_TOPMOST` + 清 `LAST_FOLLOW_POS`（下针全量重申）+ 日志 `z self-heal`。附带**渲染停滞鉴别日志**：未被埋但右缘 padding 实际像素与推送右端差 >12 → 记录 `surface stall?`（区分被埋与渲染器丢帧两种失败，为下一步 GDI 回退时钟决策提供依据）。

**部署验证（16:01）**：attach/采样正常。待用户复验⑱（重点：连续全屏退出后时钟是否还持续消失；若再现，日志 `z self-heal`/`surface stall?` 会直接给出归属）。

### 3.23 R9.3 TDR 自愈看门狗（复验⑱录屏+WER 取证：显示驱动资源超时实锤）

**复验⑱反馈**：「还是会消失」+ 录屏（16-38-48，R9.2 版）显示：退出全屏后**我们的时钟彻底消失、原生时钟持续显示**（分钟级不恢复）。

**终极取证——WER 事件日志**：`LiveKernelEvent 0x1cc（ResourceTimeout，显示驱动资源超时）`，**3 天内 9 次、签名完全一致**（09-09 22:33 → 09-12 16:40，含 00:27/10:56/12:36/16:40 等多次，与全部测试时段吻合）——**GPU/显示驱动资源超时（TDR 类）**，PotPlayer 全屏切换的高 GPU 压力触发。

**机制闭环（所有现象归一）**：TDR 发生 → 显示栈重置 → WebView2 的 GPU 表面丢失且渲染管线卡死 → 覆盖层整块透明、原生注册表时钟透出（=「我们的时钟没了变原生」）→ **重探线程卡死在 DWM/GPU 调用上**（诊断转储停更、日志静默——16:35:37 起 7 分钟全静默，appearance worker 卡在 GetPixel）→ 一切自愈失效（卡死的线程无法执行任何自愈）→ 直到用户重启应用。此前多轮「偶现色差/消失」报告中部分即 TDR 所致。WER 转储：`C:\WINDOWS\LiveKernelReports\ResourceTimeout\*.dmp`（P1=1cc，签名跨天一致）。

**修复（R9.3 TDR 自愈看门狗）**：独立看门狗线程（不触 GPU 调用）每 5s 检查 diag 转储新鲜度——静默 >20s（重探线程被 TDR 卡死的信号）→ 隐藏/重显覆盖层窗口 + `location.reload()` 强制刷新渲染管线，尝试唤醒；每轮重置新鲜度基线（仍卡死则 5s 后重试）。**边界说明**：若 WebView2 已被 TDR 完全打死，reload 可能无效——那时唯一恢复手段是重启应用或等 GPU 空闲，属环境级限制（GPU 驱动检查/更新是根本缓解）。

**部署验证（16:52）**：attach/采样正常，tdr-watchdog 特征串确认。待用户复验⑲。

**已知遗留（诚实记录）**：WebView2 渲染器在 PotPlayer 全屏切换期间仍可能丢帧出现**短暂透明帧**（与遮盖条无关的 GPU 争用，外部因素）——若复验⑰仍可见，下一步=遮盖条升级为 **GDI 回退时钟**（原生窗口直接 GDI 绘制时间文字，渲染停滞期间替代 WebView 显示，免疫 GPU 争用）。

### 3.21 R9.1 原生遮盖条（fixed-WebView 架构，复验⑯驱动）

**复验⑯反馈**：「时钟有一个明显从左往右移动的过程」+ 色差仍有残留——R9.0 平移方案的代价兑现：**平移移动的是整个窗口（含文字）**，42px 的滑动平滑可见；而旧 resize 方案的代价是 WebView 透明帧。两个方案各有一个不可接受的副作用，共同根源=**遮盖职责落在 WebView 窗口上**。

**修复（R9.1 两窗分离——复审 §6.1「固定文字窗+独立背景」的最小形态）**：
- **WebView 覆盖层恒定于 endorsed 位**（3649..3819）：零移动、零 resize、零动画——文字永不滑动、表面永不重分配（透明帧与滑动两大残余同时根治）；
- **原生遮盖条**：一个纯 Win32 实色小窗（`liCalClockCover` 类，GDI WM_PAINT 填色），全屏位形下亮出于 (native.left, endorsed.top, endorsed.left-native.left 宽)——恰好盖住原生时钟超出覆盖层左缘的部分（42/31px）。显隐即全部切换动作，零几何成本；
- 遮盖条颜色=渐变左端采样色（`cover_set_color` 随推送联动），与覆盖层左缘无缝衔接；分歧门（R8.9）继续把守污染；
- z 序与覆盖层同策略（covered 同 dive、常规 topmost）；组件关闭同步隐藏；
- R8.7 滑入动画/SHRINK_ANIMATING 门/apply_mask_geometry 的覆盖层几何路径整体退役（收缩=隐藏遮盖条一个动作）。

**架构意义**：遮盖条+覆盖层合计恰为旧并集（3607..3819），回摆期双位形同时覆盖能力保留；WebView 侧从此**恒定几何**——复审 §6.1 预言的「文字恒定几何/背景独立绘制」以最小形态落地。

**部署验证（14:49）**：attach/采样/渐变正常，单测 4 例通过。待用户复验⑰。

### 3.15 R8.7 收缩动画化（复验⑫驱动，第二段录屏）

**复验⑫反馈**：①色差残留频率降低但仍在 ②**退出全屏后大部分时候仍能感知时钟收缩「抖动」过程**，并附第二段录屏（02:14:26，836×245）。

**录屏+日志对齐取证**（视频 32.9s 处分钟翻转 02:14→02:15 对齐日志 02:15:00）：
1. 稳态色差：录屏时段日志 `pushed=(E9D9CC|E7D8CD)` 全程收敛 d=0——R8.6 高频跟随工作正常，色差残留=材质切换瞬间 ~150-500ms 的采样滞后（追踪物理下限）+ 死区 1~2 RGB，频率降低与此吻合；
2. **收缩抖动**：02:15:16~35 日志显示 19 秒内 **7 次 covered↔shrink 循环**（用户快速切换全屏），每次循环=遮盖左缘 42px 两次瞬移（展开 -42 / 收缩 +42）。时机已是机械极限（读到归位 1~6ms 落位）——**感知问题不在时机，在「跳变」本体**：42px 瞬移永远可感知，快速平滑滑入则弱化得多。

**修复（R8.7 收缩滑入动画）**：
- 遮盖收缩（左缘左移 ≥12px）不再瞬移落位：锁内只清状态并返回动画需求，锁外 4 步 × ~25ms（≈100ms）滑入，每步 `SetWindowPos([x, right])`，右缘/高度恒定；
- **安全性**：收缩仅在 UIA 读到原生=endorsed 后发生，中间矩形 [x, right] 恒 ⊇ endorsed——原生时钟全程零暴露；
- `SHRINK_ANIMATING` 原子门：动画期间 update/relocate 的几何应用让位（防 150ms 探针针打断滑入），终态由动画精确落位 + wrapper 的 update 收敛 LAST_FOLLOW_POS；
- 展开方向保持瞬发（残块暴露优先级最高，动画会引入暴露窗）。

**部署验证**：attach 正常、visible-tick 每针收敛。待用户复验⑬（重点：快速切换全屏时的收缩观感）。

**评审其余建议的处置**：UIA 分段计时/元素复用（评审 §5）未做——留作数据驱动项；固定文字窗+独立背景层、抑制原生绘制研究（评审 §6）未做——仅当颜色链修完仍有呈现层残余再启动； endorsed 包含关系表述已按评审修正（宽值⊇窄值，锁窄值时遮盖靠 union 保证，8s 采纳门期间的正常态左缘细条暴露仍为观察项）。

## 4. 现状（R8.7 后，应用户要求暂停）

| 项 | 状态 |
|---|---|
| 代码 | R8 `dbc2b61` → R8.1 `87bc06a` → R8.2 `6a62b6f` → R8.3 `67dea3b` → R8.4 `40eac93` → R8.5 `2ee6f35` → R8.6 `91adc8f` → R8.7 `e15440f`（分支 squashed，未推送远程） |
| 部署 | `D:\Program Files\li-calendar\liCalendar.exe` 为 R8.7 构建（cmp 字节一致校验），进程正常运行 |
| ④项效果 | 几何竞态/采样链/闭环/渐变/高频跟随/收缩动画全部落地：右缘出屏根除（用户确认）、色差残留频率明显降低、稳态边缘色差 ±1 RGB；**残余=材质切换瞬间 150~500ms 分叉窗 ±9~24 + 死区 1~2 + 收缩过程本身可感知**——均为「采样跟随+外部遮盖」架构极限 |
| 调试设施 | `D:\agents_tmp\clockrect_debug` 标记**当前已开启**；appearance 日志带 `left/strip/inside` 多值读数（`left/strip (d=N)` 分叉直读）；取证脚本 `measure_seam.ps1`（录屏逐列剖面） |
| 工具脚本 | `clock_baseline_probe.ps1`、`frame_capture.ps1`、`capture_with_potplayer.ps1`、`measure_seam.ps1`（均在 `D:\agents_tmp`） |
| 录屏证据 | `C:\Users\Mozart\Documents\Captura\2026-09-12\00-57-32.mp4`（复验⑨）、`F:\Software\2026-09-12\02-14-26.mp4`（复验⑫） |

## 5. 遗留问题清单（按优先级）

1. **④项残余（已暂停，架构极限）**：材质切换瞬间 150~500ms 分叉窗（±9~24）+ 死区 1~2 RGB + 收缩过程可感知（R8.7 滑入无改善）。恢复时的唯一根治路径=**呈现层改造**：原型「固定文字窗（恒定几何，WebView 只画文字）+ 原生绘制背景/遮盖窗口（GDI/DWM 换色，不经 WebView）」，或「抑制原生时钟绘制」可行性研究（评审 §6.2）。动工前先读 §3.16 架构极限刻画与 §7。
2. **y=2071 垂直漂移**（独立疑点，未归因）：仅自动隐藏任务栏场景可触发，疑点在 `read_tray_state` 跟随 dy 路径。
3. **endorsed 3660↔3649 摆动**（观察项）：时钟矩形随时间文字宽度微变；宽值⊇窄值，锁窄值时遮盖靠 union 保证，采纳门 8s 期间正常态左缘 ~11px 原生细条可能短暂可见——当前无害，继续观察。
4. **UIA 延迟优化**（评审 §5，数据驱动）：分段测量 UIA 耗时，若主要成本是重复树查找则做专用 COM 线程元素复用/属性变化事件唤醒。
5. **Phase 2 输入接管未开始**（主线下一阶段，见 PLAN §3；安全顺序：先加 `OVERLAY_TAKEOVER` 开关部署验证、后翻默认值）。

## 6. 残余感知来源分析（R8.7 终版——两段录屏+全程日志定案）

| 残余现象 | 机制 | 架构内可否消除 |
|---|---|---|
| 材质切换瞬间色差（±9~24，150~500ms） | 任务栏材质双态整体切换；采样跟随必有感知→采样→推送→渲染滞后 | ❌ 滞后可压缩（更高频采样）但不可归零；已近下限 |
| 1~2 RGB 底噪 | 死区（d<2 不推送）+ 渐变线性近似与真实表面的曲率差 | ❌ 死区防抖动必需；1~2 RGB 属不可感知边缘 |
| 收缩过程可感知（复验⑬：滑入无改善） | 42px 遮盖边界位移+材质色变同时发生，过程本身可辨 | ❌ 瞬移/滑入已双试；动画参数调优无意义 |
| 回摆期多次展开/收缩循环 | 退出动画期探测 Covered↔Visible 抖动（R6 设计：逐读数对齐） | ⚠️ 可加收缩去抖（牺牲贴边时机）——与用户「早点缩」诉求冲突，未做 |

**遮盖存在性约束（维持）**：原生偏离认可位的每一刻必须有遮盖，扩展窗总时长=原生偏离窗总时长，这部分不可压缩。**结论修订（§3.17 复审）**：原判「残余均非实现缺陷、架构固有代价」**过强**——颜色算法存在已证实的确定性缺陷（左端点不跟随采样、inside=None 误收敛、条带经 corr 染动整体、渐变随窗口宽度重映射），修复前不能把残余归于架构极限；详见 §3.17 与恢复时 R8.8 修复包。

## 7. 若将来恢复，建议的方向（按根治程度排序）

> R8.7 后顺序曾按「呈现层为唯一根治」排列；§3.17 复审后修订：**先修颜色算法确定性缺陷（R8.8 修复包，见 §3.17），再评估剩余，最后才考虑呈现层。**

0. **R8.8 颜色算法修复包（恢复时第一步，架构内小改动）**：双端直接更新（修左端点冻结/None 误收敛/积分累积）→ 动画取消与锁内置位加固 → 屏幕坐标渐变（裁切不重映射）→ 合成输入单测+真机对照。
1. **呈现层改造原型（条件性方向，非「唯一根治」）**：恒定几何文字窗 + 原生绘制背景/遮盖窗口。真实收益=文字恒定几何、背景独立绘制、两阶段耗时可观测；不承诺根治，且不替代 R8.8。
2. **「抑制原生时钟绘制」可行性研究**：本机 uia-class 无经典 HWND，需先研究受支持可恢复的抑制方式；无法证明则止步。
3. **收缩观感微调**（低成本，预期有限）：滑入参数扫描；回摆期收缩去抖（与「早点缩」诉求冲突需先确认）。
4. **UIA 延迟优化**（独立收益）：分段测量后元素复用/事件唤醒。
5. 直接进 Phase 2 输入接管（主线价值更大）。

## 8. 教训与方法论（本轮新增）

- **用户的感知描述要精确到「哪个动作的时刻」再归因**。R4/R5 两轮弯路都源于把「还是能察觉到」默认理解为「收缩动作明显」，实际是「收缩开始时刻晚」。一句澄清省两轮构建部署。
- **过滤日志必须用 `^\[HH:MM:` 前缀锚定**：`menu_dbg.log` 里有无时间戳的历史行，裸字符串比较会误捞（`"clockrect:" >= "[23:1"` 为真），曾把已废弃的 exit-pending 旧实验日志当成现行状态机输出。
- **过滤视图会制造假象**：排除 3660 读数后看到的"回摆"缺少归位侧信息，第一次归因（R4→R5 时的"反复横跳"叙事）不完整；拉全量读数序列后才看到"归位→回位→归位"的完整往返结构。
- ffplay（无退出动画）复现不了回摆，只能验机制；真机现象以用户描述+日志为准。
- **sleep 参数 ≠ 端到端量化周期**：探测本身有耗时（本机 uia-class 每针 ~65-75ms，Win11 26200 无经典时钟窗口、HWND 快速路径恒不命中），真实量化 = sleep + 探测 ≈ 220ms 而非 150ms。R7 起探测日志带 `src=`/`ms=`，时延归因不再靠猜。
- 构建产物在 `src-tauri\target\release\li-calendar.exe`（在 `src-tauri` 目录下构建）；部署后重启前清理 WebView2 进程**须限定本应用相关进程**（按命令行含 li-calendar 过滤，勿全局杀 msedgewebview2——会影响其他 WebView2 应用，评审 §7 指正）。

**R8 系列新增教训**：

- **「毒色」误判教训（R8.1/R8.3 → R8.6 翻案）**：把材质切换时采到的 F1E1D4/F0E4DB 判定为「过渡毒色」并撤销跟随，实际那是任务栏表面的真实材质状态——录屏逐帧分析证明撤销跟随制造的分叉滞后（0.4~3s，|d| 达 24）才是用户可见的色差。撤销一个机制前，先确认被撤销行为所响应的现象是真污染还是真变化。
- **录屏是色差类问题的最强取证**：两段 20fps 录屏（即使 65kbps 重压缩）比多轮日志推理更快定案——逐帧量化直接给出分叉的幅度/时刻/持续，避免在采样机制里空转猜测。配 ffmpeg 抽帧 + PowerShell 逐列剖面（注意先 `SetProcessDPIAware`）。
- **受控实验先于机制假设**：R8.4 用白窗体垫任务栏后的对照实验一枪否证「窗口垫底染色」假说（亚克力只透壁纸），避免朝错误方向写代码。
- **采样点位是特殊 UI 元素时要警惕**：「显示桌面」细条有自己的悬停高亮/材质态，其颜色可与主表面分叉（复验⑨ F0E4DB 实测）——参考点应选无交互语义的纯表面区域。
- **日志自证自洽 ≠ 屏幕无差**：R8.2 前开环链路采样与推送自洽（全 unchanged）但用户见色差——闭环（读自己渲染结果的屏幕像素）才能观测显示端偏差。
- **平涂 vs 渐变**：任务栏底色是横向渐变（壁纸透出），单色平涂必与一侧边缘差 1~4 RGB——「细微色差一直保持」的恒定来源，双端点 linear-gradient 是正解。
- **动画化不一定降低感知**（R8.7）：当可感知的是「过程存在本身」（边界位移+色变）而非其呈现形式（跳变锐度）时，平滑化无效。先分辨感知对象再选对策。
- **录屏文件名时间与日志对齐技巧**：用视频内容中唯一可锚定的事件（如分钟翻转 02:14→02:15）对齐日志时间轴，再按帧号推算各现象的日志时刻（防 NTP 撞车仍需行号锚定复核）。

## 9. 提交链速查

| commit | 内容 |
|---|---|
| `c2eeed1` | R1/R2/R3 + 毫秒时间戳日志 + 文档（前置会话） |
| `14ebbce` | R4 归位即缩（单次确认+150ms 轮询） |
| `f4a9b6e` | R5 单周期化（settle 门 1.2s，方向错误，核心已撤销） |
| `b66cfb3` | R6 实时跟踪：逐读数对齐+回摆证据保留+轮询节奏函数 |
| `07f80fa` | R7 评审驱动修复：退出观测窗+即时扩展+原子应用+src/ms 日志 |
| `68d4b38` | R7.1 Covered 相位即时展开（修扩展暴露窗 150~300ms） |
| `7c0aa73` | R7.2~R7.4 位形记忆预测展开+ATTACHED 门+死循环根除 |
| `f9831e0` | R7.5 底色动态跟随（收缩后延迟采样+emit 前端换色+归属校验） |
| `cf6a2f8` | R7.6 换色时机三层提前：Visible 切换点/遮盖在场每针/收缩后兜底 |
| `dbc2b61` | R8 色彩链可靠性重构（R7.6 评审驱动）：采样锚点稳定化+采样三道闸+单 worker 有序推送+seq 版本化+正常态 15s 保鲜 |
| `87bc06a` | R8.1 几何交叉竞态根除（GEOM_APPLY_LOCK+弃 NOSIZE，修右缘出屏卡死【复验⑦确认根除】）+撤销退出窗口期采样 |
| `6a62b6f` | R8.2 闭环色差校准 servo：内部点（覆盖层实际显示色）反馈修正，收敛 <2 RGB |
| `67dea3b` | R8.3 撤销收缩即时采样（400/1500/4000ms 三连采+退出窗内保鲜 3s） |
| `40eac93` | R8.4 主参考迁移：左侧净列（离散度滤图标）为主，条带降级回退+诊断双读数 |
| `2ee6f35` | R8.5 渐变背景双端点（linear-gradient 复现任务栏横向渐变；serde camelCase+listen 竞态修复） |
| `91adc8f` | R8.6 恢复高频跟随（录屏逐帧定案推翻「毒色」结论：visible-tick 等四时机+d≥2 死区节流） |
| `e15440f` | **R8.7 收缩动画化（最终版）**：遮盖收缩左移≥12px 时 4 步×25ms 滑入（中间态零暴露，SHRINK_ANIMATING 门防打断）；复验⑬无感知改善，暂停 |
| `81682e7`~`2d89c12` | 各轮文档入库（R7.6 评审文档、总结 §3.8~§3.16、STATE 同步） |

关键代码位置：`src-tauri/src/window_manager/windows/clock_overlay.rs`（状态机/遮盖/实时跟踪/色彩链/收缩动画）、`src-tauri/src/windows_hook/mouse_hook.rs`（重探线程轮询节奏）、`src-tauri/src/windows_hook/clock_window.rs`（UIA 探测 → `note_probe` 喂状态机）、`src/windows/ClockOverlayWindow.tsx`（渐变渲染+seq 守卫）。
