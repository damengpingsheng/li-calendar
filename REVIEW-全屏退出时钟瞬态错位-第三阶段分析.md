# 全屏退出时钟瞬态错位：第三阶段评审记录

评审日期：2026-09-08（本会话环境日期）  
评审基线：`c7a4bec` 及其之前的实现  
对象：`DEBUG-全屏退出时钟瞬态错位.md`，重点为 §0.6 与 N1 候选方案。

本记录基于文档、当前代码和 `D:\agents_tmp\menu_dbg.log` 的只读检查；本轮未运行全屏复现，未修改应用代码。日志可能继续追加或被覆盖，下述行号对应评审时的内容。

## 1. 总体判断

最新进展支持：覆盖层窗口位置保持稳定时，仍可能出现原生时钟内容露出。因此，将关注点从“继续纠正覆盖层位置”转向“覆盖范围与绘制时序”是合理的。

但现有证据不足以支持以下绝对结论：

- 原生时钟退出后滞后 2～4 秒已经被证实。
- 覆盖层整个几何、绘制和层级链路已经彻底排除。
- 问题已经根治。

N1 并集遮盖值得做受控实验。建议先验证遮盖效果，再完善正式实现，而不是继续提高采纳时间门槛。

## 2. 第三阶段日志的时间顺序需要纠正

文档称 PotPlayer 退出后仍出现两次 `uia 3618`，据此推断原生布局滞留约 2～4 秒。但当前日志末尾相关片段为：

```text
1682  apply ... below=<播放器>
1683  uia rect=(3618,2076,3769,2160)
1684  uia rect=(3618,2076,3769,2160)
1685  phase->normal
1686  apply ... below=-1 after=(3660,2076,3818,2160)
1687  uia rect=(3660,2076,3818,2160)
```

两条 `3618` 出现在程序判断退出之前，而不是之后。这可能有两种解释：

1. 用户已经看见任务栏，但程序的覆盖状态判断仍未翻转。
2. 两条记录实际上只是全屏期间采样，与退出后的残块没有直接时间对应。

没有同步画面和时间戳，目前无法区分。

此外，周期探测和治疗针都会写同类日志，不能把两条 `uia` 记录直接换算成两个 2 秒周期。当前 `uia rect=` 也未细分 HWND、UIA 等成功来源，不能仅凭标记确定底层探测路径和对象身份。

**建议表述：原生内容在恢复过渡期暴露于认可矩形之外，是有几何依据的候选解释；持续时间和与用户画面的对应仍待验证。**

## 3. 操作后矩形正确的证据范围

新日志中的 `after=(3660,2076,3818,2160)` 有价值：这些采样时刻，覆盖层外框确实位于认可矩形。

但代码仍有以下诊断边界：

- `clock_overlay.rs` 的 `ok=true` 来自 `GetWindowRect(...).is_ok()`，表示读取矩形成功；`SetWindowPos` 的返回值仍被忽略。
- 尺寸更新还经过 `apply_overlay_geometry`，其中 `set_size` 与 `set_position` 分别调用、返回值被忽略，未提供同样完整的操作后记录。
- 外框正确不能单独证明 WebView 内容、不透明背景、实际叠放关系和合成画面全部正确。

因此，合理结论是：**已记录的恢复操作没有把覆盖层外框移错；剩余现象应优先检查原生内容露出与绘制时序。**

“原生残块＋自定义时钟拼成宽时钟”在几何上成立，但仍应通过同步画面确认残块内容和位置。

## 4. 8 秒采纳门的实际保证与代价

`clock_overlay_note_probe` 的判断本质是：本次值与候选相同，且距离首次发现候选至少经过 8 秒。它不是连续观测意义上的“稳定存在 8 秒”。

- 中间探测失败或没有样本，之后再次取得同值，仍可能通过采纳门。
- 过渡态若超过 8 秒，仍会通过。
- 真实 DPI、分辨率或任务栏位置变化也会被延迟处理，不能笼统认为代价可忽略。
- 如果残余问题是原生内容在覆盖区之外，延长门槛无法增加遮盖范围。

建议将 8 秒定义为当前防抖参数，撤销“任何短暂过渡态结构性不可采纳”等过强表述。后续需区分普通布局候选与显示配置失效，避免所有变化统一等待。

## 5. 对 N1 并集遮盖的评价

N1 的方向合理，因为两个需求应分开处理：

- **内容矩形**：自定义文字保持在认可位置。
- **遮盖矩形**：不透明背景覆盖原生时钟可能露出的范围。

### 5.1 提前准备遮盖

退出后才探测并扩宽，可能已经错过第一帧。可以利用被盖期间取得的有效原生矩形，在仍被播放器遮住时准备并集范围，恢复后再确认何时收缩。

并集数据应验证来源、显示器、坐标体系和合理范围，避免错误或过期矩形遮住相邻托盘区域。

### 5.2 内容锚定不能仅依赖窗口右对齐

当前 `ClockOverlayWindow.tsx` 内容相对整个窗口右对齐。如果仅向左扩宽、右缘不变，文字可以保持位置；但并集若向右或上下扩展，文字位置也可能改变。

正式实现宜由外层承载背景，内层保留认可矩形的尺寸及屏幕位置。窗口扩展不应改变文字排版宽度或锚点。

### 5.3 扩大外框不代表背景已经绘制

需要验证新增区域何时真正不透明，防止窗口已经扩宽而 WebView 新背景尚未完成绘制。若采用其他遮盖实现，也需单独验证其位置、层级和显示时序。

扩展区域是否挡住邻近托盘按钮，以及点击是否仍按预期路由，需要随方案一起验收。

### 5.4 收缩需要稳定确认

不能仅凭一次探测回到认可矩形便立即收缩。此前已经出现“正常样本之后又出现中间态”的推断与反馈，收缩策略应通过真实画面验证，避免反复露底。

## 6. 建议的下一步：受控遮盖实验

先做一个能直接验证机制的小实验：

1. 固定自定义内容位置及排版尺寸。
2. 进入全屏后，根据本轮有效观测提前准备并集背景；本轮退出期间保持扩展，不立即收缩。
3. 使用 PotPlayer 真实退出全屏，同步记录带时间戳的探测来源、覆盖状态、目标与实际矩形，以及时钟局部连续画面。
4. 比较启用与未启用扩展背景时，左侧原生残块是否消失、文字是否始终不动。

判据：

- 残块消失且文字不动：支持覆盖不足机制，再完善动态收缩与失效处理。
- 残块仍存在：继续检查背景绘制、层级及残块的真实来源，不应直接提高等待门槛。
- 仅首帧仍露出：重点检查预备时机与新增背景是否在恢复前完成绘制。

ffplay 可保留为辅助回归，但不能替代 PotPlayer 的真实操作。截图和日志需共享可对齐时间轴；仅有 +100ms 截图不能证明全过程无残影。

## 7. 文档整理建议

- 头部状态改为“未根治：覆盖层位置已稳定，残余露底原因验证中”。
- 将历史方案、已撤销推断与当前结论分开，避免同时出现“已根治”和“真机仍失败”。
- 删除或标记过时的 P1+P3 推荐及“本机必然注册表回声”等陈述。
- 将“几何管线彻底清白”收窄为已记录采样时刻的外框证据。
- 将“原生布局退出后滞留 2～4 秒”标记为待同步画面验证，不从日志条数推算持续时间。

**当前建议：验证 N1 的预先遮盖效果，并建立内容矩形与遮盖矩形的分离；暂不将原生时钟自身滞后认定为唯一原因。**
